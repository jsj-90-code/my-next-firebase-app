# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# playwright test artifacts
.playwright-mcp/

# 기밀 원본 데이터(가맹점 실매출 등) - 분석용으로만 로컬에 두고 git에는 올리지 않는다.
/reference/

# 1회성 라이브 검증 스크립트(관리자 자격증명으로 실서비스 Firestore를 읽는다) — 커밋 금지
live-check-out.txt
exp-out.txt
# 로컬 분석 도구·산출물. 운영 스냅샷(매출 포함)이 들어 있어 통째로 제외한다.
# 다만 2026-09-15부터 **공개 통계 수집분은 git에 둔다** — 매출이 한 줄도 없고(소상공인365·SGIS의
# 유동인구·주거인구·업소수·지역평균 매출), PC를 옮길 때마다 85분씩 다시 받는 게 더 큰 손해였다.
# 게다가 재수집하면 12개월 창이 밀려 값이 달라져서, 산식 실험의 대조가 흐려진다.
/.local-tools/*
!/.local-tools/sbiz-floating-population.json
!/.local-tools/sbiz-report-tabs.json
!/.local-tools/sgis-resident-population.json
!/.local-tools/geocoded-sites.json
# 게토 시간대별 가동률 — 매출이 아니라 가동률(%)이라 공개 통계와 같은 급으로 본다.
!/.local-tools/geto-utilization.json
# 매장명 -> 게토 shop_seq + 평가창 대응표. 매출이 아니라 식별자·기간이라 git에 둔다.
!/.local-tools/geto-collect-plan.json

# Firestore 에뮬레이터 실행 로그 (npm run test:rules)
firestore-debug.log

# 세션 중 남는 실행 로그·임시 산출물. 작업 흔적이라 저장소에 넣지 않는다.
*.tmp.log
*.tmp.md
*.tmp.diff
v61-analysis-out.txt

# 로컬 보관 자료를 읽는 1회성 분석 테스트. 일반 실행에서 --exclude '**/_*.test.ts'로 빠지고
# CI·배포 대상이 아니다(관행: docs/releases의 여러 검토 문서 참고).
src/**/_*.test.ts
# 예외: 반복해서 돌리는 측정 하네스·프로브는 PC를 옮겨도 따라가야 한다(force-add 편입).
!src/lib/storeEval/_liveCheck.test.ts
!src/lib/storeEval/_driverProbe.test.ts
# 2026-09-16 — 교과서식 개선 작업용. 집/회사 PC를 오가며 같은 걸 다시 만드는 낭비가 반복돼
# (docs/handoff-office-20260916.md 3절) 편입한다.
!src/lib/storeEval/_textbookSweep.test.ts
!src/lib/storeEval/_newSignalsScan.test.ts
!src/lib/storeEval/_householdSignalTest.test.ts
!src/lib/storeEval/_effectiveTradeArea.test.ts
!src/lib/storeEval/_reportTabSignals.test.ts
!src/lib/storeEval/_demandOnly.test.ts
!src/lib/storeEval/_monopolyDemand.test.ts
!src/lib/storeEval/_utilizationTarget.test.ts
!src/lib/storeEval/_monopolyCalibration.test.ts
!src/lib/storeEval/_marketCapacity.test.ts
!src/lib/storeEval/_draftFormula.test.ts
!src/lib/storeEval/_monopolyFit.test.ts
!src/lib/storeEval/_ageWeights.test.ts
!src/lib/storeEval/_genderCross.test.ts
!src/lib/storeEval/_revenueConversion.test.ts
!src/lib/storeEval/_shareAudit.test.ts
!src/lib/storeEval/_competitionDesign.test.ts
!src/lib/storeEval/_locationRebuild.test.ts
# 2026-09-16 — 세 다리를 다 붙인 전체 성적을 재는 하네스. `_textbookScore.test.ts`가
# gitignore라 PC를 옮기며 사라졌고, 그 사이 입지가 빠진 숫자가 "교과서식 성적"으로
# 돌아다녔다. 같은 일이 또 나지 않게 편입한다.
!src/lib/storeEval/_textbookFull.test.ts
# 2026-09-17 — QSC 잔차 검정. 관문을 통과한 항이라 근거가 저장소에 남아야 한다.
!src/lib/storeEval/_qscResidual.test.ts
# 2026-09-17 — 기존점 캐시 갱신 도구. `.local-tools/refresh-existing-store-cache.mjs`가
# node로 안 돈다(src의 확장자 없는 상대 import를 못 푼다). 대체품이라 편입한다.
!src/lib/storeEval/_cacheRefresh.test.ts
# 2026-09-17 — 존구성 검정. 관문을 **못 넘은** 항이라 더 남아야 한다. 기여가 제일 큰
# 항목이라 "그럼 존구성부터 손보자"는 얘기가 또 나올 자리인데, 그때 이 하네스를 다시
# 만들지 않고 돌려보면 된다. 표본이 45~50곳 되면 r 기준 p=0.066을 다시 잰다.
!src/lib/storeEval/_zoneComposition.test.ts

# 에이전트 도구의 기계별 설정
/.agents/
/.claude/
/.mcp.json
/skills-lock.json

# 검증용으로 받아본 내보내기 이미지(2026-09-11 FHD 확인). 결과는 docs/review-needed에 적었다.
/fhd-zoom-200.png

# Codex가 분석용으로 받아둔 외부 자료(PDF 등). 용량이 커서 저장소에 넣지 않는다.
/tmp/

# 예외: 다시 만들 수 없거나 재수집 비용이 큰 수집물. 매출이 안 들어 있어 올려도 된다.
# (2026-09-16 — PC를 옮길 때마다 사라져서 같은 걸 다시 만드는 낭비가 반복됐다)
!.local-tools/roadview-judgments.json
!.local-tools/kakao-directional.json
# 2026-09-18 — 사양 검정. GPU 성능지수표를 **채택한** 근거(대조군 p=0.010)와 CPU·RAM을
# **안 바꾼** 근거가 여기 다 있다. 특히 "CPU도 같이 고치자"는 얘기가 또 나올 자리라,
# 그때 이 하네스를 다시 만들지 말고 돌려보면 된다. 표본 45~50곳 되면 CPU를 다시 켜 본다.
!src/lib/storeEval/_specScore.test.ts
# 2026-09-18 — 오송점 둘. 회사 PC에서 만든 _osongLocationScores가 바로 이 ignore에 걸려
# 커밋이 안 됐고, 인계 문서가 그 하네스를 참조하는데 집 PC엔 없어서 다시 만들었다.
# 둘 다 아직 안 끝난 얘기라(선점경쟁 근거·42m 구간 판단) 또 불려 나온다.
!src/lib/storeEval/_osongLocationScores.test.ts
!src/lib/storeEval/_osongEffectiveRadius.test.ts
# 2026-09-18 — 자사 먹거리 기준값은 **조사 결과가 나오면 다시 건다**(사용자: "조사를 먼저 하자").
# 그때 이 하네스를 다시 만들지 말고 값만 바꿔 돌리면 된다. 인테리어 4.0 유지 근거도 여기 있다.
!src/lib/storeEval/_ownFoodInterior.test.ts
