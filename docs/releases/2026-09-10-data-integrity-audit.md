# 데이터 정합성·버그 점검

2026-09-10. 원래 우선순위 1번이었는데 건너뛰었던 항목을 실행했다. 이 프로젝트가 두 번 당한
사고 유형(퍼센트 파싱 소실 2026-08-22, 관리점수 미수집 2026-08-31)은 전부 **에러 없이 조용히
null이 되는** 부류라, 그걸 잡는 데 초점을 맞췄다. 읽기 전용 점검이고 산식 변경은 없다.

## 방법

필드명을 추측하면 오탐이 난다(실제로 `competitorSummary.total`을 `totalCount`로 착각해 한 번
틀렸다). 그래서 **문서에 실제로 존재하는 키를 전수 열거**한 뒤, 의심 열만 원본 시트와 1:1로
대조했다.

## 결과 — 동기화는 건전하다

**01_점포기본정보 → `storeEvalExistingStores`: 검사한 6개 열이 전부 시트와 정확히 일치.**

| 시트 열 | 시트 유효값 | Firestore 유효값 |
|---|---:|---:|
| 평가기준_PC대수 | 12 | 12 |
| 자사_커플존 | 38 | 38 |
| 자사_VIP존 | 29 | 29 |
| 자사_프렌즈존 | 19 | 19 |
| 자사_퍼스트클래스존 | 19 | 19 |
| 자사_요금표_시간당 | 40 | 40 |

`evaluationPcCount`가 41곳 중 12곳뿐인 건 소실이 아니라 **시트에 원래 12곳만 입력돼 있는 것**이다
(나머지는 `evaluationPcCount ?? pcCount` 폴백이 설계대로 동작).

`managementScore` 결측 37건도 정상이다 — 전부 미조사 경쟁점(조사수준 "간략", kakao 자동수집,
"경쟁점 없음(독점상권 확인)" 마커)이고, 2026-08-31에 넣은 미조사 기본점수 2.5 폴백이 받는다.

폐기 필드(`comfortScore`/`interiorLevelScore`/`interiorConditionScore`/`seatZoneScore`/`foodBrand`,
각 23건 전부 null)는 8/31 시설평가 개편의 잔재로, 계산에서 빠진 게 맞다.

## 발견 1 (중간) — 05시트의 신규후보지 경쟁점은 앱으로 동기화되지 않는다

`05_경쟁점정보`에는 기존 가맹점 행뿐 아니라 **신규후보지(N코드) 행 30건**이 들어 있다
(N001:10, N002:3, N003:1, N004:3, N005:9, N009:4). Firestore에도 같은 수의 문서가 있어 얼핏
동기화된 것처럼 보이는데, 문서 id가 `N002_import_1` 형태다. **저장소 전체를 뒤져도 이런 id를
만드는 코드가 없다** — 지금은 사라진 1회성 임포트의 흔적이고, 이후 시트 수정을 반영하는 경로가
없다.

실제로 어긋난 사례가 이미 하나 있다.

| | 시트(05_경쟁점정보 170행) | Firestore(`N002_import_1`) |
|---|---|---|
| N002 춘천퇴계점 / 옵티멈존pc카페 춘천석사점 | 퍼스트클래스존 **8** | 필드 자체가 없음 |

시트에서 값이 있는데 앱에는 없는 유일한 건이다(VIP존·프렌즈존은 시트에서도 전 행 0이라 차이가
안 생겼을 뿐이다). 시트 유래 후보지 경쟁점 30건 전부가 2026-08-31에 새로 생긴 컬럼
(퍼스트클래스존/프렌즈존/VIP존/팀룸좌석수/일반2인석)의 키를 아예 갖고 있지 않다.

- **지금 영향**: N002 후보지 경쟁점 한 곳의 존구성 점수가 실제보다 낮게 잡힌다 → 그 후보지의
  자사 예측이 소폭 과대. 다른 후보지는 해당 컬럼이 시트에서도 비어 있어 차이 없음.
- **앞으로의 위험이 더 크다**: 누가 05시트에서 후보지 경쟁점을 고쳐도 앱에 반영되지 않는데,
  경고가 뜨지 않는다. 관리점수 사고와 정확히 같은 모양이다.
- **고치지 않고 남긴 이유**: 후보지는 웹 UI에서 관리하는 게 원칙이고(`docs/data-issues.md`
  2026-08-31 "07_신규후보지 시트는 손대지 않음"), 시트 값을 자동으로 밀어넣으면 웹에서 입력한
  값을 덮어쓸 수 있다. **어느 쪽이 정본인지는 사람이 정해야 한다.**
- **선택지**: ① 05시트에서 N코드 행을 걷어내고 웹을 정본으로 확정, ② 후보지 경쟁점도 시트
  동기화 대상으로 편입(덮어쓰기 규칙 필요), ③ 현행 유지하되 N002 한 건만 웹에서 수동 입력.

## 발견 2 (경미) — 죽은 코드 제거

`store.ts`의 `deleteDemandPoint`는 어디서도 호출되지 않았고, `firestore.rules`가
`storeEvalDemandPoints`의 클라이언트 delete를 의도적으로 막아둬서 호출했다면 권한 오류로
실패했을 함수다(같은 파일 `deleteCandidate` 주석이 이미 그 제약을 설명하고 있다). 제거하고
경위를 주석으로 남겼다.

## 그 외 확인

- `npm run lint` 오류 0건(경고 2건은 저장소에 없는 로컬 측정 하네스).
- 경쟁점 문서 224건 중 시트 유래 188 + kakao 자동수집 + 웹 직접입력으로 구성. 행 수는 후보지별로
  시트와 정확히 일치한다.

## 재현

`.local-tools/keyCensus.mjs`(컬렉션별 실제 키 전수), `.local-tools/sheetVsFirestore.mjs`
(시트↔Firestore 1:1 대조). 둘 다 읽기 전용이고 `.gitignore` 처리돼 저장소에 올라가지 않는다.
