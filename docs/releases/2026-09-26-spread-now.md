# 2026-09-26 밤 — 1단계(실험실 개선) 첫 측정: 퍼짐을 지금 배선으로 다시 잼 (측정만, 채택 0)

사용자 계획(09-26 밤): 1 실험실 산식 개선 → 2 실험실 자료를 V62에 넣을지 검토 → 3 두 산식을 주소만 초기평가에 완전 자동으로.
하네스 `src/lib/storeEval/_spreadNow.test.ts` — `_layerSplit (6)`과 같은 조립(MAE 4.04로 일치 확인). `_spreadLayers`는 09-22 배선(4.51)이라 낡음.

## 결과
- **퍼짐비 1.21배**(예측 log SD 0.248 · 실측 0.205, r=0.753). 인계의 "1.73배"(09-21)는 낡은 값. 보정 기울기(실측~예측) 0.62 — 과장은 남아 있다.
- 예측 퍼짐의 **92%가 수요 층**. 수요 층은 오차와 r +0.37(클수록 과대).
- 수요 안: **유동 사람수 SD 0.72인데 실측과 r 0.018** — 신호 없이 퍼짐만 보탠다. 주거는 실측 r 0.22.
- 가설 "유동인구(통신 기반)에 주민이 들어 있어 두 번 센다" → **기각**. 유동400 ÷ 400m 주민 추정 중앙 8.1배(1.5 미만 1곳), 주민 몫을 빼도 실측 r 0.026→0.015.
- 잔차 훑기(입력 숫자 사실 전부 vs 오차, 유의선 ±0.41): **입력 사실 중 넘는 것 0**. 넘는 건 산식 출력(예측 가동률 +0.58 등)뿐 —
  "빠진 사실"이 아니라 **수요 자료의 측정 잡음이 예측을 벌리는** 모양(회귀 희석).

## 판단
- 지금 입력만으로 실험실 퍼짐을 기전으로 줄일 길은 안 보인다. 누르는 길(b 보정·θ·α·고리·평면 배율)은 전부 이미 닫혔다(09-19~25).
- 남은 건 **신호 있는 새 수요 자료**뿐이다. 1단계를 더 파기보다 2·3단계로 넘어가는 것을 권했다.
