# 2026-09-23 — 실험실 산식 묶음 채택: 존구성 비중 축소 · θ 1.75 · 1km 밖 고리 λ600

운영 V62는 **바뀌지 않았다.** 실험실(교과서식, `textbookModel.ts`) 기본값만 바뀐다.
사용자 결정: *"한번 해볼까?"* (질문 *"빼는 게 정답임?"* 뒤).

## 바뀐 값 (셋이 한 묶음 — 하나만 되돌리면 안 된다)

| 파라미터 | 전 | 후 | 뜻 |
|---|---|---|---|
| `qualityWeights.zone` | 0.238 | **0.06** | 경쟁력점수에서 존구성 비중. 항목은 남기고 비중만 1/4 |
| `qualityExponent` (θ) | 3 | **1.75** | 경쟁력점수 차이를 몇 제곱해서 점유율에 반영하나 |
| `residentRingDecayM` (λ) | 0(꺼짐) | **600** | 1km 밖 고리 감쇠(m). 주거 이용자가 1km만 셀 때의 중앙 1.64배 |

## 왜

바깥 표본(경쟁점 핑봇 47곳)이 두 가지를 잡아냈다. (가) 수요 수준이 약 1.5배 작다. (나) 옛 배선은 자사우위를
3.50배로 예측하는데 실측은 1.74배(1년차 자사 9곳 vs 성숙 경쟁점, 같은 달)다. 둘은 독립이라 하나만 고치면
다른 쪽이 무너진다 — θ만 내리면 자사·경쟁점이 같이 −10%p 과소, 고리만 켜면 자사가 +10%p 과대.

"빼는 게 정답인가"에 비중 0 / .06 / .12 / .18 / .238을 돌렸다. 비중을 남길수록 자사 r·짝 r이 오른다(존구성이
순서에는 일한다). 세게 두면 자사우위를 부풀린다. 사전 등록 기준(두 편향 ±3%p → 자사우위 1.74 근접 → 자사 r)
1위가 존 0.06·θ1.75·λ600이었다. 팀룸 유입 기전(사용자)이 있으니 항목은 자르지 않는다.

## 성적 (세 잣대 · 가동률)

| | 옛 배선 | 채택 |
|---|---|---|
| 자사 37곳 MAE / 편향 / r | 5.84%p / −1.4%p / 0.504 | **7.82%p** / +0.1%p / 0.337 |
| 경쟁점 47곳 MAE / 편향 / r | 12.6%p / −10.9%p / 0.235 | **9.4%p** / −1.6%p / 0.174 |
| 동네 안 짝 r / 짝 과장 | 0.598 / 1.66배 | 0.510 / 0.89배 |
| 자사우위 예측 (실측 1.74) | 3.50배 | **1.77배** |

안쪽(자사)이 나빠지고 바깥(경쟁점)이 좋아진다 — 과적합 해소 패턴. 자사 MAE 악화 +2.0%p는 2SE(1.6)를 넘는
확실한 값이다. 숨기지 않는다.

## 못 가른 것 · 남은 것

- θ1.75 vs θ2, 존 0.06 vs 0.12, λ500~700은 사전 기준으로 못 가른다(목표 1.74가 n=9). 표본이 늘면 다시 잰다.
- **비대칭**: 수요는 2km 고리, 경쟁은 사실상 500m. 경쟁 감쇠 확대는 기각됐다(자사 r 0.34→0.14). 다음은 고리
  수요를 거리로 배분하는 구조(중력형). 밀집 도심 요동(발산역 +18 · 수원망포 +16 · 화명대로 −14%p)이 그 표시등.
- 후보지 예측: 고리 인구가 실험실 Firestore(`storeEvalLabResidentRings`)에 후보지 13곳도 있다. 새 후보지는
  SGIS 수집 후 `scripts/writeLabResidentRingsToFirestore.mjs`를 돌려야 고리가 붙는다. 없으면 1km만 센다.

## 같은 날 저녁 — 고리 점유율을 "가까운 쪽으로"(gravity)로, λ 600→800

사용자: *"예상가동률 편차가 더 커진 것 같은데?"* → 맞았다. 자사 예측 SD 8.1→9.9(실측 6.4), 최악이 발산역 +23·
수원망포 +21%p. 원인은 비대칭 — 1~2km 사람은 세면서 그 곁 PC방은 경쟁으로 안 셌다(경쟁 감쇠는 사실상 500m).
사용자: *"근거가 있어서 감수해야 하는 부분이면 인정인데 값을 위해 조정한 거라면 별로."*

고친 방법은 기하다. 우리에서 d 떨어진 경쟁점이 반경 r 고리 주민 중 우리보다 가까운 비율 = arccos(d÷2r)÷π.
이걸 경쟁점 무게로 써 고리 띠마다 점유율을 따로 구하고 1km 안 점유율과 이용자 수로 가중평균한다. 맞춘 계수 없음.
(`residentRingShare: "gravity"` · `ringRivalFraction`)

⚠️ 하네스도 고쳤다 — 경쟁점 주인공에 2km 경쟁점이 빠져 있어 gravity에서 우리만 깎이는 비대칭이 있었다.
그래서 위 표의 채택 칸 숫자가 조금 바뀐다(core λ600: 경쟁편향 −1.6→−3.3 · 자사우위 1.77→1.84).

| | core λ600 (낮) | **gravity λ800 (저녁 채택)** |
|---|---|---|
| 자사 MAE / 편향 / r | 7.82%p / +1.3 / 0.337 | **6.88%p** / −1.2 / 0.187 |
| 자사 예측 SD (실측 6.4) / ±5%p | 9.9 / 14곳 | **6.7** / 15곳 |
| 경쟁점 MAE / 편향 | 8.7%p / −3.3 | 8.2%p / −2.7 |
| 짝 r | 0.551 | 0.533 |
| 자사우위 예측 (실측 1.74) | 1.84배 | 1.65배 |
| 자사 최악 3곳 | 발산역 +23 · 수원망포 +21 · 구미산동 +19 | 구미산동 +21 · 전대후문 −18 · 송도 +14 |

λ를 800으로 올린 건 gravity가 고리 몫을 줄여 수준이 내려가서다 — **눈금 값이고 사전 기준(두 편향 ±3%p)으로
자료에서 골랐다.** 치르는 값은 동네 간 순서(자사 r 0.34→0.19, n=37에서 1SE 안팎). 짝 r은 유지된다.

## 같은 날 밤 — 특수수요 배수를 다시 켰다 (대학가 1.3 · 산업단지 1.4 · 군부대 2.25→2.0)

사용자: *"특수상권 배수 켤 수 있는 건 켜보자 — 대학상권·군인·산업단지."*
전대후문(대학가) −18%p가 출발점. 2026-09-20에 껐던 이유는 "축척을 독점 3곳에서 맞추는데 그 매장이 특수수요
유형이라 순환"이었고, 2026-09-21 축척 고정(원장 8.20h)으로 그 이유는 사라졌다. 이번엔 **바깥 표본**을 같이 봤다.

```
  유형(지금 배선)   자사 n·편향        경쟁점 n·편향       → 같이 과소면 수요 배수가 맞는 자리
  대학가            5곳  −8.9%p        5곳  −3.3%p        ✓
  산업단지          2곳 −10.1%p        1곳 −17.9%p        ✓ (표본 없음에 가깝다)
  군부대(2.25)      2곳  +1.4%p        2곳  +7.5%p        과함
```
기준(사전): 그 유형 자사·경쟁점 편향 |합| 최소. 결과 대학가 **1.30**(−1.2/+2.9, 둘 다 ±3%p 안) · 산업단지 **1.40**
(자사 −0.9, 경쟁점 한 곳은 못 맞춤. 2026-09-16 1차 측정 1.39와 일치) · 군부대 **2.00**(−2.6/+4.9).
전체 성적(넷+배수 모두 켠 최종): 자사 MAE **6.11%p** · 편향 +0.1 · 예측 SD **6.4 = 실측 6.4** · ±5%p 16/37 · ±10%p **32/37**(옛 26) ·
경쟁점 편향 −2.1 · ±5%p 20/47(옛 10) · 자사우위 1.65(실측 1.74). 남은 최악: 구미산동 +21(항아리상권) · 송도 +14(요금전쟁) · 문경시청 −12.
⚠️ 산업단지·군부대는 값이 아니라 방향이다. 표본이 늘면 다시 잰다.

## 같은 날 밤 — 항아리상권 보정을 켰다 (`useRingEnclosure: true`)

사용자 정의: *"항아리 = 상권이 그 주변에만 형성되어 있고 반경 이상에는 상권이 없다."* 지도 54장을 AI가 방향별로
"1~2km 고리에 사람 사는 동네가 있나"를 예/아니오로 판정(scripts/tradeArea). 없는 방향 수 ÷ 4를 고리 인구에서 깎는다.
52곳 중 8곳만 깎인다(시흥능곡·오송 2방향 · 구미산동·화명대로·시흥배곧·증평·문산·영월 1방향) — 사용자 대조 *"맞음"*.
성적: 자사 MAE 6.11→**5.86%p** · r 0.300→0.342 · SD 6.4→6.2 · 편향 +0.1→−0.4 · 경쟁 −2.1→−2.3. 비용 없는 작은 이득.
1차 문항(단절)은 폐기. "유일 상권 → 고리 0"은 2승 2패라 규칙 아님. 판정 저장: `storeEvalLabTradeAreaJudgments`.

## 되돌리기

네 값을 같이 되돌린다: `zone: 0.238` · `qualityExponent: 3` · `residentRingDecayM: 0` · `residentRingShare: "core"`(λ0이면 무관).
검산 자리: `_bundleCandidate.test.ts` (0) — 옛 배선 자사 MAE 5.84%p.

## ⛔ 2026-09-24 — 되돌렸다 (사용자 결정)

사용자: *"난 옛 데이터가 더 정확했다는 느낌을 받는다. 호구포역점 실제 나올 수 없는 매출이고, 하안금당도 높고 오송점도 높고."*
*"상권 늘리니까 매출 낮은 매장이 확 뛰어올라서."* 되돌린 것: θ 3 · 존구성 0.238 · λ 0 · 특수수요 배수 군부대 2.25만(대학가·산업단지
1.0). `residentRingShare: "gravity"`·`useRingEnclosure: true`는 값만 남겼다(λ0이면 안 읽힌다). 자료(고리 인구 54건·항아리 판정 52건)와
배선은 그대로 살아 있다.

근거(`_labCandidate.test.ts` (8), 2026-09-24):
```
  후보지 13곳  옛 배선 → 새 기본값(λ800·gravity·항아리·배수)   평균 +4.8%p · 3%p↑ 9곳 · 내림 0곳
    호구포역 41.3 → 55.0(상한)  오송 28.9 → 40.0  하안금당 32.4 → 42.4  평택소사벌 21.2 → 28.1
    신중동(밀집 도심) 23.7 → 26.9 — gravity는 일했다. 문제는 경쟁점 1~2곳인 자리: 점유율이 θ와 무관하게 고정이라 수요 ×2가 그대로 통과
  자사 40곳    전체 5.85 → 6.21%p · 라벨(송도·구미산동·동탄북광장·전대후문) 뺀 36곳 5.68 → 5.56%p (2SE 1.94) — 같다
  실측 수요 잣대(14동네, 실측하한 ÷ 산식수요)  λ0 1.14배 · λ800 0.58배 — 고리가 산식 수요를 실측 하한 위로 밀었다
```
치르는 값(다시 나빠지는 바깥 잣대): 경쟁점 47곳 편향 −2.3 → −11%p · 자사우위 예측 1.65 → 3.5배(실측 1.74). 이건 그대로 남은 숙제다.
사용자 회고: 1km·유동 400m는 처음부터 좁혀 잡은 값이다 — 전대상대(전대후문과 배후지 공유, 사람은 후문에 몰림)·수원망포(500m쯤 역
유동이 딸려 들어옴). "상권을 넓히면 안 맞는다"는 그때도 같은 증상이었다.
검산: `_bundleCandidate` (0) 옛 배선 5.84%p = 지금 기본값.

### 같은 날 오후 — 특수수요 배수를 옛 배선에서 다시 재서 켰다
사용자: *"특수수요 배수값은 평균에 제일 유리한 값 넣어주면 될 것 같은데? 대학/군인/산업 … 관광/유흥도 켜보던가."* "평균" = 그 유형
자사 매장들의 **예측 가동률 평균 = 실측 평균**(편향 0). `_bundleCandidate` (7b), 옛 배선.
```
  대학가 1.30 (5곳 −7.9→+0.2)  ·  산업단지 1.40 (2곳 −10.2→−1.0)  ·  군부대 2.00 (2곳 +3.1→−1.1)  ·  관광유흥 1.40 (2곳 −9.0→≈+0.4)  ·  기타 1.0 (4곳 −1.1)
  넷 켠 성적: 자사 37곳 MAE 5.84 → 4.85%p · 편향 +0.4 · 오차 SD 7.4→6.2 · ±5%p 18→21 · ±10%p 26→30 · 최악 양주덕정 −13 · 일산탄현 +11 · 문경시청 −11 · 시흥배곧 +11
  ⚠️ '전부 평균' 바닥 4.24%p를 아직 못 넘는다. 경쟁점 47곳 1:1 편향 −10.9%p 그대로(θ 문제, 배수 자리 아님)
  ⚠️ 대학가는 매장별로 갈린다 — 전대후문 −8·청주대 −6 vs 부경대 +10·전대상대 +5. 배수 하나로 5곳을 ±3%p에 못 넣는다
```
09-23 값과 같은 숫자지만 근거 배선이 다르다(그때는 λ800 gravity). 첫 세 값은 동료 세션 커밋 07b2f9c에 파일 단위로 섞여 들어갔다.

재는 자리: `_bundleCandidate.test.ts` · `_zoneAsDemand.test.ts` · `_demandPathOutside.test.ts` · `_rivalAsSubjectFull.test.ts`
