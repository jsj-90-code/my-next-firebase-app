# 인계 — 2026-09-21 집 PC. **축척을 못 박고 변별력을 되돌렸다**

앞 문서를 먼저 읽을 것:
- `docs/handoff-20260921-night3.md` — 원장 실측(1인당 8.20h)까지
- `docs/releases/2026-09-21-scale-and-share.md` — **오늘 작업 전체. 채택 2건의 근거**

---

## 다음 세션에 붙여넣을 것

```
점포평가 이어서 하자. docs/handoff-20260922-office.md 읽고 시작해.

[시작 절차]
1. git pull
2. node scripts/dumpValidationSnapshot.mjs
3. npx vitest run --exclude "**/_*.test.ts"     ->  808 통과

[오늘 할 일 — 어제 못 한 하나]
1인당 이용시간을 **상권(매장)별로** 적용할 수 있는지 검열한다.
  - 원장이 있어야 한다. 집 PC엔 없어서 미뤘다
  - 1인당 월시간 = 월 방문횟수 × 1회 평균 체류시간 으로 먼저 쪼갠다
  - 설명 변수 후보: 정액시간 비중 · PC방이용권 비중 · 야간 시작 비중 ·
                   장시간(6h+) 건 비중 · 시간당 단가
  - ⚠️ 개월차와 강하게 엮여 있다(r≈0.68). 통제 안 하면 전부 가짜 상관
  - .local-tools/_perUserSplit.mjs 에 초안이 있다(아직 안 돌렸다)

[규칙]
- 판정 기준은 **가동률**이다. 매출 MAPE 아니다
- 계수·점수를 내 확인 없이 고르지 마라. 재고 표로 남겨라
- **실무에 맞는 형태여야 한다. 값에 맞춘다는 느낌 말고**
- 운영 V62 금지 (calc.ts·evaluate.ts·usageRevenue.ts·cronSync.ts·settings.ts)
- Firestore 쓰기는 내가 시킬 때만 · 파일 삭제 금지
- 셸(sed·node -e)로 파일 고치지 마라. Edit/Write만. 읽기용은 괜찮다
- MAE만 보지 마라. 최악·SD를 항상 같이 봐라. 단위를 섞지 마라(%p는 %p로)
- 훑기 유의선은 ±0.41. 무작위 대조군까지 통과해야 한다
- 닫힌 갈래를 다시 파지 마라 (이 문서 4절)
- 설명·화면·주석은 산식 바꾼 그 커밋에서 같이 고친다
- **원장을 새로 다룰 땐 관문부터** — 원장 총시간 ÷ (PC×720)이 그 달 가동률 필드와 맞는지

[산식 현재 상태]
- 거리 감쇠 R200 λ200 · wf 0.9593
- 지수 눈금 보정 **b=0.45** · c=0.169 · 닻 0.3011     ← 어제 0.327에서 올렸다
- 축척 hoursPerUserPerMonth **8.20h 고정**(원장 실측)  ← 어제 역산을 끝냈다
- 가동률 MAE 4.175%p · ±5%p 24/38 · 최악 11.91%p · 편향 −0.18%p
- 매출 MAPE 17.18% · ±20% 24곳 (실험실 산식)
- 운영 V62(MAPE 8.832%)는 안 건드린다
```

---

## 0. 지금 상태

```
커밋  2ec152b  진단 셋 (하네스만)
      7aafc9a  ✅ 축척 8.20h 고정
      b28b0c1  ✅ b 0.327 -> 0.45
시험  808 통과 · 타입 통과 · 빌드 통과 · 푸시 완료
```

---

## 1. ⭐ 채택 (A) — 축척을 원장 실측으로 못 박았다

`hoursPerUserFixed: true` · `hoursPerUserPerMonth: 8.20`.
이제 `fitHoursPerUser`는 **역산을 안 한다**(false로 두면 옛 경로가 그대로 살아 있다).

근거 둘:
1. **성적으로 치르는 값이 없다.** 실측 범위(6.95~9.39h) 전체에서 MAE가 둔감하다.
2. ⭐ **역산값은 다른 계수에 딸려 움직인다.** 자료가 그대로인데 b=0.327에서 8.3854h이던 값이
   b=0.45에서 **7.7446h**가 된다(적합식이 log를 b로 나눈다). 그 값은 애초에 "1인당 이용시간"이
   아니었던 것이다. 실측 8.20h는 b와 무관한 상수다.

**그래서 열린 것**: 축척이 상수가 되니 독점매장에서 남는 어긋남이 곧 **수요 추정 오차**다.
재 보니 독점 3곳에서 날 수요식이 **일제히 19% 과대**하고(1.211·1.183·1.174)
**퍼짐은 1.03배**다 — 수준은 틀렸고 변별은 정확하다.

---

## 2. ⭐⭐ 채택 (B) — b를 0.327 → 0.45

사용자 지적(*"점유율이 높게 나타나고있어 변별력이 떨어진다"*)의 **범인은 점유율이 아니었다.**

점유율은 변별을 하고 있다 — '필요 점유율'(실측가동률 ÷ 수요식)과 로그상관 **0.790**,
무작위 대조군 1,000회 p=0.000, 수요밀도를 뺀 **편상관 0.469(p=0.001)**.
오히려 산식 점유율 퍼짐(4.41배)이 필요(7.08배)보다 **좁다**.

후보지가 21~34%로 몰려 보이던 건 **b의 수축**이었다.

| b | 가동률MAE | 가동률최악 | 매출MAPE | 매출최악 | ±20% | 후보지 간격 |
|---|---|---|---|---|---|---|
| 0.327 | **3.83%p** | 13.16%p | **16.11%** | 56.9% | **27곳** | 1.62배 |
| **0.45** | 4.18%p | 11.91%p | 17.18% | **51.8%** | 24곳 | **2.03배** |
| 0.55 | 4.67%p | **10.91%p** | 18.98% | 55.2% | 23곳 | 2.44배 |

⚠️ **자료는 b를 못 고른다**(중첩 LOO가 훈련평균과 같다). 용도로 고른 값이다.
0.45는 가동률·매출 **최악이 둘 다 주는** 자리다. 0.55는 가동률 퍼짐이 실측과 딱 맞지만
매출 ±20%가 4곳 빠져서 물러섰다.

후보지 값: 하안금당 34.3→**37.8%** · 영월 21.2→**18.6%** · 창원상남 21.4→19.2%.
⚠️ **순위는 거의 안 바뀐다**(스피어만 0.989). 바뀌는 건 간격이다.

---

## 3. 남은 일 — (3)번 하나가 통째로 밀려 있다

**1인당 이용시간을 매장별로 적용할 수 있나.** 지금은 전 매장 8.20h 한 값이다.

- 후보지엔 원장이 없으니, 쓰려면 **"무엇이 1인당 시간을 정하는가"**를 알아야 한다
- 원장 안에서 바로 잴 수 있는 것부터: `1인당 월시간 = 월 방문횟수 × 1회 평균 체류시간`
- ⚠️ n=8이고 개월차와 r≈0.68로 엮여 있다. **개월차를 통제하지 않으면 전부 가짜 상관이 나온다**
- ⚠️ 원장 경로가 `C:/Users/ISENS/Desktop/좌석가동률_7월`(회사 PC)로 박혀 있다.
  집 PC엔 없어서 `_ledgerPerUser`가 조용히 skip된다. **양쪽 다 찾게 고치는 게 낫다**

곁다리로 같이 받을 것: **독점매장 원장**(탕정역 2024-07~2025-06 · 광주각화 2024-12~2025-11 ·
남악 2025-04~2026-03). 받으면 19% 수준 오차의 정체를 직접 가를 수 있다.

---

## 4. ⛔ 어제 닫은 갈래

| 갈래 | 결과 |
|---|---|
| **외부옵션(PC방 안 가는 몫)으로 19%를 적기** | **기각** — MAE 3.76→3.91%p로 나빠지고 **b가 1로 안 돌아온다**(어느 칸에서도 0.2~0.327). b<1은 수준 보정이 아니라 **퍼짐 보정**이다 |
| 외부옵션 형태(자사PC 비례 vs 절대 대수) | **자료가 못 가른다**(3.907 vs 3.903%p) |
| θ 다시 훑기(1~4) | **못 고른다** — MAE는 θ가 클수록 좋고(θ=4 3.72%p) 필요 점유율 상관은 θ=2~2.5가 최고(0.843). 두 자가 다른 답을 준다 |

---

## 5. 🟡 남은 이상한 점 (사용자 지시로 안 건드림)

**입지배율이 점유율을 100%로 자르는 곳이 기존점에 2곳** — 구미산동·문경시청.
독점 3곳은 당연하고 이 둘은 입지배율 때문이다. 인계문 밤(2) 4절의 영월 2.054·호구포역과
**같은 병이 기존점에도** 있다는 뜻이다.
사용자: *"일단 여긴 억지로 지금 맞추는 건 하지 말고, 수요쪽 가동률쪽 먼저."*

그 밖에:
- 단가층(평균 10.3% 독립 오차) — 가동률 끝난 뒤
- 탕정역점에 경쟁점이 생겼는데 스냅샷 `competitorIp`가 0이다(Firestore 쓰기라 지시 대기)
- 표본 45~50곳(2027 상반기)에서 b·c·θ·감쇠 전부 다시

---

## 6. 어제 밟은 함정

1. **b=1을 `indexCalibration: null`로 넘기면 딴 산식이 된다** — 입지 지수가 c/b에서 1로 튄다.
   격자가 b=1에서만 불연속이라 걸렸다.
2. **"b는 단조변환이라 순위를 안 바꾼다"는 내 단언이 틀렸다.** 점유율 100% 상한 때문에 움직인다.
3. **순열검정은 간접 경로를 못 가른다.** 필요 점유율의 분모에 수요밀도가 있으면
   "우연이 아니다"가 "따로 하는 일이 있다"를 뜻하지 않는다. 편상관을 같이 내야 한다.
4. **하네스 잠금장치에 계수를 글자로 박으면 채택하는 날 깨진다.** 본체에서 읽게 할 것.
5. **화면이 산식 한 단계를 통째로 빠뜨리고 있었다** — 눈금 보정을 2026-09-21 낮에 채택했는데
   산식 설명 3단계에 그 이야기가 없었다. 계수를 바꾸면 화면도 같은 커밋에서.

---

## 7. ⚠️ 시험 1건 실패가 정상일 수 있다

`storedAccuracyParity`의 "기존점 캐시" 항목은 매일 **06:00 KST 크론**이 고친다.
산식을 바꾼 날 밤에는 빨간 게 맞다. 06시가 지나도 빨갛다면 그때 크론을 의심한다.

`_*.test.ts` 연구 하네스 중 **7개가 전부터 깨져 있다**(내 변경 때문이 아니다 — 확인했다).
`_decayShapeSelection`은 전체 실행 시 5초 타임아웃으로 넘어지고 혼자 돌리면 통과한다.

## 8. 파일

| 경로 | 내용 |
|---|---|
| `src/lib/storeEval/_scaleMeasured.test.ts` | 축척 실측 고정 · 독점 수요 오차 19% |
| `src/lib/storeEval/_shareDiscrimination.test.ts` | 필요 점유율 · 외부옵션 · θ |
| `src/lib/storeEval/_spreadChoice.test.ts` | 순열검정 · 편상관 · b 격자 · 매출 환산 |
| `src/lib/storeEval/_ledgerPerUser.test.ts` | 원장 1인당 시간(회사 PC에서만 돈다) |
| `.local-tools/_perUserSplit.mjs` | (3)번 초안. **아직 안 돌렸다** |
