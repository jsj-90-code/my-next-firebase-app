# 인계 — 2026-09-28. **고리 묶음을 되돌렸다(사용자 결정). 실험실 기본값 = 2026-09-23 오전 상태(검산 5.84%p)**

앞 문서: `docs/handoff-20260927.md`. 별도 갈래: `docs/handoff-address-only-tool.md`(다른 세션).

> ⭐ 한 줄로: 후보지 13곳을 옛/새로 나란히 찍자 새 배선(θ1.75·존 0.06·λ800 gravity·항아리·배수)이 **한결같이
> +4.8%p**(9곳 3%p↑, 내림 0, 호구포역 55% 상한·오송 +11)였다. 사용자: *"옛 데이터가 더 정확했다는 느낌. 호구포역점
> 실제 나올 수 없는 매출."* → θ3 · 존 0.238 · λ0 · 배수 군부대 2.25만으로 되돌렸다. 자료·배선은 남겼다. 운영 V62 무변경.

## 이날 한 것

| 커밋 | 내용 |
|---|---|
| b2fb770 | `_bundleCandidate` (0) 검산 기준 시점 맞춤 — 5.84%p는 배수 재점화(539b17b) 전 값. 검산 칸 배수를 군부대 2.25만으로 |
| 5d26927 | `_labCandidate`에 고리·항아리 자료 배선(규칙 6 위반 상태였다) + **(8) 후보지 옛/새 나란히 · 자사 라벨 뺀 성적** |
| 13858ff | **되돌림** — textbookModel 기본값 넷+배수 · lab/page.tsx 설명 4곳(고리·배수·θ·존구성, 값에서 읽어 분기) · 릴리스 노트 되돌림 절 · 1km/400m 근거 주석 |

배포: vercel --prod (my-next-firebase-app-one.vercel.app) · 정규 테스트 865 통과 · 빌드 통과.

## 되돌림 근거 (`_labCandidate.test.ts` (8))

```
  후보지 13곳   옛 → 새    호구포역 41.3→55.0(상한)  오송 28.9→40.0  하안금당 32.4→42.4  평택소사벌 21.2→28.1  울산삼산 13.6→18.4
               신중동(밀집, 경쟁 10) 23.7→26.9 — gravity는 일했다
               평균 +4.8%p · 3%p↑ 9곳 · 내림 0곳
  기전         경쟁점 1~2곳이면 점유율이 θ와 무관하게 거의 고정 → 수요 ×2가 그대로 통과. 후보지 고리 자료 자체는 정상(배율 중앙 2.04 vs 기존점 1.97)
  자사 40곳    전체 5.85 → 6.21%p · 라벨(송도·구미산동·동탄북광장·전대후문) 뺀 36곳 5.68 → 5.56%p (2SE 1.94) — 같다
  실측 수요     14동네 실측하한 ÷ 산식수요: λ0 1.14배 · λ800 0.58배 — 고리가 수요를 실측 하한 위로 밀었다
```
치른 값(다시 열린 숙제): 경쟁점 47곳 편향 −2.3 → **−11.5%p** · 자사우위 예측 1.65 → **3.69배**(실측 1.74).

사용자 회고(1km·유동 400m의 원래 근거): 전대상대점은 전대후문점과 배후지를 나눠 쓰는데 사람은 후문에 몰려 상대점 상권을
넓히면 안 맞았다. 수원망포점은 500m쯤 역이 있어 그 유동이 딸려 들어와 반경을 줄였다. "상권을 넓히면 안 맞는다"는 그때도 같은 증상.

⚠️ 배운 것: **자사 37곳 성적만으로는 수요 반경 갈래를 판정할 수 없다** — 옛/새가 자사에서 안 갈린다. 후보지 옛/새 표를 먼저 찍는다.
"튀는 매장 버리기"는 축척 고정이라 후보지 숫자를 안 바꾼다 — 빼지 말고 라벨로.

---

## 다음 세션에 붙여넣을 것

```
점포평가 이어서 하자. docs/handoff-20260928.md 읽고 시작해.

[집 PC 주의 — 다음 세션은 집에서 한다(사용자)]
- `.local-tools/`는 gitignore다. 스냅샷은 2번으로 다시 뜬다(`.env.local`의 FIREBASE_CLIENT_EMAIL·FIREBASE_PRIVATE_KEY 필요 — 집에서 전에도 썼다).
- 고리 인구·항아리 판정·QSC는 전부 Firestore → 스냅샷에 들어오므로 SGIS 파일·qsc-scores.json·trade-area-judgments.json 없어도 된다
  (`_bundleCandidate`의 SGIS 파일 필수 조건은 2026-09-24에 풀었다). 없으면 그 하네스가 skip이 아니라 돈다.
- 빌드는 package.json이 힙 8GB로 돼 있어 `npm run build` 그대로. `python`은 회사 PC에서 스텁이었다(집은 모름) — node로 한다.
- 두 세션이 같은 저장소를 만지면 커밋 전 `git pull --rebase`. 오늘 동료 세션 커밋(07b2f9c)에 내 hunks가 파일 단위로 섞였다.

[시작 절차]
1. git pull
2. node scripts/dumpValidationSnapshot.mjs
3. npx vitest run --exclude "**/_*.test.ts"
4. npx vitest run src/lib/storeEval/_bundleCandidate.test.ts -t "(0)" --disable-console-intercept   ← 검산: 옛 배선 = 새 기본값 = 5.84%p
5. npx vitest run src/lib/storeEval/_labCandidate.test.ts -t "(8)" --disable-console-intercept      ← 후보지 13곳 옛/새(지금은 차이 0)

[산식 현재 상태 — 2026-09-24 되돌림 + 오후 배수 재점화. 실험실만, 운영 V62 무변경]
  θ 3 · 존구성 비중 0.238 · residentRingDecayM 0(꺼짐; gravity·항아리 값은 남아 있으나 안 읽힘)
  특수수요 배수 **대학가 1.3 · 산업단지 1.4 · 군부대 2.0 · 관광유흥 1.4**(기타·없음 1.0) — 옛 배선에서 다시 잼. 기준(사용자): 그 유형
    자사 예측 가동률 평균 = 실측 평균. 표본 2~5곳이라 방향값. `_bundleCandidate` (7)(7b)
  축척 8.20h 고정 · 상품몫 1,493원 · 감쇠 R200 λ200 · 흡인력 0 · 눈금보정 껐음 · 2km 경쟁점 ON
  자사 37곳 MAE **4.85%p**(배수 전 5.84) · 편향 +0.4 · 오차 SD 6.2 · ±5%p 21/37 · ±10%p 30/37 · 최악 양주덕정 −13 · 일산탄현 +11 · 문경시청 −11 · 시흥배곧 +11
  ⚠️ '전부 평균' 바닥 4.24%p(동료 세션 07b2f9c 화면 ScoreBoard)를 **아직 못 넘었다**. 목표 3%p. 경쟁점 47곳 편향 −10.9%p(1:1)
  대학가는 배수 하나로 못 맞춘다: 전대후문 −8 · 청주대 −6 vs 부경대 +10 · 전대상대 +5 → 다음 갈래 "대학 규모×거리"(사용자에게 자료 여부 물음)
  후보지 13곳 가동률: 신중동 23.7 · 춘천퇴계 20.3 · 호구포역 41.3 · 하안금당 32.4 · 울산삼산 13.6 · 평택소사벌 21.2 · 창원상남 10.1 ·
                    마산산호 36.8 · 산본 25.5 · 김포풍무 32.1 · 영월 10.4 · 구리돌다리 23.8 · 오송 28.9

[오늘 할 일] — 사용자 방향(2026-09-24 퇴근 전): **"가동률차 나는 거 보완할 방안 찾기, 오차 큰 것부터 어떤 요소로 잡을지"**. 목표 3%p
1. ⭐⭐⭐ 가동률 오차 큰 매장부터 요소 찾기(/store-eval/lab 매장별 표가 가동률 오차 순). 지금 최악: 양주덕정 −13 · 일산탄현 +11 ·
   문경시청 −11 · 시흥배곧 +11 · 전대후문 −8 · 청주대 −6 · 부경대 +10(대학가 1.3 후). 매장별로 "수요 전부라면"(수요 층)과
   "실제 먹은 몫 vs 산식 점유율"(점유율 층) 두 칸으로 어느 층이 틀렸는지 먼저 가른다
2. ⭐⭐ 대학가는 배수 하나로 안 된다 — "대학 규모 × 정문·후문 거리" 항목 갈래. 사용자에게 **학생 수·거리 자료가 있는지 물었고 답 대기**
   (없으면 대학알리미 재학생 수 + 지도 거리로 채우고 5곳으로만 검증)
3. 후보지 13곳 검토 — 사용자 "일단 넘어가자, 이해 안 되는 곳 있긴 한데". 배수 켜서 숫자가 또 바뀌었다(호구포역 산업단지 1.4·오송 대학가 1.3). 물으면 본다
4. 경쟁점 47곳 평가창 가동률 — **핑봇이 고장나 지금은 못 한다**(사용자). 고쳐지면 목록(handoff 본문)대로
5. 요금 경쟁력 갈래 — 송도 옛 배선 오차 +1.5라 동기 약함. 4번 자료 뒤에
6. 남은 숙제: 경쟁점 47곳 1:1 편향 −10.9%p(θ 문제). 자사우위 배수(3.69 vs 1.74)는 **판정 잣대에서 뺐다**(핑봇 평균 금지)
7. 독점 3곳 원장 — 사용자 "나오면 드림"

[규칙] — 전날과 같다 + 새 것:
- ⭐ **핑봇은 1:1 대조에만 쓴다**(경쟁점 A 예측 vs A 핑봇, 47쌍 편향·MAE). 핑봇 잡힌 1~3곳을 "동네 경쟁점 평균"으로 삼는
  자사우위 배수(실측 1.74·산식 3.69)는 **판정 잣대·순위 기준에서 뺀다**(사용자 2026-09-24: "핑봇은 1:1 대조할 때만 쓰고 평균 자료로는
  쓰지 말자"). 재고 표엔 참고 줄로만. 핑봇 잡힌 경쟁점이 센 쪽으로 치우쳐 있다(조사 좌석가동 24~27% vs 안 잡힌 쪽 18%)
- ⭐ 사용자가 경쟁점 47곳 가동률을 **우리 평가창 기간**으로 조회해 주기로 했다(목록 전달함). 받으면 경쟁점 문서에 기간별로 저장(지시 시)
  → 경쟁점 편향 −11.5%p를 같은 기간으로 다시 잰다. 2023~2024 평가창은 조회 불가할 수 있어 2순위(우리 월별 자료 있는 달)
- ⭐ 수요 반경·수요 크기 갈래는 **후보지 13곳 옛/새 표(_labCandidate (8))를 먼저** 찍고 올린다. 자사 성적만으로 채택 금지
- 실험실 매장별 표는 **가동률 오차(|예상−실측| %p) 큰 순**(사용자 정정 2026-09-24). 후보지·빠진 매장 표는 예상 가동률 순
- ⭐ 튀는 매장은 빼지 말고 라벨(송도·구미산동·동탄북광장·전대후문). 축척 고정이라 빼도 후보지 숫자는 안 바뀐다
- 예상가동률 먼저 · 채택은 재고 표 + 추천 명시 · 자사 먹/인 4.0 안 내림
- 운영 V62 금지 · git add -A 금지(_*.test.ts는 git add -f) · Firestore 쓰기는 시킬 때만 · 파일 삭제 금지 · 셸로 파일 수정 금지
- 고리를 다시 켤 일이 있으면 residentRingDecayM>0 하나로 산다(gravity·항아리·자료 다 남아 있음). 단 θ·존구성과 묶음으로만
```

## 파일

| 경로 | 내용 |
|---|---|
| `_labCandidate.test.ts` (8) | ⭐ 후보지 옛/새 나란히 + 자사 라벨 뺀 성적. OLD 상수 = θ3·존 0.238·λ0·배수 군부대 2.25만 |
| `_bundleCandidate.test.ts` (0) | 검산. 옛 배선 5.84%p. "(참고) 배수만" 줄은 지금 옛=새라 같은 값 |
| `docs/releases/2026-09-23-bundle-zone-theta-ring.md` | 끝에 "⛔ 2026-09-24 — 되돌렸다" 절 |
| `textbookModel.ts` DEFAULT_TEXTBOOK_PARAMS | 되돌림 주석 5곳 + 1km/400m 근거 주석 |
