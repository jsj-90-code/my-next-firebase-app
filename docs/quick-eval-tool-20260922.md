# 주소만 초기평가 도구 — 만든 것과 안 만든 것 (2026-09-22)

화면: **`/quick-eval`** (독립 주소) · 인계문: `docs/handoff-address-only-tool.md`

---

## ⛔ 다른 세션이 제일 헷갈릴 것 — 여기 있는 건 **전부 주소만 자동화 전용**이다

사용자 지시(2026-09-22): *"여기서 한 거는 주소만 입력 자동화니까 그거 구분해서 다른 세션에서
안 헷갈리게 꼭 구분해줘야 함"* · *"주소만 자동화에만 적용하는 거다. 딴 곳에는 적용하지 말고"*

| 이 문서에 나오는 것 | 어디에 적용되나 |
|---|---|
| 자동화 전용 산식(PC대수+시급, MAPE 18.27%) | **`/quick-eval` 화면에만.** 운영 V62·실험실과 무관 |
| 송도점·동탄북광장점 표본 포함(38 -> 40곳) | **`/quick-eval` 계산에만.** Firestore `excludedFromModel`은 **true 그대로** |
| 동탄북광장점 QSC = 가맹점 최저점 | **`/quick-eval` 계산에만.** Firestore QSC 자료에 **안 썼다** |
| 학습 경쟁점 비우기(비대칭 수정) | **`/quick-eval`에만** — 운영 V62는 실측 경쟁점을 그대로 쓴다 |
| 유동 반경·2km 경쟁점 측정 | **기각**. 아무 데도 안 들어갔다 |

**Firestore에는 이 작업으로 한 줄도 안 썼다.** 운영 V62 파일(`calc.ts`·`evaluate.ts`·
`usageRevenue.ts`·`cronSync.ts`·`settings.ts`)도 한 글자도 안 고쳤다 — 읽고 부르기만 한다.

⚠️ 그래서 **여기 숫자를 운영 V62 성적으로 인용하면 안 된다.** 운영은 정밀 8.83%이고,
여기 27.79%·18.27%는 주소만 조건에서 잰 다른 숫자다.

⚠️ 반대로 **실험실(교과서식) 결론을 이 도구에 그대로 옮기면 안 된다** — 5-2절에서 두 번
측정해 둘 다 기각됐다. 두 산식이 같은 자료를 다르게 쓴다.

⚠️ **점포평가 시스템(`/store-eval`) 안에 있지 않다.** 사용자 지시로 주소·화면을 완전히 분리했다
("링크를 완전히 분리해줘 v62웹에넣지말고", 2026-09-22). 점포평가 메뉴에 걸면 안 되고, 입구는
홈 화면(`/`)의 도구 카드 하나다. 산식만 운영 V62를 그대로 부른다 — 계산을 따로 만들지 않았다.

---

## 0. 무엇인가

주소 한 줄로 상권자료를 자동수집해서 **운영 V62 산식 그대로** 예상매출을 내고, AI 평가문까지
붙이는 화면이다. 점포개발 **초기 선별**용이고, 결재 숫자는 여전히 [신규후보지]에서 낸다.

산식은 `evaluateCandidate()` 한 곳만 부른다 — [최종결과] 탭과 **같은 배선**이다.
다른 건 입력 자료뿐이고, 조사로만 알 수 있는 칸은 비워서 운영 V62에 **이미 있는** 기본값
장치가 채우게 둔다(`computeCompetitorAppliedPcCount`, `applyStandardOwnFacilityDefaults`,
`resolveManagementScores`).

**운영 V62 파일은 한 글자도 안 고쳤다.** 기존 화면도 안 건드렸다(메뉴 한 줄만 추가).

---

## 1. 사용자가 정한 것 (2026-09-22)

| 물어본 것 | 정해진 것 |
|---|---|
| 유동인구 | 소상공인365 자동수집을 시도한다. 실패하면 **지어내지 않고** "수집 실패"로 남긴다 |
| 입지평가(가시성 등) | 기존 Gemini AI 초안으로 자동 채점한다 |
| 결과 저장 | **저장 안 함.** Firestore에 한 줄도 안 쓴다 |
| 성인PC방 거르기 | 인허가 짝맞추기는 생략. **상호 이름 규칙**으로 거른다(인계문 4절) |
| AI 평가문 | **Gemini 무료 티어**로 쓴다 (웹검색이 필요 없어서 무료로 된다) |

---

## 2. 자료가 학습자료와 같은 자로 잰 값인지 — 확인함

도구가 넣는 값이 V62를 학습시킨 값과 출처가 다르면 예측이 조용히 어긋난다. 그래서 먼저 쟀다.

| 항목 | 대조 | 결과 |
|---|---|---|
| 주거인구(SGIS 반경조회) | 운영 52곳 손입력값 | 중앙 차이 **0.0%**, 51곳 중 34곳이 1% 이내 (2026-09-15에 이미 측정, 그래서 운영도 이 값으로 덮었다) |
| 유동인구 500m(소상공인365) | 운영 54곳 | 비율 중앙 **1.000**, 최악 0.98, **54곳 전부 ±10% 안** (2026-09-22 측정) |

=> **환산계수가 필요 없다.** 그대로 쓴다.

---

## 3. 자동 / 기본값 경계

화면과 AI 평가문은 `src/lib/storeEval/quickEval/quickEvalDefaults.ts`의 재고 표를 **읽어서**
그린다. 숫자를 화면에 글자로 박지 않는다(CLAUDE.md 규칙).

| 항목 | 출처 |
|---|---|
| 좌표 | 카카오 주소검색 |
| 주거인구 500m·1km(연령9구간·남녀) | SGIS 반경조회 2024년 |
| 유동인구 500m(총·성별·연령) | 소상공인365 (최근 12개월 평균 + 최근월을 그 축척으로 환산) |
| 경쟁점 목록·거리 | 카카오 장소검색 500m, 격자분할 |
| 경쟁점 PC 대수 | **기본값 90대** (운영 V62의 간략_기본대수) |
| 경쟁점 품질 | **전부 빈칸** → 결측 처리(동급) |
| 입지평가 5항목 | Gemini AI 초안 |
| 자사 시설·관리점수 | 회사 표준 / 가맹점 평균 |
| 자사 PC대수·시급 | **사람이 입력**(기획값이라 자동화 대상이 아니다. 사용자 확인 2026-09-22: 기본값 안 넣고 직접 쓴다) |
| 자사 층·지상지하·엘리베이터 | 사람이 입력(입력 첫 줄). **AI 입지평가에 사실로 넘어가** 접근가시성에 반영된다 |
| 후보지명 | 사람이 입력, **접이식**(비우면 주소를 쓴다) |
| 자사 먹거리 브랜드 | **쉐프앤클릭 고정** — 자사 브랜드라 후보지마다 다를 일이 없어 입력칸을 없앴다 |
| 예상 오픈월 | **입력칸 없음** — 비우면 운영 V62가 "평가한 달의 다음 달"로 잡는다(기준매출 계산에만 쓰인다) |

### 화면은 "주소 치고 조회" 형태다 (2026-09-22 사용자)

> *"점포개발자들이 점포평가 심사까지의 업무구간이 있으니까 이거 간략히 1차적으로 넘어가려는
> 상황"* · *"최대한 주소만 치고 조회하는 형태로 할 거야"*

그래서 입력은 **한 줄(주소 + PC대수 + 시급 + 조회)**이고, 층·엘리베이터·후보지명은 접어 뒀다.
오차 설명과 재고 표도 접었다 — **지우지는 않았다.** 없애면 초기 추정이 정밀 평가로 오해되고,
그게 인계문 6-2가 경고한 바로 그 사고다.

### ⚠️ 반만 채우지 마라

측정값이다(`_addressOnlyMode.test.ts`): 품질만 비우고 대수는 실측 = **12.57%**,
둘 다 비우면 **10.82%**. 어설프게 반만 채우면 자료가 어긋나 **더 틀린다.**
조사가 들어오면 품질과 대수를 **같이** 채워야 한다.

---

## 4. 화면에 띄우는 오차 (측정값)

| 단계 | MAPE | ±20% |
|---|---|---|
| 정밀 평가(전부 조사됨) | 8.83% | 92.11% |
| **주소만** | **13.40%** | **86.84%** |

⚠️ 이 측정은 **기존 가맹점 38곳**으로 잰 것이고, **가시성을 표본 중앙값으로 고정**해서 쟀다.
이 도구는 가시성을 AI로 매기므로 조건이 완전히 같지 않다 — 화면에도 그렇게 적어 뒀다.
AI 채점을 쓸 때의 오차는 **아직 안 쟀다**(다음 세션 과제, 6절).

---

## 5. 과금

| 자료원 | 과금 |
|---|---|
| 카카오 주소검색·장소검색 | 무료(일 할당량) |
| SGIS 주거인구 | 무료 |
| 소상공인365 유동인구 | 무료(스크래핑) |
| **입지평가 AI 초안** | **유료.** googleSearch 그라운딩이 유료 티어 전용이라 `GEMINI_API_KEY_LOCATION_EVAL`(전용 결제 프로젝트)로 나간다. 그라운딩만 월 5,000건 무료 |
| **AI 평가문** | **무료.** 웹검색이 필요 없어 무료 티어 키(`GEMINI_API_KEY`)로 돈다 |

⚠️ 평가문이 쓰는 무료 티어 키는 **좌석배치도 자동화와 할당량을 공유**한다. 연달아 돌리면
좌석배치도 쪽에서 429가 날 수 있다. 무료 티어는 구글이 입력을 제품 개선에 쓸 수 있으므로,
그게 걸리면 `GEMINI_API_KEY_QUICK_EVAL`에 유료 키를 넣으면 그쪽이 우선한다.
입지평가 전용 유료 키로는 **폴백하지 않는다** — 그 분리는 일부러 만든 것이다.

---

## 5-2. ⛔ 실험실 검증값을 이 도구에 넣어 봤다 — 둘 다 나빠졌다 (2026-09-22 밤)

사용자: *"자동화 산식에 실험실 데이터로 좀 넣을 수 있는 거 넣어서 개선해볼까? 예를 들어
유동인구 이런 거는 500 너무 크다고 데이터로 나왔잖아. 실험실에서 검증된 값 적용해서
최대한 맞춰보도록 하자."*

측정: `src/lib/storeEval/_quickEvalRadius.test.ts` (주소만 모드 L3 배선 그대로, 정식검증군 38곳)

### (가) 유동인구 반경을 줄이면

| 반경 | MAPE | ±10% | ±20% | 상권수요 중앙 |
|---|---|---|---|---|
| 100m | 13.06% | 50.00% | 84.21% | 2,714 |
| 200m | 13.50% | 44.74% | 84.21% | 2,793 |
| 300m | 13.71% | 44.74% | 81.58% | 3,536 |
| 400m | 13.63% | 44.74% | 81.58% | 3,800 |
| **500m (현재)** | **13.40%** | 47.37% | **86.84%** | 4,462 |

**개선 근거가 없다.** 100m가 MAPE 0.33%p 낫지만 ±20%는 500m가 제일 좋고, 표본 38곳에서
0.33%p는 잡음 범위다. → **500m를 유지한다.**

### (나) 2km 경쟁점을 넣으면

| 경우 | MAPE | ±20% | 덧붙인 경쟁점 |
|---|---|---|---|
| **500m만 (현재)** | **13.40%** | **86.84%** | 0곳 |
| +2km(평가창 50%↑ 영업) | 14.03% | 78.95% | 512곳 |
| +2km(평가창 99%↑ 영업) | 14.03% | 78.95% | 502곳 |

**명확히 나빠진다**(±20%가 7.9%p 빠진다). → **넣지 않는다.**

### ⭐ 왜 실험실 결론이 여기서 안 통하나 — 이걸 기억해야 한다

두 산식이 **같은 자료를 다르게 쓴다.**

| | 실험실 교과서식 | 운영 V62 (이 도구) |
|---|---|---|
| 유동인구 | 반경이 곧 상권 크기 → 반경이 중요하다 | **회귀 피처**로 쓰고 매 호출 재학습 → 수준 변화를 계수가 흡수한다 |
| 경쟁점 | 거리별 감쇠로 점유율을 세운다 | 경쟁IP 합계 하나로 들어간다 → 512곳을 더하면 피처가 망가진다 |
| 판정 기준 | **가동률** | 매출 MAPE |

수요 중앙값이 2,714↔4,462로 1.6배 차이 나는데 MAPE가 0.3%p만 움직인 게 그 증거다.
=> **실험실에서 좋아진 것을 이 도구에 그대로 옮기면 안 된다. 옮길 때마다 여기서 다시 재라.**

### 그럼 이 도구를 개선할 갈래는 무엇인가

1. **경쟁력 비중·접근성 개선안** — 운영 V62 자체를 좋게 하는 안이고 이미 측정돼 있다
   (MAPE 10.00 -> 9.10%, 재현율 여섯 지표 전부 ★). 기본 꺼져 있고 **사용자 확인 대기** 상태다.
   켜면 이 도구와 점포평가 웹이 **같이** 좋아진다 — 지금 남은 갈래 중 제일 크다.
2. **가시성 AI 채점의 오차를 아직 안 쟀다**(6절 1번). 이 도구가 실제로 몇 %인지는 그걸 재야 안다.
3. 경쟁점 PC 대수(6절 2번) — 자료가 없어 막혀 있다.

---

## 5-3. ⭐⭐ 왜 높게 나왔나 — 학습/예측 비대칭을 고쳤다 (2026-09-22 밤)

사용자: *"지금 두세 군데 돌려봤는데 예상매출이 좀 높게 나오는 듯하다?"* ·
*"기존매장으로 재평가해서 비교해보면 좋을듯?"*

그 방법으로 쟀다(`_quickEvalBias.test.ts`): 기존매장의 조사 자료를 지우고 **후보지인 척** 도구와
같은 배선으로 돌린 뒤 실제 매출과 견줬다. **자기 자신은 학습에서 뺐다**(leave-one-out).

| 단계 | MAPE | ±20% | 예측÷실측 | 증가 |
|---|---|---|---|---|
| ① 정밀(실측 경쟁점·실측 자사·사람 입지평가) | 10.03% | 94.74% | 0.991 | — |
| ② + 자사를 표준값으로(새로 지으면) | 18.39% | 60.53% | 1.132 | +14.2% |
| ③ + 입지평가를 가시성 고정값으로 | 26.67% | 47.37% | 1.169 | +3.3% |
| ④ + 경쟁점 비움 = **고치기 전 도구** | **46.35%** | 26.32% | **1.461** | **+24.9%** |
| ⑤ ④ + **학습 경쟁점도 같이 비움** | **27.79%** | 42.11% | **1.211** | −17.1% |

①이 0.991·10.03%로 나온 게 **통제군 통과**다 — 측정 배선 자체는 멀쩡하다는 뜻이고,
그래서 ②~④의 증가를 믿을 수 있다.

### 무엇이 잘못이었나

도구가 **한쪽만 비우고 있었다**:

```
학습 표본(기존점) : 사람이 다 조사한 실측 경쟁점 — 진짜 대수·품질
예측 대상(후보지) : 전부 기본값 — 90대 · 품질 결측
```

90대는 실측 분포의 아래쪽이다. 학습은 큰 경쟁점을 보고 계수를 정했는데 후보지엔 작은 경쟁점만
놓이니 **경쟁IP가 과소평가되고 점유율이 과대평가**된다. 35/38곳이 실제보다 높게 나왔다.

### 고친 것 (한 줄)

`trainingCompetitors`를 `blankCompetitorForTraining`으로 태워 **학습 쪽도 같은 수준으로** 비운다.
=> MAPE 46.35 -> **27.79%**, 예측÷실측 1.461 -> **1.211**, ±20% 26.32 -> **42.11%**

### 안 한 것과 그 이유

- **기본대수 90 -> 200**: 대칭 조건에서 배율이 1.211 -> 1.166으로 조금 움직이지만 **근거 없는
  값**이다(실측 경쟁점 분포와 안 맞고 38곳에 과적합). 구조를 맞추는 게 먼저다.
- **남은 과대(1.21)를 계수로 눌러 1.0으로 맞추기**: 그 중 +14.2%p는 **자사를 표준값으로 두는
  것**에서 온다 — "지금 새로 지으면"이라 후보지 평가의 본질이고 정밀 평가도 똑같다. 그걸 누르면
  후보지를 실제보다 낮게 보게 된다. **대신 화면에 "실제보다 21%쯤 높게 나오는 경향"을 적었다.**

⚠️ 화면의 정확도 문구는 이제 `QUICK_EVAL_BACKTEST`(되짚어 잰 값)를 읽는다. 4절의 13.40%는
**검증 배선**으로 잰 값이라 도구의 성적이 아니다 — 화면에 그 숫자를 쓰지 마라.

---

## 5-4. 자동화 전용 산식 — 더 넣을 게 없다 (2026-09-22 밤)

사용자: *"자동화 범위에서의 산식 만들기"* · *"자동화산식은 자동화 웹에서만 보이게해"* ·
*"그럼 자동화 산식은 끝난 겨? 개선 여지 없는 최선인가"*

V62를 **비워 쓰는 대신**, 자동화로 실제 얻는 값만으로 회귀식을 세웠다(`quickEvalOwnModel.ts`).
리브원아웃 40곳:

| 조합 | MAPE | ±20% | 예측÷실측 |
|---|---|---|---|
| 기준선 — 전부 평균(산식 없음) | 22.35% | 55.00% | 1.000 |
| **PC대수 + 시급** | **18.27%** | 60.00% | 0.976 |
| PC대수만 | 18.74% | 60.00% | 1.013 |
| + 개점경과 | 18.54% | 57.50% | 0.988 |
| + 경쟁점 개수 | 18.67% | 60.00% | 0.989 |
| + 유동인구 | 19.16% | 55.00% | 1.016 |
| + 주거인구 | 19.38% | 52.50% | 0.980 |
| + 층수 | 19.92% | 55.00% | 0.973 |
| (참고) V62를 비워 쓰기 | 27.79% | 42.11% | 1.211 |

**"PC대수 + 시급"을 이기는 조합이 하나도 없다.** 인구·유동인구·경쟁점 개수·층수·개점경과를
다 넣어 봤고 전부 나빠졌다. 개점경과는 "후보지는 늘 신규인데 학습 타깃은 누적평균"이라는
의심으로 넣어 본 것인데 역시 안 됐다.

=> **지금 자료로는 여기가 최선이다.** 다음 개선은 **표본 증가(2027 상반기)**나
**새 자료(경쟁점 PC 대수)**를 기다려야 한다. 같은 자료로 피처를 더 찾지 마라 — 이미 훑었다.

⚠️ 이 산식은 **상권을 못 본다**(같은 대수·요금이면 어느 동네든 같은 값). 그래서 V62 값을
덮지 않고 화면에 **나란히** 놓는다 — 두 값이 벌어지면 그게 "이 상권이 평균과 다르다"는 신호다.

---

## 6. 안 한 것 / 남은 것

1. **AI 채점 입지평가의 오차를 안 쟀다.** `_addressOnlyMode.test.ts`의 L3는 고정값 기준이다.
   재려면 기존점 38곳에 AI 초안을 돌려서(유료 호출 76회) 같은 배선으로 비교해야 한다.
2. **경쟁점 PC 대수는 여전히 90대 기본값이다.** 인계문 5절의 막다른 길 그대로다.
3. **카카오 40건 상한** — 격자분할로 우회했지만 2단계(49질의)에서 멈춘다. 그래도 잘리면
   화면에 "일부 빠졌을 수 있음"으로 뜬다.
4. **인허가 짝맞추기를 안 붙였다.** 성인PC방은 이름 규칙으로만 거른다 — **새 업태가 생기면
   `pcBangNameFilter.ts`를 갱신해야 한다.**
5. **저장이 없다.** 초기추정과 정밀평가를 나중에 대조하려면 저장이 필요해진다(그때 결정).

---

## 7. ⚠️ 빌드 메모리 (이 작업 중 발견)

`npm run build`가 **Node 기본 힙(2GB)으로는 OOM**으로 죽는다. 타입 검사 하나가 이미
1.85GB를 쓴다(파일 2,347개, `npx tsc --noEmit --extendedDiagnostics`로 측정).
**이 도구 때문이 아니라 저장소 전체가 천장에 닿아 있는 것이다.**

```
NODE_OPTIONS=--max-old-space-size=8192 npm run build     # 이러면 통과한다
```

근본 해법 후보(아직 안 함): 빌드용 tsconfig에서 `*.test.ts` 제외, 큰 JSON을 import 대신
런타임 로드로 바꾸기(`src/lib/storeEval/data/rival2km.json` 152KB).

---

## 8. 파일

```
src/lib/storeEval/quickEval/
  tm.ts                    좌표변환(5179/5181) — scripts/lib/tm.mjs와 같은 검산점
  pcBangNameFilter.ts      성인PC방·오락실 이름 규칙
  quickEvalDefaults.ts     ⭐ 재고 표 + 측정된 오차표 (화면이 읽어 그린다)
  kakaoPcBangs.ts          카카오 PC방 격자분할 조회
  sgisRadiusPopulation.ts  SGIS 반경 주거인구
  sbizFloating.ts          소상공인365 유동인구
  buildQuickCandidate.ts   CandidateInput·Competitor[] 조립(순수함수)
  quickEvalReviewPrompt.ts AI 평가문 프롬프트(순수함수)
  quickEvalReviewAi.ts     Gemini 호출(무료 티어 키)
  quickEval.test.ts        27건 — 좌표검산·이름규칙·조립·기본값 장치
src/app/api/quick-eval/collect/route.ts      자동수집
src/app/api/quick-eval/ai-review/route.ts    평가문
src/app/quick-eval/page.tsx                  화면
src/app/quick-eval/layout.tsx                라우트 전용 title
src/app/quick-eval/QuickEvalChrome.tsx       전용 껍데기(로그인 관문·홈으로·테마) — 점포평가와 공유하지 않는다
src/app/page.tsx                             홈 도구 카드(유일한 입구)
```

⚠️ `src/app/store-eval/` 아래에는 **아무것도 두지 않는다.** 점포평가 메뉴
(`StoreEvalChrome.tsx`)에 이 도구를 걸지 마라 — 분리한 이유가 무너진다.

---

## 9. ⭐⭐ 보정계수를 안 넣고 값을 조정했다 (2026-09-22 밤 2차)

사용자: *"보정넣지말고 산식을 변경하는쪽으로해봐"* ·
*"실제보다 21퍼 높게나오는 경향있따고했잖아. 이걸 산식을통해 개선하면되지않니? 보정말고 값을 조정하면되자나"*

### 9-1. ⛔ 먼저 정정 — 8절(5-3절)의 기본대수 스윕은 비대칭이었다

`_quickEvalBias.test.ts`의 `run()`이 **학습 쪽은 90대로 두고 후보지만** 스윕값으로 바꾸고
있었다. 도구는 `blankCompetitorForTraining`으로 양쪽을 같게 비우므로 그건 도구가 낼 값이
아니다. 고쳐서 **양쪽에 같이** 주고 다시 쟀다:

| 기본대수(양쪽 같이) | MAPE | ±20% | 배율 |
|---|---|---|---|
| 90대 | 27.79% | 42.11% | 1.211 |
| 111대(실측 중앙) | 27.76% | 42.11% | 1.207 |
| 200대 | 27.67% | 47.37% | 1.200 |
| 300대 | 27.60% | 47.37% | 1.199 |

=> **"200대로 올리면 1.166"은 비대칭 때문에 생긴 허깨비였다.** 대칭으로 올리면 수준 변화를
**재학습이 흡수**한다(유동인구 반경에서 본 것과 같은 현상, 5-2절).

실측으로 대수를 아는 경쟁점 159곳: 최소 61 · 1사분위 97 · **중앙 111** · 3사분위 150 · 최대 400 ·
평균 129.1대. 90대보다 작은 건 23/159곳(14%)뿐이라 **90대가 낮은 건 사실이다** — 그런데 고쳐도
배율이 안 움직인다. (인계문의 "3/4이 90대보다 크다"는 실제로는 86%였다)

### 9-2. ⭐ 결정적 측정 — 값 조정과 보정계수는 수학적으로 같다

예측을 전부 중앙배율로 나눠(=곱셈 보정을 먹여) 수준을 1에 맞춘 뒤 남는 오차가 **줄 세우기
실력**이다. 값 조정이 그걸 못 내리면, 그 조정은 보정계수와 하는 일이 같다.

| 조건 | MAPE | **수준보정 MAPE** | 배율 |
|---|---|---|---|
| ① 정밀(실측 전부) | 10.03% | **10.10%** | 0.991 |
| ⑤ 도구 — 표준 기획값 | 27.79% | **17.04%** | 1.211 |
| ⑤ + 기본대수 111대 | 27.76% | 17.05% | 1.207 |
| ⑤ + 품질도 실측 대표값 | 28.18% | 16.19% | 1.240 |
| ⑤ + **자사 사양을 표본 최빈으로** | **23.14%** | **16.94%** | **1.140** |

=> 어느 값을 조정해도 **수준보정 MAPE가 17%에서 안 내려간다.** 분별력 천장은 값으로 안 뚫린다.
정밀 평가(10.10%)와의 7%p 격차는 **경쟁점 실측 자료**가 있어야 메워진다.

### 9-3. 그래서 무엇을 바꿨나 — 자사 사양을 표본 안쪽으로

사용자: *"몰라 뭐가정답이야"* → 내가 정했다. **`QUICK_EVAL_OWN_HARDWARE`를 기존 가맹점 38곳의
최빈 사양으로 내렸다.**

| | 예전 | 지금 |
|---|---|---|
| CPU | 울트라5 225F | **i5 14400F** |
| VGA | RTX 5060 | **RTX 4060** |
| RAM | 16GB | 16GB |
| MAPE | 27.79% | **23.14%** |
| ±20% | 42.11% | **47.37%** |
| 배율 | 1.211 | **1.140** |

**근거(보정계수와 다른 점):** 표준 기획값은 **학습 표본에 아예 없는 사양 구간**이다. 기존점은
전부 i5 14400F · RTX 4060 세대라, 표준 기획값을 넣으면 모형이 표본 밖으로 외삽한다. 그 사양
프리미엄이 진짜인지는 **확인할 길이 없다** — 신사양 매장의 실매출이 아직 없고, 사양과 개점
시점이 얽혀 있다(코호트 교란). 임의의 계수가 아니라 **표본 안에 머문다**는 자료 논리다.

⚠️ 예전 주석은 이 +14.2%를 "새로 지으면 사양이 진짜 좋으니 정상"이라고 적어 뒀다. 그 판단을
뒤집은 근거가 위 문단이다.

### 9-4. ⚠️ 이 조정이 만든 부작용 — 정밀 평가와 갈라진다

`buildQuickCandidate.ts`의 예전 주석은 이 상수를 신규후보지 화면(`blankCandidate`)과 **같은
값으로 유지하라**고 못 박아 뒀다. 이제 갈라졌다 — 같은 후보지에 **도구가 정밀 평가보다 약 6%
낮은** 자사 경쟁력점수를 준다.

일부러 그렇게 뒀다: 같은 측정의 ②단계에서 **정밀 평가도 표준 기획값 때문에 실제보다 13% 높게
나온다**(배율 1.132)는 게 나왔다. 정밀 평가에 맞추는 건 같이 틀리자는 뜻이 된다.

⛔ **정밀 평가(`/store-eval`)를 이 판단으로 고치지 마라** — 사용자 지시로 이 도구 전용이다.
표준 기획값은 `OWN_HARDWARE_PLANNED_STANDARD`로 남겨 뒀다(두 값의 관계를 보이게).

### 9-5. ⛔ 막힌 길 — 사양을 비우기

"후보지는 아직 안 지었으니 사양 미정이 사실이다"로 비워 봤는데 **V62가 금액을 아예 못 낸다**
(n=0). 그 길은 닫혔다.

### 9-6. 남은 배율 1.140은 값으로 못 줄인다

단계별로 보면 가시성 고정값(+3.3%)과 경쟁점 비움(+3.6%)이 남아 있다. 가시성은 도구가 AI로
매기니 조건이 다르고(아직 안 쟀다), 경쟁점 비움은 9-1·9-2에서 값으로 안 된다는 걸 쟀다.
=> **다음 개선은 경쟁점 실측 대수 자료다.** 같은 자료로 값을 더 만지지 마라.

---

## 10. 화면에서 안내문 둘을 지웠다 (2026-09-22 밤 2차)

사용자: *"조회했을떄 이런 문구뜨는데, 쓸때 없는내용빼라"* · 두 문단 각각 *"이거는 삭제해도될듯"*

지운 것: 조회 화면 머리의 (1) 층·엘리베이터 설명 문단, (2) 되짚기 오차 설명 문단.

⚠️ **내용이 사라진 건 아니다.** 지우면 화면에 `ADDRESS_ONLY_ACCURACY`의 13.40%만 남아
**도구 성적을 실제보다 좋게** 보여주게 되므로, 접이식("이 숫자를 얼마나 믿나")으로 옮겼다:

- 층수 0.0% → 재고표의 "자사 층·지상지하·엘리베이터" 항목 설명
- 되짚기 성적·수준보정 오차 → 오차표 아래 `<li>` 두 줄

그러면서 재고표가 **글자로 박아 두고 있던 민감도 숫자**(0.0% · −16.5% ~ +9.4% 등)를
`QUICK_EVAL_INPUT_SENSITIVITY`에서 읽게 바꿨다(CLAUDE.md 규칙).

---

## 11. 빌드 힙 — 범인은 테스트가 아니라 `googleapis`였다 (2026-09-22 밤 2차)

7절이 근본 해법 후보로 적어 둔 "빌드용 tsconfig에서 `*.test.ts` 제외"를 실제로 재 봤다.
**거의 효과가 없다.**

| 조건 | 파일 | Memory | Check time |
|---|---|---|---|
| 지금(`_` 테스트만 제외) | 2,353 | **1,889MB** | 7.39s |
| 모든 `*.test.ts` 제외 | 2,268 | 1,807MB | 5.43s |

82MB(4.3%)뿐이다. ⚠️ 처음 잰 값은 **캐시에 오염돼 있었다**(`incremental: true` 때문에 첫
실행이 검사를 건너뛰어 Check time 0.51s로 나왔다). `--incremental false`로 다시 재야 한다.

### 진짜 몸통 — 읽어들이는 정의 파일 개수

| 패키지 | .d.ts 파일 수 |
|---|---|
| **googleapis** | **917** |
| next | 334 |
| zod | 94 |
| @grpc/grpc-js | 89 |
| @anthropic-ai/sdk | 84 |

`googleapis`는 구글 API 전체의 타입을 싣는다. 그런데 이 저장소가 쓰는 건 **셋뿐**이다:
`google.sheets({version:"v4"})` · `google.slides({version:"v1"})` · `google.drive({version:"v3"})`
(`googleDeskOrder.ts` · `googleSheets.ts` · `googleSlides.ts` · `storeEval/cronSync.ts` ·
스크립트 둘).

### 한 것 / 안 한 것

- **한 것**: `package.json`의 build에 힙을 박았다 —
  `node --max-old-space-size=8192 ./node_modules/next/dist/bin/next build`.
  이제 `npm run build`만 쳐도 OOM이 안 난다(NODE_OPTIONS를 외울 필요가 없다).
  타입검사만 돌리는 `npm run typecheck`도 추가했다.
- **안 한 것**: `googleapis` → `@googleapis/sheets`·`@googleapis/slides`·`@googleapis/drive` 교체.
  d.ts 917개를 수십 개로 줄이는 **근본 해법**이지만 **운영 `cronSync.ts`를 건드린다**(Firestore
  동기화). 의존성 교체라 사용자 확인이 필요하다 — 다음 세션 과제.
- **기각**: 별도 `tsconfig.build.json`. 4%밖에 못 줄이는데 설정이 갈라지고 빌드에서 테스트
  타입검사가 조용히 빠진다. 만들었다가 지웠다.
