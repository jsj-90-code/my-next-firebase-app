# 인계 — 2026-09-26. **존구성을 수요 항으로 옮겨 봤다 — 어느 자리에 넣어도 자사우위가 더 부푼다**

앞 문서: `docs/handoff-20260925.md`.
별도 갈래: `docs/handoff-address-only-tool.md`(주소만 넣는 자동화 도구 — **다른 세션**).

전날 답: 문제는 둘(수요 1.5~2배 작다 · 자사 품질 후하다)이고 첫 단서가 "팀룸은 유입 장치".
이날 그 단서를 **본 산식으로 세 잣대에 대고 재봤다.** 계수는 하나도 안 바꿨다.

> ⭐ 한 줄로: **존구성은 점유율에 있든 수요에 있든 자사우위 과장을 키운다**(자사 존 3.19 vs
> 경쟁점 1.39 = 2.3배인데 실측 우위는 1.49배). 바깥 표본은 존구성을 **어디서도 못 본다**
> (경쟁점끼리 r −0.07, 짝 안 r −0.04). 그리고 격자에서 **"격차는 품질 눈금, 수준은 수요"**로
> 두 문제가 깨끗하게 갈라졌다 — θ 2~2.5 · 수요 ×1.5 칸에서 자사·경쟁점 편향이 **동시에** ±3%p 안.

---

## 다음 세션에 붙여넣을 것

```
점포평가 이어서 하자. docs/handoff-20260926.md 읽고 시작해.

[시작 절차]
1. git pull
2. node scripts/dumpValidationSnapshot.mjs
3. npx vitest run --exclude "**/_*.test.ts"

[오늘 할 일]
1. ⭐⭐⭐ 묶음 후보를 사용자에게 재고 표로 올린다(8절): **존구성 뺌 + θ≈2 + 1km 밖 고리(거리감쇠)**
   - 하나만 하면 무너진다. 지금 배선(θ3)에서 고리를 켜면 자사 +6~13%p 과대
   - 고리 가중은 자료로 고르지 말고 **거리감쇠 규칙**으로(1 / .5 / .25 등). 후보 몇 개를 표로
   - 목표 격차는 **1년차 자사 vs 성숙 경쟁점 = 1.74배**(n=9). 1.49는 시점이 섞인 값이었다
   - ⛔ 닫힌 것: 주거 500m(자사 r 0.40→0.16) · 경쟁점 11.58h(총시간 안 는다) · 유동 ×0.30(기전 없음)
2. ⭐⭐ 실험실 파라미터로 고리를 넣을 수 있게 배선한다(textbookModel.ts에 residentRings 같은 항 — 기본 꺼짐).
   지금은 테스트 안에서 residentAges를 바꿔 넣는 방식이라 화면에서 못 돌린다
3. 독점 3곳 원장 (광주각화·탕정역·남악) — ⚠️ 아직 못 받았다(사용자 확인). 받으면 시작
4. ✅ 팀룸 좌석효율 상쇄 가설 — **기각**(4절). 최소 인원 규칙(5인룸 4인 이상)
5. ✅ 자사 먹/인 기본값 4.0은 **내리지 않는다**(사용자 결정). 격차 손잡이는 θ만

[규칙] — 전날과 같다. 요약:
- 판정 기준은 가동률. 성적은 세 잣대(자사 37 / 경쟁점 47 / 동네 안 짝)로 같이 찍어라
- 2SE를 같이 적어라(자사 40곳 MAE 0.70%p · 이번 자료 0.82%p)
- '전부 평균' 바닥 같이 찍되 격차를 사실로 읽지 마라
- 계수·점수를 내 확인 없이 고르지 마라. 재고 표로 남겨라. 자료에 맞추지 마라
- 운영 V62 금지 (calc.ts·evaluate.ts·usageRevenue.ts·cronSync.ts·settings.ts) ※ 읽기는 됨
- 저장소를 다른 세션과 같이 쓴다. `git add -A` 금지 (`_*.test.ts`는 `git add -f`)
- Firestore 쓰기는 내가 시킬 때만 · 파일 삭제 금지 · 셸로 파일 고치지 마라(Edit/Write만)
- 가정을 세우기 전에 현장에 물어라

[산식 현재 상태 — 아무것도 안 바꿨다]
- 축척 8.20h 고정 · 상품몫 1,493원 · θ=3 · 감쇠 R200 λ200 · 흡인력 0 · 눈금보정 껐음
- 2km 경쟁점 ON · 입지 지수 0.5 · 존구성은 여전히 점유율 항(비중 23.8%)
- 자사 가동률 MAE 5.84%p · r 0.504 · 퍼짐 1.30배 (37곳, 경쟁점 있는 동네만)
- 경쟁점 47곳(바깥) MAE 12.6%p · r 0.235 · 편향 −10.9%p · 자사우위 실측 1.49 / 예측 3.50배
```

---

## 1. ⭐⭐⭐ 존구성을 수요 항으로 — **결과: 안 된다**

재는 자리: **`_zoneAsDemand.test.ts`** (1)·(2)·(3). 산식 코드는 안 고쳤다 — 주인공(자사 또는
경쟁점)마다 `qualityWeights.zone=0`으로 점유율에서 빼고, `hoursPerUserPerMonth × Z(주인공 존)`으로
그 주인공 수요시간만 Z배 했다(눈금보정이 꺼져 있어 가동률은 수요시간에 정비례한다).

```
  변형                       자사MAE 자사편향 자사r | 경쟁MAE 경쟁편향 | 짝r   짝과장 | 자사우위 실측/예측
  지금(존구성=점유율)          5.84%p  −1.4%p 0.504 | 12.6%p −10.9%p | 0.598 1.66배 |  1.49/3.50배
  존구성 뺌(어디에도 없음)      7.25%p  −5.8%p 0.399 | 11.6%p  −9.9%p | 0.559 1.40배 |  1.49/2.45배
  (ㄱ) 수요 Z=z^0.25          7.15%p  +2.5%p 0.418 | 11.6%p  −9.1%p | 0.596 1.56배 |  1.49/3.16배
      수요 Z=z^0.5           12.66%p +12.0%p 0.418 | 11.5%p  −8.2%p | 0.607 1.75배 |  1.49/4.11배
      수요 Z=z^1             23.42%p +23.4%p 0.158 | 11.8%p  −5.8%p | 0.553 2.03배 |  1.49/4.52배
  (ㄴ) 수요 Z=1+1·큰룸비       6.65%p  −2.9%p 0.392 | 11.5%p  −9.5%p | 0.575 1.47배 |  1.49/2.61배
      수요 Z=1+2·큰룸비        6.84%p  −0.0%p 0.375 | 11.5%p  −9.1%p | 0.583 1.54배 |  1.49/2.88배
      수요 Z=1+5·큰룸비       10.38%p  +7.6%p 0.316 | 11.5%p  −7.9%p | 0.582 1.74배 |  1.49/3.32배
  2SE(지금 대비 MAE 변화) 자사 0.82%p · 경쟁점 0.59%p
```
(ㄱ) z = 존구성 점수 1~5 · (ㄴ) 큰룸비 = (팀룸 5석·퍼스트 11석 환산) ÷ PC. 미조사 경쟁점은 Z=1.

### 왜 안 되나 — 산수다
자사 존 **3.19** vs 경쟁점 **1.39**(2.3배). 실측 자사우위는 **1.49배**뿐이다. 존구성에 양의 효과를
주는 순간, 점유율 자리든 수요 자리든 **우리만 커진다.** 유일하게 격차를 줄이는 건 "존구성을
약하게 하는 것"이고 그 끝이 "통째로 빼기"(2.45배)다. 자리를 옮기는 건 답이 아니다.

### 바깥 표본은 존구성을 어디서도 못 본다 — (2)
존구성을 뺀 상태의 잔차 log(실측÷예측)가 존을 따라가나:
```
  경쟁점끼리(47)   잔차~log존구성 r −0.071 · 잔차~큰룸비 r −0.030
                  큰룸 있음(11) − 없음(36) 잔차차 −7% (2SE 54%)      ← 판정력이 없다
  자사(37)         r 0.155 / 0.064 (n.s.)
  짝 경쟁점끼리(21쌍)  잔차차~존구성차 r −0.039 · ~큰룸비차 r 0.033
  짝 자사-경쟁점(47쌍) r 0.015 / −0.012
```
⚠️ **"합쳐서 84" r −0.406은 착시다** — 자사(존 높음·잔차 작음)와 경쟁점(존 낮음·잔차 큼)의
수준차가 존과 겹쳐 보이는 것. 수요 부족 문제가 존구성 옷을 입고 나타난 것이라 읽지 말 것.

⚠️ 큰룸 있는 경쟁점이 **11곳**뿐이라 2SE가 54%다. "효과 없다"가 아니라 **"50% 미만 효과는
이 자료로 못 본다"**다. 기전이 틀렸다는 증거는 아니다 — 4절 질문으로 이어진다.

### 대조군 — (3)
```
  이중(점유율+수요 z^0.5)   자사편향 +16.5%p · 자사우위 5.16배   <- 더 나쁘다(예상대로)
  경쟁점 존만 뺌(자사만 존) 자사우위 2.26배 · 자사편향 −7.4%p   <- 기전 없음. 채택 후보 아님
```

---

## 2. ⭐⭐⭐ 격차와 수준이 갈라졌다 — (4)·(5)

존구성을 뺀 상태에서 θ·항목·자사 기본값을 돌리면 **전부 같은 패턴**이다: 자사우위는 1.49에
다가가는데 **자사·경쟁점이 같이 −8~−14%p 과소예측**된다.

```
  존구성 뺌 · θ=3     자사편향  −5.8 · 경쟁편향  −9.9 · 자사우위 2.45 · 짝과장 1.40 · 짝r 0.559
  존구성 뺌 · θ=2     자사편향  −9.9 · 경쟁편향  −8.5 · 자사우위 1.82 · 짝과장 0.97 · 짝r 0.502
  존구성 뺌 · θ=1.5   자사편향 −11.9 · 경쟁편향  −7.8 · 자사우위 1.57 · 짝과장 0.77 · 짝r 0.439
  자사 먹/인 4.0→3.0  자사편향  −8.9 · 경쟁편향  −9.1 · 자사우위 1.97
  자사 먹/인 4.0→2.5  자사편향 −10.6 · 경쟁편향  −8.7 · 자사우위 1.75
```
⭐ **θ=2에서 짝 과장이 0.97로 딱 맞는데 자사우위는 1.82(1.22배 과)** — 경쟁점끼리의 눈금은
θ=2가 맞고, 자사에는 그 위에 **자사만의 후함**이 한 겹 더 있다. 전날 결론과 일치한다.

### 수요배수를 같이 돌리면 — (5) 격자
```
  칸 = 자사편향/경쟁편향(%p) · 자사우위예측 · 짝r     (* = 두 편향 다 ±3%p 안)
  θ    먹/인   수요×1.00          ×1.25            ×1.50             ×1.75
  3.0  4.0    −5.8/−9.9 2.45     0.4/−7.4 2.45    6.6/−5.1 2.45    11.7/−2.7 2.35
  2.5  3.0   −10.4/−8.5 1.76    −5.4/−5.7 1.76   −0.3/−3.0 1.76*    4.7/−0.3 1.76
  2.0  4.0    −9.9/−8.5 1.82    −4.7/−5.6 1.82    0.5/−2.8 1.82*    5.7/−0.1 1.82
  2.0  3.0   −11.9/−8.0 1.57    −7.2/−4.9 1.57   −2.5/−2.0 1.57*    2.2/ 0.8 1.57*
  1.5  4.0   −11.9/−7.8 1.57    −7.2/−4.7 1.57   −2.5/−1.6 1.57*    2.1/ 1.3 1.57*
```
- **두 편향이 동시에 0 근처인 칸이 있다** → 구조가 "격차는 품질 눈금, 수준은 수요"라는 강한 증거
- 그 칸들의 자사우위 예측 **1.57~1.82배**(실측 1.49). 짝 r은 0.44~0.52로 지금(0.56)보다 낮다
- θ=2·먹/인 3.0 과 θ=1.5·먹/인 4.0 이 **같은 줄**이다 — 둘은 바꿔칠 수 있어 자료로 못 가른다
  ([[project_own_facility_scores_are_time_aligned]]). 사용자 판단 자리.
- ⛔ **채택 제안이 아니다.** 수요배수는 진단용. 실제로는 이용자 수를 ×1.5 하는 정당한 경로가 필요.

### ⛔ 주거 500m은 닫혔다 — (6)
`_measuredDemand`(n=14, 실측 수요 모양)에서는 500m이 이겼지만, **가동률 세 잣대**에서는:
```
  주거 1000m   자사 r 0.399 · 퍼짐 1.22배
  주거  500m   자사 r 0.156 · 퍼짐 1.54배   (수요배수를 어떻게 줘도 r은 안 변한다)
```
⚠️ 500m은 연령 분해가 없어 반경+연령가중 **둘이 같이 바뀐다.** 그래도 r이 반토막이라 닫는다.
잣대가 다르면 답이 다르다는 걸 다시 확인했다(자사 매출 기준 1km · 실측 수요 모양 500m · 가동률 1km).

---

## 3. 시점차 교란 — 방향이 안 잡힌다 — (7)

경쟁점 문서 `createdAt`은 **전부 2026**(일괄 입력)이라 조사 시점을 못 대표한다. 우리 매장
오픈 연도를 대리값으로 썼다:
```
  오픈연도  n   경쟁점 품질평균  핑봇평균  r(품질,핑봇)  유의선
  2023     13     1.85         18.6%      0.270      ±0.55
  2024     12     2.15         23.4%     −0.426      ±0.58
  2025      8     2.47         18.1%      0.100      ±0.71
  2026     14     2.46         20.6%      0.414      ±0.53
  조사 경과년수 ↔ |잔차| r 0.122 (n.s.)
```
- 오래된 묶음일수록 상관이 약해지는 **단조 패턴이 없다**(2024가 음수). 교란이 크다는 증거 없음
- 묶음당 8~14곳이라 **크기는 못 잰다.** 사용자 판단("리뉴얼 드물다, 작다")과 어긋나지 않는다
- 부산물: 경쟁점 품질평균이 연도와 함께 오른다(1.85→2.46). **최근 동네일수록 경쟁점이 좋다**
  — 룸 확산 코호트([[project_cohort_effect_confound]])와 같은 현상

---

## 4. ⭐ 사용자에게 물을 것 — **팀룸이 좌석 가동률을 깎는 건 아닌가**

기전은 사용자 말대로 유입일 수 있는데 자료에 안 보인다. 상쇄 요인 후보 하나:
> 핑봇이 "켜진 PC 수"를 센다면, 5인 팀룸에 3명이 들어가면 그 룸은 60%다.
> 팀룸은 **손님을 끌지만(+) 좌석 효율은 깎는다(−).** 둘이 상쇄되면 가동률엔 안 보인다.

### ✅ 사용자 답(같은 날) — **상쇄 가설은 기각**
> *"팀룸 좌석효율이라하면, 최소이용인원이 있음. 5인룸이면 4인 이상 뭐 이런 기준 있음.
> 한산한 오전이나 야간은 제한 풀기도 하지만 암튼 기본적인 룰을 이렇게 진행함."*

- 피크에는 **최소 인원 규칙**(5인룸 4인 이상)이 있어 좌석 손실이 최대 20% 안팎이다. 내가
  가정한 40~60% 손실은 없다.
- 한산한 시간에 제한을 풀면 1~2명이 룸을 쓰지만, 그 시간엔 룸이 비어 있을 자리라 가동률에
  **더해지는** 쪽이다.
- → 팀룸이 좌석 가동률을 깎아 유입을 상쇄한다는 설명은 **안 선다.** 존구성이 자료에 안 보이는
  건 상쇄 때문이 아니라 **효과 자체가 작거나, 큰룸 경쟁점 11곳으로는 못 보는 것**이다.
- 존구성은 여전히 가동률 항으로 두는 게 맞다. 자리 문제도, 상쇄 문제도 아니고 **세기** 문제다.

---

## 5. ⛔ 닫힌 갈래 (다시 파지 마라)

| 갈래 | 결과 |
|---|---|
| 존구성을 수요 배수로(점수형 z^ζ) | 자사우위 3.16~4.52배로 **더 부푼다**. 자사 편향 +2~+23%p |
| 존구성을 수요 배수로(큰룸비 선형) | ζ=2에서 자사편향 0이지만 자사우위 2.88배(뺀 것 2.45보다 나쁨). 짝 r 0.583 |
| 점유율+수요 이중 | 5.16배. 대조군 확인용 |
| 주거 500m(가동률 잣대) | 자사 r 0.40→0.16. **잣대가 다르면 답이 다르다** |
| 경쟁점 createdAt으로 조사 시점 | 전부 2026. 못 쓴다 |
| 독점 3곳 원장 | **파일이 아직 없다**(폴더엔 기존 8곳만) |

---

## 6. 파일

| 경로 | 내용 |
|---|---|
| **`_zoneAsDemand.test.ts`** | ⭐ 오전. (1) 자리 바꾸기 (2) 잔차↔존 (3) 대조군 (4) θ·항목·기본값 (5) 격자 (6) 500m (7) 시점차 |
| **`_demandPathOutside.test.ts`** | ⭐ 오후. (1) 실측 수요 잣대 (2)(3) 세 잣대 두 배선 (4) 경쟁성숙 ⛔ (5) 같은 달 자사우위 |
| `.local-tools/sgis-resident-population.json` | SGIS 100m~5km 연령 분해 54곳(2026-09-20 수집). 고리 계산 자료. git 밖 |
| `_rivalAsSubjectFull.test.ts` | 세 잣대 원본 하네스. 이 파일의 주인공 조립을 그대로 가져왔다 |
| `_measuredDemand.test.ts` | 실측 수요 모양(500m이 이겼던 자리) — 가동률 잣대와 결론이 갈린다 |

---

## 8. ⭐⭐⭐ 수요 경로 — **1km 밖 고리가 기전 있는 경로다** (같은 날 오후, `_demandPathOutside.test.ts`)

사용자: *"수요경로는 너가 알아서 해바"* · *"먹거리·인테리어 기본값은 내리지 않는다(미비하게만)"*.
그래서 격차 손잡이는 θ만 두고, 수요 경로를 찾았다. **SGIS 파일에 1.5km·2km·5km 연령 분해가
이미 있었다**(`.local-tools/sgis-resident-population.json`, 40곳 전부. 1km 값은 스냅샷과 중앙 1.000배).

### 후보와 기전
| 후보 | 기전 | 판정 |
|---|---|---|
| (가) 1km 밖 고리를 거리감쇠로 더함 | PC방은 3시간·월 3.7회 목적지형. 1km 밖에서도 온다 | ⭐ 살아 있다 |
| (나) 경쟁점에 성숙 1인당 시간 11.58h | 핑봇 경쟁점은 1년 넘은 매장 | ⛔ **기각** (아래) |
| (다) 유동 ×0.30 | 없음(보정상수) | 대조용 |

10~50대 인구 배율(1km 대비): 1.5km 1.58배 · 2km 2.17배 · 5km 7.66배.

### 잣대 A — 실측 수요(14동네)
```
  변형                    배율(하한÷산식)  퍼짐   r(산식,실측)  산식퍼짐/실측퍼짐
  지금 그대로               1.15배        0.411   0.618        0.74배  (산식이 좁다)
  +1.5km고리 w1            0.74배        0.451   0.622        0.93배
  +2km고리 w.25            0.86배        0.420   0.640        0.84배
  감쇠 1/.5/.25 (2km)      0.78배        0.433   0.630        0.89배
  감쇠 1/.5/.25/.05 (5km)  0.66배        0.407   0.653        0.89배
  유동 x0.30                0.80배        0.390   0.606        0.72배
  수요 x1.5(상수)           0.77배        0.411   0.618        0.74배
```
고리는 **r을 조금 올리고 산식 퍼짐을 실측 쪽으로 넓힌다.** 상수배는 아무것도 못 바꾼다. 유동 ×0.30은
퍼짐 하나만 좋고 r·모양은 나쁘다.

### 잣대 B — 세 잣대
지금 배선(θ3·존구성 있음)에서는 고리가 **자사를 +6~+13%p 과대예측**한다 — 지금 점유율이 우리 몫을
너무 크게 잡고 있어서 수요를 키울 자리가 없다. 후보 배선(θ2·존구성 뺌)에서는:
```
  변형                    자사MAE 자사편향 자사r | 경쟁MAE 경쟁편향 경쟁r | 짝r  | 자사우위 예측
  존뺌·θ2 그대로           10.19%p  −9.9%p 0.293 | 10.4%p  −8.5%p 0.147 | 0.502 | 1.82배
  +1.5km고리 w1             7.88%p  +0.5%p 0.354 |  9.5%p  −2.8%p 0.159 | 0.511 | 1.82배
  +2km고리 w.5              7.78%p  +0.8%p 0.317 |  9.4%p  −2.7%p 0.186 | 0.508 | 1.82배
  감쇠 1/.5/.25 (2km)       7.54%p  −1.9%p 0.330 |  9.3%p  −4.1%p 0.173 | 0.502 | 1.82배
  감쇠 …/.05 (5km)          8.00%p  +2.7%p 0.340 |  9.3%p  −1.3%p 0.221 | 0.516 | 1.82배
  수요 x1.5(상수)            7.23%p  +0.5%p 0.293 |  9.3%p  −2.8%p 0.152 | 0.520 | 1.82배
```
⭐ 고리는 **상수 ×1.5와 같은 수준을 만들면서 자사 r을 0.29→0.32~0.35, 경쟁 r을 0.15→0.16~0.22로
올린다.** 상수배는 r을 못 움직인다. 모양이 맞는 쪽이라는 뜻이다. (자사 r 차이는 n=37에서 1SE 안팎.)
⚠️ 치르는 값: 지금 배선 대비 자사 MAE 5.84→7.5%p(2SE 0.82 넘는 악화)·짝 r 0.60→0.50. **안쪽이 나빠지고
바깥이 좋아지는** 과적합 해소 패턴 그대로다.

### ⛔ (나) 기각 — 그리고 **실측 자사우위도 시점 착시가 있었다** — (5)
지금까지 "자사우위 실측 1.49배" = 자사 **평가창(1년차)** ÷ 경쟁점 **2026-08**. 시점이 달랐다.
자사도 핑봇 달 월별 가동률로 바꿔 26동네를 다시 재면:
```
  자사우위 실측    평가창 기준 1.48배  ·  같은 달 기준 **1.32배**
  자사 가동률     평가창 → 같은 달(개월차 중앙 26)  **×0.93**  (안 는다. 조금 내려간다)
  1년 안 9곳      같은 달 우위 **1.74배**   ·   1년 초과 17곳  1.29배
```
- 1인당 시간은 1.41배 늘었는데(원장) 총시간은 ×0.93 → **이용자 수가 그만큼 줄었다.** 단골 축적은
  "소수가 오래 쓴다"다. 그러니 11.58h는 수요 눈금이 아니고 (나)는 기각이다. 같은 함정
  ([[project_per_user_hours_grow_with_age]])을 다시 밟을 뻔했다.
- ⭐ **산식이 맞혀야 할 목표 격차는 매장 나이에 따라 다르다.** 1년차 우리 매장 vs 성숙 경쟁점은
  **1.74배**(n=9, 흔들린다). 그 기준으로는 지금 3.50배가 2.0배 과장, 존뺌·θ3 2.45배가 1.4배 과장,
  존뺌·θ2 1.82배는 **거의 맞는다.** 전날 "2.35배 과장"은 1.49 기준이었다 — 조금 과하게 읽은 것.

### 다음
- 채택 후보 하나가 아니라 **묶음**이다: 존구성 뺌 + θ≈2 + 1km 밖 고리(거리감쇠). 하나만 하면 무너진다.
- 고리 가중(w)은 자료로 고르지 말고 **거리감쇠 규칙**으로 정한다(예: 1 / 0.5 / 0.25). 후보는 재고 표.
- ⚠️ 잣대 A는 n=14, 자사 r 차이는 1SE — **판정은 안 끝났다.** 핑봇을 더 모으는 게 제일 값싸다.

---

## 7. 자동화 도구(다른 세션)에 대한 답 — **바뀌지 않았다**

**V62를 쓴다.** 실험실은 구조 진단 중이고 채택된 변경이 없다.
