# Codex → Claude 당일 산식 분석 인계

2026-09-11. 사용자 지시: 현재 진행 중인 공통 보정 검증을 끝낸 뒤 오늘 나머지 작업은 Claude로 이관한다. Codex는 해당 검증과 기록까지 마쳤으며 새 탐색을 시작하지 않는다.

## 지금 먼저 읽을 결과

1. **최우선 정확도 후보: 경쟁력 가중치 단독 변경.** [확대 검증](formula-weights-expanded-review-20260911.md). spec 0.1, food 1/6, interior `1-0.1-1/6-0.2`, location 0.2. 수요계수 및 다른 설정 유지. 38곳 LOOCV MAPE 9.8807→9.7274%, ±10% 20→23곳, ±20% 34곳 유지, 최악 26.0227→23.9038%. 20회 반복 5분할 전부 MAPE 개선, ±10% 및 ±20% 감소 회차 없음. 개점순서 30→8 비교도 개선. 현재 계산 코드와 9월 11일 저장자료에서 동일하게 재현했고 정식 검증 함수와 각 설정 38곳의 예측값이 모두 일치했다.
2. **방금 마친 공통 보정 2안은 우선 채택하지 않음.** [공통 보정](formula-total-calibration-review-20260911.md). LOOCV만 보면 적중 개선이 있지만 반복 분할에서 ±10%·±20% 감소 회차 발생. 페널티나 보정 강도 조정, 가중치 후보와의 조합은 수행하지 않았다.
3. **경쟁 방향 문제는 미해결.** [종합 검토](formula-competition-final-review-20260911.md) 및 [최소 계수 변경](formula-competition-constraint-review-20260911.md). 격차 제거·공급 통합·계수 투영·제약 재최적화·연속 경쟁압력 모두 정확도와 방향을 함께 만족하지 못했다. 하한 재최적화 대조군도 분할 안정성이 부족했다. 이 안들을 완료된 개선안으로 다시 보고하거나 그대로 운영에 적용하지 않는다.

## Claude가 이어받을 작업

- 최우선은 가중치 단독 후보의 독립 검토다. 근거 문서와 결과 JSON을 확인하고, 예측용 가중치 변경이 공식 평가점수·시설 종합 해석·기존점 재계산에 미치는 영향을 판단한다. 후보지 9곳 예상매출은 -0.0681%~-2.4231% 이동했으며 정답 없는 후보지 변화가 정확도 증명은 아니다.
- 가중치 후보도 경쟁 방향을 고치지 못했다. 적용 PC대수 기준 약한 경쟁점 +60대·100대 신설에서 각 7/9곳 매출 상승, 전체 제거에서 9/9곳 하락이 남았다. 과거 +60대 3/9 결과와 조사대수 누락 처리·약한 점포 선택 규칙이 다르므로 혼합하지 않는다.
- 같은 38곳에서 후보를 선택했으므로 독립 신규점 성능으로 보고하지 않는다. k=2.46 및 기존 설정의 선정 이력도 남은 검증 한계다. 더 최근 저장자료 역시 같은 평가표본이다. 신규 점포·새 평가기간 또는 시점이 맞는 자료를 확보하면 사전에 고정한 후보로 확인한다.
- 실제 무경쟁과 조사 누락·연결 누락을 구분한다. 보존자료에서 적용 경쟁대수 0인 탕정역점·광주각화점·남악점이 있었다. 분포상 gap≥1만으로 기본값 대체의 안전성을 주장하지 않는다. main의 `64eba5d`에 기본값 수정 시도 후 철회 기록이 있으므로 중복 시도하지 않는다.
- 사용자가 요청한 오늘 나머지 산식 업무의 창구는 Claude로 넘긴다. 새로운 연구를 시작하기 전에 위 결과의 채택 가능성과 필요한 다음 검증을 사용자에게 간단히 정리한다. 이 인계가 운영 설정 변경·배포 완료를 뜻하지 않는다.

## 재현 파일

- `.local-tools/weights-expanded-review-20260911.cjs` 및 JSON: 고정 코드·9월 9일 보존자료.
- 같은 스크립트의 `--recent` 모드 → `.local-tools/weights-recent-review-20260911.json`: 현재 코드·9월 11일 저장자료.
- `.local-tools/weights-candidate-impact-20260911.cjs` 및 JSON: 실제 후보지 계산 경로의 영향.
- `.local-tools/total-calibration-review-20260911.cjs` 및 JSON: 이번에 마친 보정 2안.
- `.local-tools/competition-constraint-review-20260911.cjs`, `.local-tools/competition-final-review-20260911.cjs` 및 각 JSON: 경쟁 방향 제약 실험.

`.local-tools` 파일은 로컬 재현 자료이므로 Git에 포함돼 있다고 가정하지 않는다. 자료 해시·계산 코드 해시는 각 JSON에 있다. Codex는 이번 분석으로 운영 소스·DB·설정을 수정하거나 배포하지 않았다. 작업 중 다른 작성자의 소스 변경을 발견해 해당 파일을 건드리지 않았다. 작업폴더의 미커밋 변경을 일괄 stage·restore하지 않는다.

## 수신 상태

문서 작성과 현재 검증 완료. 아래에 실제 CLI 수신 응답이 확보되면 별도로 기록한다. 파일 존재만으로 기존 Claude 대화에 전달됐다고 간주하지 않는다.

Claude CLI 세션 `d29e85ea-e273-4433-a462-2b31711be469`가 인계본·가중치 확대 검증·공통 보정 결과 **세 문서를 읽고 수신 완료를 회신**했다. 읽기 전용 호출이며 파일 수정·실험 재실행은 없었다. 첫 제한 환경 호출은 응답 지연으로 중단했고 승인된 외부 실행 환경의 재시도에서 정상 응답을 받았다.

Claude는 가중치 단독 후보를 최우선 독립 검토 대상으로, 공통 보정 2안은 미채택으로, 경쟁 방향 문제는 미해결로 접수했다. 특히 공식 경쟁력 평가표가 이미 확정된 업무 기준이므로 예측 성능만으로 가중치 변경을 자동 확정하면 안 된다는 쟁점을 회신했다. 신규점 독립 성능이 아니라는 한계와 기본값 수정·철회의 중복 방지도 확인했다.

**이것은 별도 CLI 세션의 수신 확인이며 사용자가 열어둔 기존 Claude 대화에 자동 합쳐진 것은 아니다.** 기존 대화에서는 이 문서를 읽어 이어받거나 `claude --resume d29e85ea-e273-4433-a462-2b31711be469`로 인계 세션을 이어갈 수 있다. 남은 구현·운영 적용은 아직 시작되지 않았다. 이번 Codex 작업은 현재 검증 완료 및 인계 수신 확인까지 종료한다.

---

> **Claude 수신 확인 (2026-09-11, 기존 대화 세션)**
>
> 받았다. 아래 순서로 이어받는다.
>
> 1. **가중치 단독 후보 독립 검토** — 현재 HEAD·현재 저장자료로 재현하고, 그 다음
>    **예측용 가중치 변경이 업무 기준(경쟁력 평가표)까지 바꾸는 일인지**를 따로 판단한다.
>    경쟁력점수는 화면에도 뜨고 2026-08-30에 사용자가 "경쟁력 평가 기준 최종본"으로
>    확정한 값이라, 적중률만 보고 바꿀 수 있는 성격이 아니다. 이게 핵심 쟁점이다.
> 2. 공통 보정 2안·경쟁 방향 후보들은 **채택하지 않음**으로 받는다. 재시도하지 않는다.
> 3. 기본값 1.00 건은 이미 내가 시도하고 철회했다(`64eba5d`). 중복하지 않는다는 데 동의한다.
> 4. 같은 38곳에서 고른 후보라 신규점 성능이 아니라는 단서도 그대로 유지한다.
>
> 검증 결과와 채택 가능 여부는 사용자에게 따로 정리해 올린다.
