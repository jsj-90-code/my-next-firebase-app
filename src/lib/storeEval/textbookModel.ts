// 교과서식 산식 (실험판) — 2026-09-15 신설.
//
// 사용자 방향: "지금 가맹점 데이터 기준값으로 하는 건 일단 현재 시점에서 두고, 산식을 또
// 만드는 구조로 하는 건데. 이걸 내가 보고 판단하려면 웹에 반영이 되어야 한다. 예를 들어
// 점포평가 test 버전 같은 페이지를 또 하나 만드는 거지. 여기에 유동 100, 200 등을 추가해서
// 궁극의 목표로 가보자는 거지. 지금 가맹점 기준 데이터로 맞추는 건 모수가 늘어도 신빙성이
// 없을 것 같다."
//
// ⚠️ **이 파일은 기존 산식(calc.ts / usageRevenue.ts)을 대체하지 않는다.** 운영 화면은 계속
//    기존 산식을 쓰고, 이 모듈은 /store-eval/lab 실험 화면에서만 호출한다. 기존 파일은 한 줄도
//    건드리지 않는다 — 두 산식을 나란히 두고 비교하는 게 이 작업의 목적이다.
//
// ── 구조 ─────────────────────────────────────────────────────────────────
//   1) 총수요   = (주거인구 x 이용률 + 유동인구 x 이용률 x α) x 1인당 월이용시간
//   2) 점유율   = (자사PC x 격차^γ) / (자사PC x 격차^γ + 경쟁IP)
//   3) 자사수요 = 총수요 x 점유율   -> 가동률 = 자사수요 / (자사PC x 720)
//   4) 매출     = 자사PC x 720 x 가동률 x 총단가,  총단가 = PC몫(정가) + 상품몫(상수)
//
// ── 축척은 두 개다 (2026-09-16 구조 변경 · 2026-09-21 앞엣것을 원장 실측으로 고정) ──
//   층마다 실측값이 따로 있으니 축척도 따로 맞춘다. 자세한 근거는 fitHoursPerUser 위 주석.
//     hoursPerUserPerMonth <- **가맹점 원장 실측** 8.20h (오픈 1년 안 4곳 평균)
//     productUnitPrice     <- **전 매장** 실매출÷실측가동률 (직접 측정되므로 독점 제약 없음)
//   그전에는 축척 하나가 둘을 겸해서, 환산층이 틀린 만큼이 가동률로 되밀려 들어갔다.
//
// 기존 산식과 다른 점은 **회귀로 덮지 않는다**는 것이다. 기존 V61/V62는 위 값을 특징 하나로
// 넣고 기존 38곳 평균에 맞춰 회귀하는데, 그래서 "기존 가맹점 평균에서 ±"라는 설명밖에 못 한다.
//
// ── 알려진 한계 (2026-09-15 실측) ────────────────────────────────────────
//   관측 총수요를 인구로 맞춘 최선이 MAPE 42%다(주거1km + 유동x0.2). 목표 10%, 마지노선
//   20%에 한참 못 미친다. 반경·가중을 바꿔도 이 벽이 있다:
//     주거 500m 총수    r=0.299   주거 1km 총수  r=0.511   주거 1km 연령가중 r=0.516
//     유동 500m 총수    r=0.692   유동 500m 연령가중 r=0.635
//   그래서 이 모듈의 기본값은 "정답"이 아니라 **출발점**이다. 화면에서 손으로 돌려보며
//   어디까지 가는지 재라고 파라미터를 전부 밖으로 뺐다.

/** PC방 이용률 가중 — 연령대별로 "이 연령 100명 중 몇 명이 PC방을 쓰나". */
export type AgeUsageWeights = {
  age0s: number; age10s: number; age20s: number; age30s: number;
  age40s: number; age50s: number; age60plus: number;
};

/**
 * 연령·성별 PC방 이용률 — **1,000명 중 몇 명이 PC방을 쓰나** (2026-09-16 사용자 제공 실측 조사).
 *
 *   연령   남성   여성
 *   10대   390    130
 *   20대   420    150
 *   30대   170     45
 *   40대   100     20
 *   50대    35      8
 *
 * 이 값은 **우리 데이터로 맞춘 게 아니다.** 외부 조사값을 그대로 넣었는데 독점매장 3곳이
 * 최대오차 0.8% 안에 들어왔다(직접 만든 추정 가중치는 3.2~4.7%였다). 과적합일 수 없는
 * 종류의 일치라 이 값을 신뢰한다.
 *
 * ⚠️ 2020~2021 조사라 절대 수준은 낡았을 수 있다. 다만 산식에서 의미를 갖는 건 **연령·성별 간
 *    비율**뿐이고, 전체 수준은 hoursPerUserPerMonth(축척 계수)가 흡수한다.
 */
export const PC_USE_RATE_MALE: AgeUsageWeights = {
  age0s: 0, age10s: 0.390, age20s: 0.420, age30s: 0.170,
  age40s: 0.100, age50s: 0.035, age60plus: 0,
};
export const PC_USE_RATE_FEMALE: AgeUsageWeights = {
  age0s: 0, age10s: 0.130, age20s: 0.150, age30s: 0.045,
  age40s: 0.020, age50s: 0.008, age60plus: 0,
};

/**
 * 지역 남성비율 m으로 남녀 이용률을 섞는다. m=0.5면 남녀평균이다.
 *
 * ⚠️ **성별 x 연령 교차 자료는 존재하지 않는다.** SGIS도 소상공인365도 성별 총수와 연령 총수를
 *    따로 줄 뿐이다(2026-09-16 확인, classDeg 1·2·3 전부 같은 74필드). 그래서 "각 연령대 안의
 *    성비 = 그 지역 전체 성비"로 근사한다. 이건 임시방편이 아니라 자료의 한계다.
 */
export function blendUsageByGender(maleRatio: number | null): AgeUsageWeights {
  const m = maleRatio == null || !Number.isFinite(maleRatio) ? 0.5 : Math.min(1, Math.max(0, maleRatio));
  const mix = (key: keyof AgeUsageWeights) => m * PC_USE_RATE_MALE[key] + (1 - m) * PC_USE_RATE_FEMALE[key];
  return {
    age0s: mix("age0s"), age10s: mix("age10s"), age20s: mix("age20s"), age30s: mix("age30s"),
    age40s: mix("age40s"), age50s: mix("age50s"), age60plus: mix("age60plus"),
  };
}

/**
 * 유동인구를 어느 반경으로 쓸지.
 * 2026-09-15에 100/200/300/400m 수집 경로를 열었다(scripts/collectSbizFloatingPopulation.mjs).
 * 500m는 원래 갖고 있던 값이라 출처가 다르다 — 반경끼리 비교할 땐 100~400m 안에서 본다.
 */
export type FloatingRadius = 100 | 200 | 300 | 400 | 500;
/** 주거 연령 분해 한 벌 — 1km 원과 1km 밖 고리(누적 원)에 같은 모양을 쓴다. */
export type ResidentAges = {
  age0s: number; age10s: number; age20s: number; age30s: number;
  age40s: number; age50s: number; age60plus: number;
};
/** 1km 밖 고리의 바깥 반경(m). 값은 **그 반경 원의 누적 인구**다(고리 인구는 계산에서 뺀다). */
export type ResidentRingRadius = 1500 | 2000 | 5000;
/** 고리 띠 — 안쪽부터. 무게는 중간반경에서 잰다(`residentRingWeight`). */
export const RESIDENT_RING_BANDS: readonly { from: 1000 | ResidentRingRadius; to: ResidentRingRadius; midM: number }[] = [
  { from: 1000, to: 1500, midM: 1250 },
  { from: 1500, to: 2000, midM: 1750 },
  { from: 2000, to: 5000, midM: 3500 },
];
/** 고리 무게 = exp(−(중간반경 − 1000) ÷ λ). λ≤0이면 0(꺼짐). 1km 안은 항상 1이다. */
export function residentRingWeight(midM: number, decayM: number): number {
  return decayM > 0 ? Math.exp(-(midM - 1000) / decayM) : 0;
}
/**
 * 우리에서 거리 d(m)인 경쟁점이, 우리 중심 반경 r(m) 원 위의 주민 중 **우리보다 가까운** 비율.
 * 원 위 점 P에 대해 |P−경쟁점| < |P−우리| ⇔ P가 수직이등분선 너머 ⇔ 각도 조건 → arccos(d/2r)/π.
 * d ≥ 2r면 원 위 어느 점도 경쟁점이 더 가깝지 않다(0). 거리를 모르면 보수적으로 0.5(반반).
 * ("gravity" 고리 점유율용. 기하라 맞춘 계수가 없다.)
 */
export function ringRivalFraction(distanceM: number | null | undefined, ringMidM: number): number {
  if (distanceM == null || !Number.isFinite(distanceM)) return 0.5;
  if (!(ringMidM > 0)) return 0;
  const x = distanceM / (2 * ringMidM);
  if (x >= 1) return 0;
  return Math.acos(Math.max(0, x)) / Math.PI;
}
/** 주거인구를 어느 반경으로 쓸지. 500m는 연령 분해가 없어 총수만 쓸 수 있다. */
export type ResidentRadius = 500 | 1000;

export type TextbookParams = {
  residentRadius: ResidentRadius;
  floatingRadius: FloatingRadius;
  /** 주거인구에 연령가중을 적용할지. 500m는 연령 자료가 없어 무시된다. */
  useResidentAgeWeights: boolean;
  /** 유동인구에 연령가중을 적용할지. */
  useFloatingAgeWeights: boolean;
  /** 유동인구를 수요로 볼 때 깎는 비율. 스쳐 가는 사람이 대부분이라 1.0일 수 없다. */
  floatingFactor: number;
  /**
   * **1km 밖 고리 감쇠 λ(m)** — 2026-09-23 신설, 같은 날 600으로 채택 → **저녁에 800**(고리 점유율을 gravity로
   * 바꾸며 다시 고름. 아래 `residentRingShare`). 0이면 끈 것. 주거 1km에서만 작동한다.
   *
   * PC방은 평균 3시간·월 3.7회 오는 **목적지형**이라 1km 밖에서도 온다. 1km 원 밖의 고리
   * (1~1.5km · 1.5~2km · 2~5km) 인구를 연령가중해서 더하되, 멀수록 덜 센다:
   *
   *   고리 무게 w = exp(−(고리 중간반경 − 1000) ÷ λ)     (1km 안은 무게 1)
   *   λ=1000이면 1.25km 고리 0.78 · 1.75km 0.47 · 3.5km 0.08
   *
   * ── 왜 이 경로인가 (2026-09-23 측정, `_demandPathOutside.test.ts`) ─────────────
   * 수요 수준이 약 1.5배 작다는 게 바깥 표본(경쟁점 핑봇 47곳·실측 수요 14동네)에서 드러났다.
   * 상수배(×1.5)는 수준만 옮기고 매장 간 순서(r)를 못 움직인다. 고리는 같은 수준을 만들면서
   * 자사 r 0.29→0.35 · 경쟁 r 0.15→0.22(존구성 뺌·θ2 배선)로 **순서까지 움직였다** — 모양이
   * 맞는 쪽이라는 증거다. 주거 500m·유동 ×0.30·경쟁점 11.58h는 같은 자리에서 기각됐다.
   *
   * ⚠️ **옛 배선(θ=3·존구성 0.238)에서 켜면 자사를 +6~13%p 과대예측한다.** 점유율이 우리 몫을
   *    너무 크게 잡고 있어 수요를 키울 자리가 없다. θ1.75 + 존구성 0.06과 **묶음으로만** 뜻이 있다.
   *    채택 칸(2026-09-23, 사전 등록 기준 1위): 자사 MAE 7.82%p(옛 5.84) · 자사 편향 +0.1%p · 경쟁점 편향 −1.6%p
   *    (옛 −10.9) · 자사우위 예측 1.77배(1년차 실측 1.74 · 옛 3.50). 안쪽이 나빠지고 바깥이 좋아지는 과적합 해소.
   * ⚠️ 고리 인구는 `TextbookInput.residentAgesByRadius`(누적 원 인구)로 들어온다. 자료가 없으면
   *    `missing`에 "1km 밖 고리 인구"를 적고 1km만으로 계산한다 — 지어내지 않는다.
   *    자료 출처: SGIS 반경 집계(`.local-tools/sgis-resident-population.json`, 40곳 전부).
   *    화면(실험실 Firestore 복제본)에는 아직 없다 — 동기화 스크립트가 필요하다.
   */
  residentRingDecayM: number;
  /**
   * **고리 수요의 점유율을 어떻게 구하나** (2026-09-23 신설).
   *
   *   "core"    — 고리 수요도 1km 안 수요와 **같은 점유율**을 먹는다(우리 매장 기준 경쟁점 거리감쇠).
   *               λ600 채택 직후의 동작. ⚠️ 밀집 도심이 부푼다 — 1~2km 고리 사람은 2km까지 세면서
   *               그 사람들 곁의 PC방은 경쟁으로 안 세기 때문(경쟁 감쇠는 사실상 500m).
   *               사용자(2026-09-23): 자사 예측 SD 8.1→9.9, 최악이 발산역 +23·수원망포 +21%p.
   *   "gravity" — 고리 사람은 **우리와 그 근처 PC방 중 가까운 쪽으로 간다.** 우리에서 거리 d인 경쟁점이
   *               반경 r 고리 주민 중 얼마에게 우리보다 가까운지는 **기하로 정해진다**:
   *                   g(d, r) = arccos(d ÷ 2r) ÷ π     (d ≥ 2r면 0)
   *               예: d=500·r=1250 → 44% · d=1000·r=1250 → 37% · d=1900·r=1250 → 23%.
   *               고리 띠마다 점유율 = 자사PC ÷ (자사PC + Σ 경쟁PC × 품질비^θ × g)로 따로 구하고,
   *               1km 안 점유율(기존 그대로)과 **이용자 수로 가중평균**한다. 맞춘 계수가 없다.
   *               ⚠️ 500m 밖 경쟁점 대수는 미조사 기본값이다. g ≤ 0.5라 한 경쟁점이 분모를 휩쓸진 않지만
   *                  대수 자체의 불확실성은 그대로 들어온다.
   * ✅ 2026-09-23 저녁 **"gravity" 채택**(`_bundleCandidate` (6), 경쟁점 주인공에도 2km 경쟁점을 넣은 공정한 하네스):
   *   core λ600     자사 MAE 7.82 · 편향 +1.3 · r 0.337 · 예측 SD 9.9 · ±5%p 14 | 경쟁 −3.3 | 짝 r 0.551 | 우위 1.84 | 최악 발산역 +23
   *   gravity λ800  자사 MAE 6.88 · 편향 −1.2 · r 0.187 · 예측 SD 6.7 · ±5%p 15 | 경쟁 −2.7 | 짝 r 0.533 | 우위 1.65 | 최악 구미산동 +21
   * 퍼짐이 실측(6.4)으로 돌아오고 밀집 도심 과대가 사라진다. 치르는 값은 동네 간 순서(자사 r). 남은 최악
   * 구미산동 +21·전대후문 −18은 고리 문제가 아니라 그 매장 사정일 가능성 — 현장에 물을 것.
   */
  residentRingShare: "core" | "gravity";
  /**
   * **항아리(막힌) 상권 보정** (2026-09-23 신설·같은 날 밤 채택). 켜면 고리 인구에 `(1 − 깎는 방향 수 ÷ 4)`를 곱한다.
   *
   * 사용자 정의: *"항아리라는 건 하천이나 철도로 끊겼다기보다는 상권이 그 주변에만 형성되어 있고 반경 이상에는 상권이
   * 없다는 것."* → 문항은 **"그 방향 1~2km 고리에 사람 사는 동네(아파트·주택가·시가지)가 있나"**. 없는 방향(논밭·산단·
   * 산·물)의 고리 주민은 없는 것이니 깎는다. 지도 한 장을 AI가 **예/아니오 사실**로 판정(scripts/tradeArea, 로드뷰 판정과
   * 같은 원칙 — 점수 아님) → 사용자가 깎이는 8곳 목록을 대조해 *"맞음"*. 사실 개수 ÷ 4라 맞춘 계수가 없다.
   * ⛔ 1차 문항("하천·철도·고속도로로 끊겼나")은 폐기 — 63%가 2방향 이상으로 후하고 구미산동을 못 잡았다.
   * ⚠️ "2km 안 유일 상권 → 고리 0"은 규칙이 아니다(청주터미널·화명대로가 크게 과소). 입력은 `ringBlockedDirections`(0~4).
   */
  useRingEnclosure: boolean;
  ageWeights: AgeUsageWeights;
  /** 이용자 1인당 월 PC방 이용시간. 총수요를 시간 단위로 바꾸는 계수. */
  hoursPerUserPerMonth: number;
  /**
   * 축척을 **원장 실측값으로 못 박을지** (2026-09-21 채택. 사용자 지시).
   *
   * true면 `fitHoursPerUser`가 역산을 안 하고 `hoursPerUserPerMonth`를 그대로 돌려준다.
   * 그전에는 독점매장 실측 가동률에서 **역산**했는데, 그러면 축척 하나가 두 일을 겸했다.
   *   (가) 한 사람이 한 달에 몇 시간 쓰나        ← 가맹점 원장으로 **실측 가능**
   *   (나) 수요 추정이 몇 배 틀렸나(이용률표·반경) ← 못 잰다
   * (가)를 실측으로 못 박으면 남는 어긋남이 곧 (나)가 되어 **수요식 검증 경로가 열린다.**
   *
   * 값 8.20h의 근거: 가맹점 건별 매출원장 8곳 중 **오픈 1년 안** 4곳의 평균
   * (광주첨단 8.58 · 문산 9.39 · 증평 7.88 · 발산역 6.95). `_ledgerPerUser.test.ts`.
   * ⚠️ 1년 초과 4곳은 평균 11.58h로 1.41배 크다(단골 축적). 평가창이 오픈 1년이라
   *    **섞으면 안 된다** — 섞은 값(11.58)으로 재면 편향이 +3.02%p로 혼자 튄다.
   *
   * 못 박아도 성적으로 치르는 값이 없다는 게 채택 근거다(`_scaleMeasured.test.ts`,
   * 2026-09-21): 역산 8.3854h에서 MAE 3.874%p → 실측 8.20h에서 3.836%p.
   * 실측 범위(6.95~9.39) 전체에서 3.82~4.11%p로 **성적이 축척에 둔감하다.**
   *
   * ⚠️ 원장이 늘면 이 값을 갱신한다. 되돌리려면 false로 두면 옛 역산으로 돌아간다.
   */
  hoursPerUserFixed: boolean;
  /** 점유율에서 경쟁력격차를 얼마나 세게 볼지. 1이면 기존 구조와 같다. */
  gapExponent: number;
  /**
   * **PC방을 안 가는 몫** (Huff 모형의 no-purchase option). 경쟁IP와 같은 단위로 분모에 더한다.
   *
   * 2026-09-15에 이게 없어서 교과서식이 무너졌다 — 경쟁점이 0곳이면 점유율이 100%가 되어
   * 그 동네 수요를 통째로 먹는다. 광주각화점 2억2천(실제 7,616만), 남악점 1억7천(실제 6,286만),
   * 탕정역점 1억6천(실제 7,586만)이 전부 점유율 100% 케이스였다.
   * 경쟁 PC방이 없다고 동네 사람이 다 우리 매장에 오지는 않는다. 그 "안 오는 몫"이 이 값이다.
   */
  outsideOptionIp: number;
  /**
   * **우리 몫 상한** (2026-09-24 밤 신설) — 분모에 `k × 자사PC`를 더한다. 경쟁이 하나도 없어도 점유율이 1/(1+k)를 못 넘는다(k 0.25 → 80%).
   *
   * 왜: 경쟁 없는 자리에서 산식은 동네 수요를 100% 먹는다고 봤다. 독점 자사 4곳의 실제 먹은 몫은 탕정역 68 · 구미산동 72 · 남악 83 · 광주각화 83%다.
   * 후보지도 같은 자리에서 높았다(사용자 실무 감각: 호구포역 6천 중후반인데 산식 8,243만 · 오송 3천 중반인데 4,864만 · 마산산호 5천만대인데 7,442만).
   * `outsideOptionIp`(상수 IP)는 수요 작은 소도시를 제일 깎아 기각됐고(`_labCandidate` (12)), 자사 규모에 비례하는 몫은 경쟁 많은 곳(몫 20~40%)엔
   * 거의 안 걸리고 독점형에만 건다. 뜻은 "아무리 좋아도 동네 손님의 80%가 최대" — 집에서 하거나 원정 가는 몫이다.
   * 재고 표 `_layerSplit` (7): k0.25 → 자사 이해되는 오차 4.23 → 4.07%p · 후보지 실무 잣대 0.42 → 0.34 · 경쟁점 편향 −11.3 → −11.7(같음).
   * 자사에서 움직인 곳: 탕정역 +7 → 0 · 남악 +5 → −2 · 일산탄현 +13 → +8 · 시흥배곧 +12 → +7 / 나빠진 곳: 청주지웰 −1 → −7 · 광주각화 −6 → −10.
   * ⚠️ k는 독점 4곳 + 후보지 실무 6곳에서 고른 값(0.15/0.25/0.35 중 0.25). 표본이 작아 값이 아니라 방향. 고리 gravity 점유율에도 같은 상한을 건다.
   */
  ownShareCapK: number;
  /**
   * **고리 사람의 품질 지수** (2026-09-24 밤 신설, 기본 없음 = θ와 같음). 1km 밖 고리 주민이 "우리와 그 근처 PC방 중 어디로 가나"를 정할 때
   * 경쟁점 품질비에 붙는 지수. 사용자(양주덕정): *"1~2km 사이 배후지 인구는 PC방 가고 싶으면 우리 상권밖에 선택지가 없어서 온다. 1.6km까지는
   * 오는 것 같고 2km 밖은 힘들 듯."* — 먼 사람은 품질보다 **거리**로 상권을 고른다. θ3을 고리에도 그대로 쓰면 밀집 도심의 고리 사람까지 우리 쪽으로
   * 몰려(전에 고리를 켰을 때 밀집 +11%p) 되돌렸었다. 여기서 0이면 고리 사람은 대수·거리로만 나뉘고, 1이면 품질이 약하게만 작용한다.
   * `_labCandidate` (13)에서 λ × 고리θ를 잰다. null/undefined면 옛 동작(θ 그대로).
   */
  ringQualityExponent?: number | null;
  /**
   * **상권 흡인력** — 경쟁점이 많다는 건 그 자리가 좋다는 뜻이기도 하다.
   * 수요에 (1 + factor x ln(1 + 경쟁점수))를 곱한다. 0이면 끈 것이다.
   *
   * 지금 구조는 경쟁을 벌점으로만 본다. 그래서 금촌역·양주덕정·수원인계처럼 경쟁이 많은데
   * 실제로 잘 되는 매장을 절반 아래로 깎아버렸다(오차 56~64%).
   */
  agglomerationFactor: number;
  /**
   * **주거 밀집도 보정** — 2026-09-15 야간 발견.
   *
   * 치우침(예측/실제-1)이 500m 주거인구와 r=0.660으로 강하게 붙어 있었다(유의선 0.32).
   * 수요를 1km 주거인구로 재는데, 같은 1km 인구라도 500m 안에 얼마나 몰려 있는지가
   * 매장마다 다르다. 가까이 몰려 있는 곳을 그만큼 더 크게 세고 있었던 것이다.
   *
   * 수요에 (500m주거 / 1km주거)^(-계수)를 곱해 상쇄한다. 0이면 끈 것이다.
   */
  densityCorrection: number;
  /**
   * **상품몫** — PC 1대가 1시간 채워졌을 때 들어오는 **상품매출**(먹거리·음료, 원).
   *
   * 2026-09-16 저녁(3) 신설. 그전에는 총단가 하나에 정가를 통째로 곱했는데, 그러면
   * **요금을 올리면 라면 값도 같이 오르는** 꼴이 된다. PC방 매출의 절반이 상품이라
   * (실측 32곳 상품몫 중앙 1,456원 vs PC몫 1,366원) 그냥 두면 안 되는 크기다.
   *
   *   총단가 = PC몫(정가가 여기에만 들어감) + 상품몫(정가와 무관)
   *
   * 독점 3곳에서 총단가가 16.8% 벌어지는데, 그 차이의 **75%가 상품몫**이다
   * (탕정역 1,591원 vs 남악 1,247원). PC몫 차이는 25%뿐이다. 그래서 정가로 총단가를
   * 맞히려던 옛 구조는 애초에 4분의 1짜리를 겨냥하고 있었다.
   *
   * ⚠️ 상품몫을 후보지에서 예측할 방법은 아직 없다. PC대수 r=0.251 · 정가 r=−0.015 ·
   *    가동률 r=0.118로 전부 유의선 0.354 미만이고, 좌석 구성 19개 항목도 다중비교 수준이다.
   *    경쟁력점수가 r=0.713으로 강해 보이지만 **착시다** — 평가창 시점을 통제하면 0.170으로
   *    무너진다(경쟁력점수 ~ 시점 r=0.913). 쓰지 않는다. 사용자 결정(2026-09-16)과도 맞는다:
   *    "독점매장은 경쟁력점수로 맞추는건 안할거고. 애초에 점유100이라 경쟁력점수가 의미가없음".
   *    그래서 지금은 **상수**이고, `fitProductUnitPrice`가 실측 가동률이 있는 전 매장에서 구한다.
   */
  productUnitPrice: number;
  /**
   * 상품몫을 **한 번 재서 못 박을지** (2026-09-21 밤 채택. 사용자 지시).
   *
   * true면 `fitProductUnitPrice`가 표본에 맞추지 않고 `productUnitPrice`를 그대로 돌려준다.
   *
   * ── 왜 ────────────────────────────────────────────────────────────────────
   * 사용자: *"표본 평균 하지 말자. 표본평균 산식에 넣는거 안좋은거같아."*
   * 그전에는 38곳 평균으로 **매번 다시 맞췄다.** 그러면 매장을 하나 넣고 빼는 것만으로
   * 산식이 움직인다(송도·동탄북광장을 넣으면 1,450 → 1,470원). 축척에서 겪은 것과 같다.
   *
   * ── 산식을 찾아봤고, 못 찾았다 ────────────────────────────────────────────
   * 사용자: *"산식을 어떻게해서든 찾아내는걸로 해보는게 정답같은데."* 그래서 훑었다
   * (`_productShareFormula.test.ts`). 후보지에서 아는 변수 20개 중 유의선 ±0.41을 넘은 건
   * **유동인구 하나뿐**이다(500m r=.434 · 대조군 p=.0055, 본페로니 .0025는 미달).
   * 뜻은 통하는데(지나다니는 사람이 많으면 먹거리를 더 산다) **홀드아웃에서 상수를 못 이긴다**
   * — LOO MAE 189 vs 상수 204원, 95% 구간 [−42, +13]이 0을 품는다.
   * ⚠️ 그리고 **어떤 식을 찾아도 상수항은 남는다**(적합식 −178 + 146·ln유동의 −178).
   *    표본 평균이 사라지는 게 아니라 값이 매장 사정에 반응하게 되는 것뿐이다.
   * 🟡 이 갈래는 **닫지 않는다.** 표본 45~50곳이면 다시 잰다(신호가 없는 게 아니라 검정력 부족).
   *
   * ⚠️ 후보지에는 어차피 이 상수 하나가 들어간다 — 실매출이 없으니 달리 방법이 없다.
   *    매장별 실측값을 예측에 쓰면 기존점 채점이 "답을 보고 맞히기"가 된다.
   */
  productUnitPriceFixed: boolean;
  /**
   * **정가 탄력도 β** — 실제로 받는 시간당 요금이 정가를 얼마나 따라가는지.
   *
   * 2026-09-16 저녁(3)부터 이 지수는 **PC몫에만** 걸린다(상품몫은 정가와 무관하다). 그래서
   * PC몫 = 기준정가 x (정가/기준정가)^β가 되고, 이건 운영 산식 `effectiveHourlyRate`와 **완전히
   * 같은 식**이다. 실측으로도 축척이 필요 없었다 — 32곳에서 실측 PC몫 ÷ eff(정가)의 기하평균이
   * 1.005라 eff()가 수준까지 그대로 맞는다.
   *
   * **운영 산식의 `effectiveHourlyRate`(usageRevenue.ts)와 같은 형태다.** 거기 기본값은
   * `tariffEffectiveExponent = 0.546` · `tariffReferenceRate = 1343`(38곳으로 2026-09-14에
   * 구함)이고, 이 β에 0.546을 넣으면 그 식과 완전히 같아진다. 2026-09-16에 32곳으로 독립
   * 측정한 값도 0.49~0.535라 그 지수 자체는 잘 선 값이다 — 32곳 실효단가를 MAPE 9.16%로
   * 맞힌다(정가를 그대로 쓰면 10.43%).
   *
   * **왜 이 지수가 필요한가 — 정액권 할인이 실체다.**
   *
   * 건별 원장(바탕화면 `좌석가동률_7월`, 8곳)으로 2026-09-16에 처음 직접 측정했다. 정액권
   * 손님은 시간당 296~663원으로 **정가의 1/3**만 내고, 그 시간 비중이 매장마다 2.1~9.1%로
   * 4배 차이 난다. 그래서 정가 1,700원 매장이 실제로는 1,397원(82%)만 받고, 1,000원 매장은
   * 1,255원(126%)을 받는다. 이 지수는 그 **평균**을 한 숫자로 담은 것이다.
   *
   * 사용자(2026-09-16): "매장별 정액할인 금액이 틀리고 신규후보지도 마찬가지로 정액할인에
   * 대한 정보가 없으니까 평균값이 가장 정답일것같다" — 맞는 판단이다. 정액할인의 *크기*는
   * 매장 운영 정책이라 후보지에서 알 수 없지만, **정가가 비쌀수록 할인이 크다는 경향**은
   * 38곳에서 측정된 정보라 버리지 않는다. 여기서 쓰는 "평균"이 바로 그 조건부 평균이다.
   *
   * ⚠️ **요금제 자체는 아직 반영되지 않는다.** 들어가는 건 `시간당 정가` 한 숫자뿐이고,
   *    정액권 가격·구성, 좌석별 추가과금, 상품 가격은 안 들어간다. 매장별 정액권 비중을
   *    받을 수 있게 되면 그때 이 지수를 매장별로 풀 수 있다.
   *
   * ── 구조 비교 (환산층만 떼어 잰 성적, 실측 가동률을 넣고 매출만 계산, 32곳) ──────
   *
   *   덧셈(채택) PC몫 + 상품몫 1,508원        MAPE 10.48%  · 독점 최대 10.1%
   *   곱셈(옛것) 2,681 x (정가/1300)^0.546   MAPE  9.23%  · 독점 최대 12.9%
   *   요금 무시  총단가 2,928원 상수           MAPE 12.18%  · 독점 최대  8.7%
   *
   * 덧셈이 전체 MAPE는 1.1%p 나쁘다. 곱셈이 반칙 이득을 보기 때문이다 — 정가가 상품몫과도
   * 약하게 붙어 있어서(r=0.405, 비싼 매장이 먹거리도 잘 판다) 정가가 "매장 전반의 급"을
   * 대신 나타내는 역할을 한다. 논리적으로는 그러면 안 된다. 덧셈은 독점 오차가 낮고
   * 요금 인상 반응도 맞다(정가 +80%일 때 총단가 덧셈 +16% vs 곱셈 +38%).
   * 2026-09-16 사용자 결정: "덧셈으로하자".
   */
  rateElasticity: number;
  /** 정가 탄력도의 기준점(원). 이 정가인 매장에서 총단가 = T0가 된다. 32곳 중앙정가. */
  referenceHourlyRate: number;
  /**
   * **특수수요 배수** — 인구 통계에 안 잡히는 수요원이 있는 상권의 **수요를** 키운다.
   *
   * 사용자(2026-09-16): "기존처럼 입지평가로 특수수요를 플러스하는게아니라 수요산식을 정할떄
   * 적용해보자고" — 그래서 입지 보정이 아니라 **수요 산식 안**에서 곱한다.
   *
   *   수요 = (주거 + 유동 x α) x 특수수요배수
   *
   * ⚠️ **운영 산식(V62)의 특수수요와 이름만 같고 형태가 다르다**(사용자 지적: "기존 특수수요는
   *    말은같지만 형태가 많이 틀릴텐데"). V62는 `computeSpecialDemandScore`를 **회귀의 이진
   *    더미 피처**로 써서 예측값을 통계적으로 밀어준다 — "이런 매장은 평균보다 잘 나오더라"다.
   *    교과서식은 회귀로 덮지 않는 게 목적이라 수요 배수로 다시 정의한다 — "군부대·대학가·
   *    산업단지가 인구 통계에 안 잡히는 이용자를 데려온다"는 물리적 뜻이다.
   *
   * ── 2026-09-16 1차 측정 (지금은 일부만 살아 있다) ────────────────────────
   * 필요 점유율(= 실측가동률 ÷ 경쟁없을때 예측)을 유형별로 보고 "없음" 대비 배수를 잡았다:
   *
   *   군부대   2곳 118.3% -> 2.25배      대학가   5곳 76.0% -> 1.45배
   *   산업단지 3곳  72.8% -> 1.39배      기타     4곳 65.6% -> 1.25배
   *   없음    22곳  52.5% -> 1.00        관광·유흥 2곳 44.1% -> 0.84배
   *
   * ── ✅ 2026-09-20 2차 가공 — **산업단지·기타를 1.00으로 껐다** ────────────
   * 1차 값은 **순환에 오염돼 있었다.** 수요 축척 A는 독점 3곳에서 정하는데 그 3곳이
   * 탕정역(산업단지)·남악(기타)·광주각화(없음)이다. 즉 **산업단지와 기타의 배수가 자기 자신을
   * 재는 자를 부풀리고 있었다.** 배수를 켜면 A가 6.796 -> 5.653으로 내려가, 배수가 없는
   * 22곳은 가만히 있는데 수요가 16.8% 줄었다. 그리고 필요 점유율 100% 초과 6곳이 **전부
   * "없음"**이었다 — 제일 굶는 쪽에서 걷어다 특수수요 쪽에 얹어 주고 있었다.
   *
   * 자를 바로잡고 다시 뽑으니(독점은 유도에서 제외, `_anchorExemption.test.ts` (5)절):
   *
   *   군부대   2.22 (1차 2.25)  그대로    ← 자를 고쳐도 안 변한다 = 진짜 신호
   *   대학가   1.45 (1차 1.45)  그대로    ← 이때는 남겼다. **2026-09-20 밤에 껐다(아래 3차)**
   *   산업단지 1.08 (1차 1.39)  **사실상 1**
   *   기타    0.94 (1차 1.25)  **사실상 1**
   *
   * LOO 흔들림도 작다(산업단지 1.07~1.08 · 기타 0.87~1.10). 그래서 둘을 1.00으로 둔다.
   *
   * ── ✅ 2026-09-20 밤 3차 — **대학가도 1.00으로 껐다** ────────────────────
   * 2차 재도출(경쟁을 무시한 필요 점유율 기준)에서는 1.45가 살아남았는데, **경쟁 항까지
   * 넣고 보면 달라진다.** 근거 셋이 같은 곳을 가리켰다.
   *
   * **(1) 대학가는 방학에 안 뛴다** — 여기가 결정적이다.
   *   사용자(2026-09-20): *"대학가는 방학 때는 비성수기, 개학 때가 성수기지."*
   *   월별 가동률로 재니 그대로다(`_accuracyCeiling.test.ts` (1)절):
   *
   *       방학(1·2·7·8월) ÷ 학기월     없음 1.178 · 군부대 1.260 · **대학가 1.019**
   *
   *   일반 상권은 방학에 18% 뛰는데(중고생이 동네에 있다) 대학가는 안 뛴다 — 대학생이
   *   고향으로 가기 때문이다. 우리가 맞히는 건 **연평균**이므로, 대학가는 연평균 수요가
   *   남들보다 **낮아야** 맞는다. 그런데 배수를 **올려서** 주고 있었다. 방향이 반대였다.
   *
   * **(2) 경쟁까지 넣은 함축배수가 1.03이다**(`_universityTowns.test.ts` (3)절).
   *   매장별로 역산하면 부경대 0.854 · 전대상대 1.024 · 전대후문 1.034 · 울산대 1.227 ·
   *   청주대 1.276 — **중앙 1.034이고 1.45는 다섯 곳 중 아무에게도 안 맞는다.**
   *   대학가 5곳은 전부 '경쟁 과소'(예측 점유율 > 필요 점유율)다. 즉 1.45는 수요 배수인
   *   척하면서 실은 **경쟁 항의 오차를 대신 받던 값**이었다.
   *
   * **(3) 교차검증이 1.45를 한 번도 안 골랐다**(`_labFrontier.test.ts` (2)절).
   *   38겹 중 30겹이 1.1, 7겹이 1.0, 1겹이 1.2를 골랐다.
   *
   * 성적(정직한 LOO): MAPE 22.16% → **19.02%** · 중앙 15.9% → 15.5% ·
   * ±20% 58% → **63%** · ±30% 71% → **79%** · **최악 70.2% → 54.7%**
   * 📌 사용자가 짚은 "오차 범위가 너무 크다"에 직접 듣는 **유일한** 손잡이였다 —
   *    나머지 열한 개는 한 칸씩 훑어도 −0.8%p 이하이고, 다 같이 맞추면 중첩 교차검증에서
   *    오히려 나빠진다(24.16%). 38곳으로 손잡이 12개는 과적합이다.
   *
   * ⚠️ **이것도 성적으로 고른 게 아니다.** 근거는 (1)의 계절 패턴과 (2)의 함축배수이고,
   *    MAPE는 확인일 뿐이다. 표본이 늘면 (2)를 다시 재야 한다.
   * ⚠️ 이제 1이 아닌 배수는 **군부대 2.25 하나뿐**이다. 그건 자를 두 번 고쳐도 안 변했다.
   *
   * 📌 **끄니까 순환이 저절로 끊긴다** — 기준점 3곳이 전부 배수 1.00이 되어, 예외 규칙 없이
   *    A가 배수와 무관해진다. 기준점 3곳의 필요 점유율 벌어짐이 **1.416배 -> 1.019배**,
   *    100% 초과가 **6곳 -> 4곳**, 광주각화점이 121% -> 101%가 된다.
   *
   * ⚠️ **성적으로 고른 게 아니다.** 표본 안 MAPE는 21.59% -> 22.08%로 오히려 나빠진다.
   *    다만 **LOO는 22.17% -> 22.16%로 같다** — 1차 값의 표본 안 이득은 홀드아웃에서
   *    사라지는 과적합이었다. 근거는 위 재도출값이고, MAPE는 확인일 뿐이다.
   *
   * ⚠️ 사용자 지적(2026-09-20): *"걸거면 같이 걸어야 되는 거 아님? 산업, 기타는 안 걸고
   *    나머지는 걸고."* 한때 "독점 3곳에만 배수를 안 건다"는 안을 냈는데, 같은 산업단지인데
   *    독점이면 안 걸고 경쟁상권이면 거는 규칙이라 뜻이 안 선다. **유형으로 가른다.**
   *
   * 📌 **"기타"는 유형 이름도 아니었다** — 남악·발산역·송도·진주혁신도시본점·야탑을 묶어
   *    놓은 것인데 공통점을 설명할 수 없다. 물리적 뜻 없이 1.25배를 받고 있었다.
   *
   * ⚠️ **표본이 2곳이라 계수를 확정할 수준이 아니다.** 군부대도 방향만 믿는다.
   * ⚠️ 관광·유흥은 측정값이 0.84로 오히려 낮지만 n=2라 신뢰할 수 없어 **1.00으로 둔다.**
   *    V62도 근거 부족으로 제외한 유형이다(SPECIAL_DEMAND_TYPES_WITH_EVIDENCE).
   * ⚠️ 강도(없음/낮음/보통/높음)는 아직 안 쓴다. 표본 대부분이 "높음"이라 유형과 분리가 안 된다.
   *    사용자가 "같은 군부대라도 규모가 다르다"고 지적한 자리다(2026-09-20) — 규모 자료
   *    (병력·재학생·종사자 수)가 생기면 곱셈이 아니라 **덧셈**으로 넣는 게 맞다.
   * ⚠️ **특수수요가 100% 초과를 다 설명하지도 않는다.** 4곳 중 2곳(금촌역·문산)만 군부대이고,
   *    문경시청(197.7%)·양주덕정(124.6%)은 유형이 "없음"이다. 문경시청은 주거1km 18,786명으로
   *    표본 최소인데 가동률 30.6%가 나온다 — 인구 적은 동네를 과소평가하는 별개의 문제다.
   *
   * 사용자(2026-09-16): "지금 이 특수수요를 정하는게 정확하지않으니까, 경쟁력점수작업끝나면
   * 2차가공하는것도 좋을거같다. 1차는 우선 최대한 예측해서넣고" — 그래서 이건 **1차**다.
   *
   * ✅ **2026-09-23 저녁 — 다시 켰다(대학가 1.3 · 산업단지 1.4 · 군부대 2.0).** 근거는 DEFAULT 쪽 주석.
   *    껐던 이유(축척 순환)가 축척 고정으로 사라졌고, 이번엔 바깥 표본(경쟁점 핑봇)이 같은 방향을 가리켰다 —
   *    대학가 동네는 자사(−8.9%p)도 경쟁점(−3.3%p)도 과소예측됐다. 자사만 그랬다면 점유율 문제였을 것이다.
   */
  specialDemandMultipliers: Record<string, number>;
  /**
   * **강도 문** (2026-09-24 밤 신설, 사용자 결정) — 여기 적힌 유형은 `specialDemandIntensity`가 **"높음"일 때만** 배수를 곱한다.
   * 보통·낮음·없음·자료없음은 1.0.
   *
   * 왜: 오송 후보지가 AI 초안에서 "대학가/보통"으로 들어왔다. 약대·보건의료 행정타운·연구단지라 학부 대학가(전남대·청주대·
   * 부경대·울산대 — 재학생 수만 명, 원룸촌 배후지)와 같은 분류가 아닌데, 배수는 유형만 보고 ×1.3을 그대로 곱혔다(28.9 → 37.6%).
   * 자사 대학가 5곳은 전부 "높음"이라 문을 달아도 성적이 안 바뀌고(40곳 MAE 5.56 = 5.56), 후보지에서 오송만 막힌다
   * (`_labCandidate` (10)). 강도의 뜻은 `locationEvalAi.ts` 프롬프트와 입지평가 화면 도움말에 못 박았다.
   * 같은 날 더 늦게 산업단지·군부대도 문에 넣었다(`_labCandidate` (11)) — 산업단지는 높음 2곳 ×1.45 vs 보통 탕정역 ×0.82로 강도 방향이
   * 맞고, 군부대는 자사 2곳이 모두 높음이라 성적은 그대로인 안전문이다. 관광유흥은 배수 자체가 1.0이라 문과 무관.
   */
  specialDemandHighOnly: string[];
  /**
   * 배수를 **어느 항에** 곱하나 (2026-09-25 실험, 기본 없음 = 총수요). 기전대로라면 군부대·대학가·산업단지는 **주거 항**(주민등록 밖 사람이
   * 주거 통계에서 빠진 것)에, 관광유흥은 **유동 항**(밤 손님이 유동 집계에서 빠진 것)에 곱해야 한다. 총수요에 곱하면 유동이 큰 번화가 대학가
   * (부경대, 유동 14.7만 — 학생이 이미 유동에 있음)는 과대, 유동이 작은 대학가(전대후문·청주대)는 과소가 된다. `_labCandidate` (23).
   */
  specialDemandTerm?: Record<string, "resident" | "floating" | "total"> | null;
  /**
   * **점유율 환산을 켤지 끌지** (2026-09-16 밤 신설).
   *
   * 사용자 설계: "일단 전체가맹점을 점유율환산하는 값을 전부제거해서 0부터 시작한다음에,
   * 점유율이 얼마가 되어야 예상매출로갈수있는지 확인해볼수있도록하면 지금 수요의 문제점이
   * 있는지 2차검증을 해볼수있을것같다."
   *
   *   "formula" — 지금까지 동작. 점유율 = (자사PC x 격차^γ) / (자사PC x 격차^γ + 경쟁IP + 안가는몫)
   *   "off"     — 점유율을 1로 둔다. 경쟁 항을 통째로 들어낸 상태다.
   *
   * `off`로 두면 예측 가동률이 "경쟁이 없다면 이만큼"이 되고, 화면의 **필요 점유율**
   * (= 실측가동률 ÷ 그 예측)이 **이 매장이 실제로 먹은 몫**이 된다. 수요식이 맞다면 그 값이
   * 0~1 안에 들고 경쟁이 셀수록 낮아야 한다. 1을 넘으면 그 동네 수요를 과소평가한 것이다.
   *
   * 왜 이게 필요한가: 2026-09-16 점검에서 지금 점유율 환산이 **아무것도 설명하지 못한다**는
   * 게 드러났다(경쟁력격차 r=0.025, 사양 -0.062, 입지 0.075~0.220 — 전부 유의선 0.371 미달).
   * 게다가 수준도 안 맞는다(자사PC/(자사PC+경쟁IP) 중앙 0.186 vs 실측 0.519). 근거는
   * `_shareAudit.test.ts`에 있다. 그래서 경쟁 항을 고치기 전에 수요식부터 다시 봤다.
   *
   *   "quality" — **2026-09-17 신설, 새 기본값.** 유효거리 안의 경쟁점만 품질로 겨룬다.
   *               위 점검이 남긴 빈칸(상권 겹침)을 메운 것이다. 홀드아웃·대조군 결과는
   *               `_competitionDesign.test.ts` 머리말에 있다.
   */
  shareMode: "formula" | "off" | "quality";
  /**
   * **유효거리 R(m)** — 이 안의 경쟁점만 진짜 경쟁자로 센다 (shareMode="quality"에서만).
   *
   * 2026-09-16 사용자 실무 의견: "거리는 유효거리안에있으면 거리가 크게작용안한다는게
   * 실무의견이고, 매장의 품질이 (점수)가 작용되어야함". 자료도 이쪽을 지지한다 —
   * 지수 감쇠(최선 r=0.510)보다 **계단**(r=0.560)이 낫다.
   *
   * ⚠️ **300m은 잠정값이다.** 300·400·500m가 MAPE로 30.1~30.5%라 구별이 안 된다.
   *    300을 고른 근거는 상관(0.560 vs 400m 0.434)과 실무 의견뿐이다. 표본이 늘면 여기부터
   *    다시 본다. 근거: `_competitionDesign.test.ts` 검정 2·5.
   */
  effectiveRadiusM: number;
  /**
   * **품질 지수 θ** — 품질 차이를 얼마나 세게 볼지 (shareMode="quality"에서만).
   *
   * θ=3은 두 경로가 독립으로 만난 값이다. 자료로 고른 최선이 3이고, 사용자가 매출 변화폭에서
   * 역산한 값이 2.74~4.92·중앙 3.25다. **감각은 매출액, 측정은 가동률 — 경로가 완전히 다르다.**
   *
   * ⚠️ θ가 하는 일의 상당 부분은 **수준 보정**이다. 자사/경쟁 품질비가 1.75인데 1.75³=5.36이고,
   *    이는 최적 상수배율 λ=5와 같은 값이다. "θ가 오차를 줄였다"고 읽으면 안 된다 —
   *    θ가 줄인 건 수준이고, 남긴 건 **순서**다(r 0.223 → 0.560).
   *
   * ✅ **2026-09-23 — 3 → 1.75.** 위 θ=3은 **자사 40곳 안에서** 고른 값이었다. 경쟁점 핑봇 47곳을
   *    바깥 표본으로 세우자 θ=3은 자사우위를 3.50배로 예측했는데 실측은 1.74배(1년차 자사 vs 성숙
   *    경쟁점, n=9)였다 — 과적합. θ=2에서 경쟁점끼리 짝 과장이 0.97로 딱 맞고, 자사엔 후함이 한 겹
   *    더 있어 존구성 비중 축소와 묶었다. θ 혼자 내리면 자사·경쟁점이 같이 −10%p 과소예측되므로
   *    **1km 밖 고리(residentRingDecayM)와 한 묶음**이다. 재고 표: `_bundleCandidate.test.ts`.
   *    ⚠️ θ1.75와 θ2는 사전 기준으로 못 가른다(자사우위 1.77 vs 1.92·1.82). 1.75는 존구성 0.06과 짝일 때 1위.
   */
  qualityExponent: number;
  /**
   * **경쟁력점수 비중** — 사양·먹거리·존구성·인테리어·관리 (shareMode="quality"에서만).
   *
   * 합이 1이 아니어도 된다. 있는 항목만 모아 그 합으로 나눠 쓴다(결측 항목은 자동 제외).
   *
   * 기본값은 **실무 감각 비중**이다. 2026-09-16 사용자 지적대로 기존 비중은 적중률 튜닝
   * 산물이고("지금 적용한값을 적중률 맞출려고 조정하다보니 저래된고고"), 교과서식은 회귀로
   * 덮지 않는 게 목적이다. 정확도 비용도 없다 — 세 벌을 돌려도 r이 0.553~0.561로 차이 0.008이다.
   *
   * ⚠️ **입지는 여기 없다.** 사용자 방향대로 항목·평가값·개념을 전부 다시 만든 뒤 넣는다.
   *    실무 감각 비중에서 입지 몫은 17.0%였고, 나머지 넷이 그 몫을 나눠 갖고 있는 상태다.
   *
   * ✅ **2026-09-23 — 존구성 0.238 → 0.06 (항목은 남긴다).** 바깥 표본(경쟁점 47곳)이 존구성을 어디서도
   *    못 보고(r −0.07, 다만 큰룸 경쟁점 11곳이라 2SE 54% — 판정력이 없다), 자사 존 3.19 vs 경쟁점 1.39
   *    (2.3배)라 이 비중이면 자사우위가 3.50배로 부푼다(실측 1.74). 사용자 질문 *"빼는 게 정답임?"*에
   *    비중 0 / .06 / .12 / .18 / .238을 돌렸다: 비중을 남길수록 자사 r·짝 r이 오르고(순서에는 일한다),
   *    세게 두면 우위를 부풀린다. 사전 기준(두 편향 ±3%p → 자사우위 1.74 근접 → 자사 r) 1위가 **0.06**.
   *    팀룸 유입 기전(사용자)은 살아 있으니 항목을 자르지 않는다([[feedback_item_vs_coefficient]]).
   *    ⚠️ 0.06과 0.12는 못 가른다(자사우위 1.77 vs 1.86). 표본이 늘면 다시 잰다.
   *
   * ⚠️ **관리(management)는 실험실에서 QSC로 채운다**(2026-09-17). 운영 V62는 전 매장 4.00
   *    고정이지만, 실험실은 본사 점검 점수를 1~5로 환산해 넣는다(3.22~5.00 · 평균 4.25).
   *    환산자와 근거는 `labInput.ts`의 `qscToManagementScore`에 있다. 이 파일은 안 바뀌었다 —
   *    관리는 원래 여기 있던 칸이고, 그 칸에 들어오는 **값**이 상수에서 실측으로 바뀐 것이다.
   */
  qualityWeights: { spec: number; food: number; zone: number; interior: number; management: number };
  /**
   * **입지 — 점유율에 곱하는 독립 항들** (2026-09-17 전면 재설계).
   *
   * ── 왜 경쟁력점수 안이 아니라 밖인가 ────────────────────────────────
   * 입지를 품질에 섞으면 **자사÷경쟁의 나눗셈**이 된다. 그런데 접근성은 "경쟁점보다 낮은
   * 층인가"가 아니라 **"올라오기 얼마나 번거로운가"**다 — 6층 매장은 경쟁점이 없어도 덜 온다.
   * 손님이 경쟁점으로 가는 게 아니라 **아예 안 온다.** 그래서 비율이 아니라 곱셈이다.
   * 실측도 같은 말을 한다: 품질에 섞으면 r이 0.554 -> 0.565로 0.011밖에 안 오르는데,
   * 독립 곱셈 항으로 두면 0.554 -> 0.725로 오른다. 근거는 `_locationRebuild.test.ts` ③④.
   *
   * ── 기존 주관 3항목은 폐기했다 ─────────────────────────────────────
   * 상권위치·동선 / 선점경쟁 / 접근가시성은 1~5점인데 **실제로 3~4개 값만 쓰였고**
   * (상권위치·동선은 29곳 중 19곳이 5점) 셋 다 유의선 0.371 미달이었다(0.075 / 0.220 / 0.181).
   * 층수에서 기계적으로 나온 값이 0.389로 유일하게 유의했다. 41곳에 매겨둔 평가값도 안 쓴다.
   *
   * ── 계수 출처 라벨 ────────────────────────────────────────────────
   * 사용자 방침(2026-09-17): **항목은 뜻으로 정하고, 계수만 자료로 정한다.**
   * 검정은 "이 항목이 중요한가"가 아니라 "자료에서 주워온 숫자를 믿어도 되나"를 묻는 것이라,
   * 감각으로 정한 값에는 필요 없다. 표본이 늘면 `[보류]`를 `[감각]`이나 `[자료]`로 올린다.
   */
  locationExponents: {
    /**
     * `[자료]` **상권 중심도** ν — 관문을 전부 통과했다. 지금까지 중 제일 강하다.
     *   중심도 = (유동 300m ÷ 유동 1km) x (1000/300)²  — 1보다 크면 문 앞이 상권 평균보다 빽빽
     *   LOO 24.52% -> 25.50%(벌어짐 0.98%p) · 5겹 78% · 대조군 MAPE p=0.002 · r p=0.012
     * ⚠️ 300m/500m 짝은 쓰지 않는다 — 순서는 더 잘 맞히지만(r 0.801) MAPE 홀드아웃에서
     *    6.65%p 벌어져 gamma(7%p)와 비슷한 징후를 보인다.
     */
    centrality: number;
    /**
     * `[자료]` **접근성(층수)** κ — 켠다. 한때 기준에 따라 갈렸는데 **원인이 기준값이었다.**
     *
     * 중앙값을 기준으로 쓸 때는 MAPE 기준이 κ=0을, r 기준이 κ=0.25를 골라 갈렸다.
     * 층수 항이 편향을 키웠고(-5.5% -> -10.0%) MAPE가 편향을 포함하기 때문이다.
     * **기준값을 기하평균으로 바꾸니 두 기준이 같은 답을 낸다**(아래 locationReferences 참고):
     *   κ=0     편향 -8.3%  MAPE 24.39%  r 0.693
     *   **κ=0.25 편향 -9.2%  MAPE 24.08%  r 0.736**  <- 두 기준 모두 최선
     *   κ=0.5   편향 -9.4%  MAPE 26.13%  r 0.727
     * LOO도 κ=0.25를 93%(MAPE)·86%(r) 고르고 벌어짐은 1.79%p다.
     */
    access: number;
    /**
     * `[보류]` **유동 방향(편심도)** ω — 기본 0. 자료는 모았고 **부호도 맞다**(음수).
     * 다만 중심도가 이미 먹고 있다 — 잔차 상관이 -0.280 -> (중심도·층수 반영 후) -0.069로
     * 사라지고, 편심도 ↔ 중심도가 r=-0.335로 겹친다. 대조군도 p=1.000 / 0.216으로 미달.
     * **항목을 버린 게 아니라 자료가 계수를 못 정한 것이다.** 사용자가 정하면 그날 켜진다.
     */
    direction: number;
    /**
     * `[보류]` **동선 방해** — 기본 0. *"유동이 몰리는 쪽에 경쟁점이 있으면 그게 우리 동선을
     * 막는다"*(사용자 착상 2026-09-20). 항목의 뜻은 멀쩡하다.
     *
     * ⚠️ 옛 보류 사유는 **"경쟁점 좌표가 22%뿐이라 방위를 못 잰다"**였는데 **그 사유는
     *    2026-09-20에 해소됐다 — 좌표가 229/232곳(98.7%)이다.** 그래서 처음으로 실제로 재봤다.
     *
     * **재봤더니 신호가 없다** (`_rivalDirection.test.ts`, 경쟁상권 33곳):
     *   공식 넷(최근접·거리가중·붐비는방위 0/1·붐비는쪽 경쟁PC비율) 전부 상관이 0 근처였고,
     *   **쏠림이 뚜렷한 상권만 추리면 부호가 오히려 뒤집혔다**(전체 -0.24 -> 상위절반 +0.15~+0.33).
     *   신호라면 방향이 뜻을 갖는 상권에서 더 강해져야 하는데 반대다 = 잡음의 모양이다.
     *
     * ⚠️ 다만 **"착상이 틀렸다"가 아니라 "이 자료로는 안 잡힌다"**로 읽어야 한다. 한계가 셋:
     *   1. 방향 자료가 거칠다 — `kakao-directional.json`이 반경 300m·격자 300m라 방위당 한 칸이다.
     *   2. 300m 바깥에서 오는 통행을 아예 안 본다.
     *   3. 실험실 자체 MAPE가 22.5%라 n=33에서 어지간한 효과는 묻힌다.
     *   더 고운 방향 자료(격자 50~100m)나 실제 보행 동선(도로망)이 생기면 다시 열 자리다.
     */
    flowBlock: number;
    /** `[보류]` **간판·출입구 가시성** — 기본 0. 로드뷰 평가가 필요하고, 그때는 1~5점이 아니라
     * 예/아니오 사실 질문으로 물어야 한다(지금 3항목이 그래서 망가졌다). 경쟁점 좌표부터. */
    visibility: number;
  };
  /**
   * **입지 지표의 기준값** — 이 값에서 1배가 된다. **기하평균으로 잡는다(중앙값이 아니다).**
   *
   * 곱셈 보정 (x/기준)^지수는 표본 전체에서 **평균적으로 1배**여야 중립이다. 그러려면 기준이
   * x의 **기하평균**(로그 공간의 중심)이어야 한다. 중앙값을 쓰면 분포가 한쪽으로 꼬리를 끌 때
   * 중립이 깨진다 — 중심도는 0.37~6.95로 오른쪽 꼬리가 길다.
   *
   * ⚠️ **이건 자유계수가 아니라 정규화다.** 자료에 맞춰 고르는 값이 아니라 "보정이 평균적으로
   *    아무 일도 안 하게" 만드는 유일한 값이다. 그래서 검정 대상이 아니다.
   *
   * 2026-09-17에 중앙값(3.95 / 4)을 쓰다가 기하평균(3.22 / 3.42)으로 고쳤다. 그 전에는
   * 편향이 -12.8%였고 MAPE 기준과 r 기준이 서로 다른 κ를 골랐다. 고친 뒤엔 편향 -9.2%,
   * 두 기준이 같은 답(κ=0.25·ν=0.25)을 낸다. **수준을 자유계수 λ로 푸는 것보다 낫다** —
   * λ를 풀면 표본 안 MAPE는 비슷한데(24.04%) LOO 벌어짐이 1.79%p -> 6.45%p로 커진다.
   */
  locationReferences: { centrality: number; access: number };
  /**
   * **중심도 잔차화** — 중심도에서 *동네 규모* 몫을 걷어낸다 (2026-09-20 채택).
   *
   * ── 왜 필요한가 (이중계산) ────────────────────────────────────────────────
   * 중심도의 뜻은 *"동네 규모를 감안했을 때 우리가 상권 중심이냐 끝이냐"*다. 그런데 날값에는
   * **동네 규모 자체가 섞여 있다** — 수요식이 쓰는 유동400m와 r=0.599다. 그래서 수요에서
   * 이미 센 것을 점유율에서 한 번 더 곱했다. log(중심도)를 log(유동400m)에 회귀시켜
   * **잔차만** 쓰면 남는 게 원래 재려던 그것이다. 겹침이 0.599 -> **−0.154**로 사라진다.
   *
   *     잔차화 중심도 = 기준 x (중심도 / G_c) x (G_f / 유동400m)^기울기
   *
   * ── 근거 (`_residualCentralityPort.test.ts`) ──────────────────────────────
   *   정직한 홀드아웃(축척·기울기를 **훈련겹에서만** 맞춤, n=38)
   *     손 안 댄 중심도 ν=0.25 (옛 본체)   24.40%
   *     잔차화 ν=0.5                      **22.12%**   <- 2.28%p 좋아짐
   *   겹마다 기울기를 다시 적합해도 유지된다(기울기 고정 22.16% vs 재적합 22.12%) —
   *   즉 예전 하네스가 적어 둔 '벌어짐 −0.05%p'는 누수가 아니었다.
   *   무작위 대조군 200회 p=0.005 · 훈련겹 38회 중 37회가 ν=0.5를 골랐다(`_textbookFull`).
   *
   *   ⚠️ 채택 당시(2026-09-20 오전)에는 23.15% -> 22.02%로 쟀다. 같은 날 오후에 특수수요
   *      배수를 고치면서 **기준선이 통째로 옮겨져** 위 숫자가 됐다. 폭이 커졌을 뿐 결론은
   *      같다. 숫자를 인용할 때는 어느 시점 기준인지 같이 볼 것.
   *
   * ⚠️ **이 셋은 표본에서 적합한 상수다 — 자유계수가 아니라 정규화다.** MAPE로 고르지
   *    않았다(OLS 기울기와 기하평균이라 고를 여지 자체가 없다). locationReferences와 같은
   *    부류다. 다만 **표본이 늘면 다시 적합해야 한다** — `_residualCentralityPort.test.ts`의
   *    (1)번 시험이 지금 표본의 값을 찍어 준다.
   *
   * ⚠️ 인계 문서(2026-09-21)에 *"새로 고를 계수가 없다"*고 적혀 있었는데 **정확하지 않다.**
   *    굳혀야 하는 상수가 셋이고 ν도 0.25 -> 0.5로 바뀐다. 옮겨도 되는 이유는 "고를 게
   *    없어서"가 아니라 **고른 값이 정직한 홀드아웃에서 버텨서**다.
   *
   * ⚠️ **유동400m가 없으면 중심도 항을 통째로 뺀다**(1배). 날값으로 되돌리지 않는다 —
   *    ν=0.5는 잔차 눈금에 맞춘 값이라 날값에 그대로 먹이면 딴 걸 재게 된다.
   *
   * null로 두면 잔차화를 끄고 날 중심도를 쓴다(옛 동작).
   */
  centralityResidual: {
    /** log(중심도) ~ log(유동400m) OLS 기울기. */
    slope: number;
    /** 중심도의 기하평균 G_c. */
    geoMeanCentrality: number;
    /** 유동400m의 기하평균 G_f. */
    geoMeanFloating400: number;
  } | null;
  /**
   * **경쟁점 거리 감쇠** — `effectiveRadiusM`의 계단 함수를 대신한다 (2026-09-20 신설).
   *
   * ── 왜 필요한가 ───────────────────────────────────────────────────────────
   * 지금은 **300m 안이면 100%, 밖이면 0%**로 자른다. 임의의 자이고, 지방에서 깨진다:
   *   문경시청점 — 373m 콤마PC가 **0으로** 세진다(23m 차이). 1.5km의 4곳도 0이다.
   *              그래서 산식상 완전 독점인데 실제로는 시내 PC방들과 나눠 먹는다.
   * Huff 모형의 원형은 계단이 아니라 **거리에 따라 완만히 줄어드는 곡선**이다.
   *
   *     무게 = 1                                   (거리 ≤ plateauM)
   *          = exp(−(거리 − plateauM) ÷ scaleM)      (그 밖)
   *
   * **평지 + 감쇠**다. plateauM 안쪽은 지금과 **똑같이** 100%로 세고, 그 밖만 완만히 줄인다.
   *
   * ⚠️ 첫 설계(평지 없이 exp(−d/scaleM))는 **근거리 경쟁점 무게까지 바꿔서** 300m 밖에
   *    경쟁점이 하나도 없는 도시 매장 23곳도 평균 7.68%p나 움직였다(2026-09-20 측정).
   *    그건 "거리를 반영했다"가 아니라 "경쟁을 통째로 줄였다"이고, 실제로 무작위 대조군을
   *    못 넘었다. 평지를 두면 **300m 안이 전부인 매장은 정확히 0 움직인다** — 가설을
   *    제대로 겨냥하게 된다.
   *
   * ⚠️ **계단 동작을 포함한다(중첩 모형).** plateauM = effectiveRadiusM이고 scaleM → 0이면
   *    계단 함수와 정확히 같다. 그래서 "계단 vs 감쇠"를 공정하게 견줄 수 있다.
   *    ⚠️ 2026-09-21 저녁부터 plateauM은 **200**이다(effectiveRadiusM 300과 다르다) —
   *       가동률 기준으로 다시 골랐다. 자세한 경위는 DEFAULT_TEXTBOOK_PARAMS 쪽 주석에 있다.
   *
   * ⚠️ 거리를 모르는 경쟁점은 **1배(가장 가까운 것처럼)** 센다. 지금 계단 규칙이 "모르면
   *    유효거리 안으로 본다"인 것과 같은 방향이다 — 빼는 쪽이 아니라 세는 쪽이 보수적이다.
   *
   * ── 📜 2026-09-20 측정 결과 — 그날은 **기각**이었다 (`_rivalDecay.test.ts`) ───────
   * ⚠️ 아래는 **지나간 기록이다.** 2026-09-21에 총량 정규화(weightFactor)를 넣고 다시 재서
   *    채택했다. 그때 기각된 이유가 정확히 "총량을 안 묶었다"였다는 게 아래에 적혀 있다.
   * 평지를 둔 뒤 확인 기준 1은 통과했다(300m 밖 경쟁점이 없는 23곳은 **정확히 0.00%p**
   * 움직인다). 그런데 **모든 scaleM에서 성적이 단조롭게 나빠진다**:
   *
   *     계단300     MAPE 21.59%   LOO 22.17%   경쟁상권 잔차 −16.9%
   *     감쇠 100m   MAPE 23.36%   LOO 24.33%   경쟁상권 잔차 −19.5%
   *     감쇠 300m   MAPE 24.27%   LOO 24.85%   경쟁상권 잔차 −21.1%
   *     감쇠 500m   MAPE 24.52%   LOO 25.09%   경쟁상권 잔차 −21.6%
   *
   * 기전이 분명하다 — 먼 경쟁점을 **새로 세면** 경쟁무게가 늘어 점유율이 내려가는데,
   * 경쟁상권은 **이미 과소예측**이라 더 나빠진다. 대조군도 미달(p=0.408).
   *
   * 📌 이 결과는 `_worstErrors.test.ts` (4)절과 **같은 방향을 가리킨다** — 과소예측 8곳은
   *    경쟁무게를 지금의 **0.38배로 줄여야** 맞는다. 즉 **경쟁을 더 세는 방향은 전부 틀린다.**
   *    "문경의 373m 경쟁점이 안 세진다"는 관찰은 맞았지만, 그걸 세면 더 나빠진다 —
   *    문경의 병목은 경쟁이 아니라 수요다(필요 점유율 229%).
   *
   * 다시 열 자리: 경쟁무게를 **전반적으로 줄이는** 손잡이를 먼저 찾은 뒤, 그 위에서 거리를
   * 다시 본다. 순서가 반대였다.
   *
   * null이면 계단 동작(`effectiveRadiusM`)을 쓴다. **2026-09-21 채택 이후 기본값은 null이
   * 아니다** — `DEFAULT_TEXTBOOK_PARAMS`를 볼 것. null은 "계단으로 되돌리기"에 쓴다.
   */
  /**
   * ⚠️ `weightFactor` — **총량 정규화 상수** (2026-09-21 추가). 자의적 계수가 아니다.
   *
   * 감쇠를 켜면 plateauM 밖 경쟁점이 새로 세져 **총 경쟁량이 늘어난다**. 그런데 축척
   * (hoursPerUserPerMonth)은 **독점매장(경쟁 0)에서만** 맞추므로 그 증가를 못 흡수하고,
   * 전 매장이 통째로 아래로 밀린다. 2026-09-20에 이 설계가 실패한 이유가 바로 그것이다
   * (`_rivalDecay` 대조군 p=0.995 — "거리를 반영했다"가 아니라 "경쟁을 늘렸다"였다).
   *
   * 그래서 **계단과 같은 총 경쟁량을 유지하는 전역 상수 하나**를 곱한다:
   *     weightFactor = Σ(ip x 계단무게) ÷ Σ(ip x 감쇠무게)      기존점 38곳에서 잰 값
   * 이 정규화를 넣어야 수준이 약분되고 **거리 구조만** 남는다. 실제로 그렇게 재서
   * 매출 최악 오차가 54.7% -> 51.9%로 줄었다(`_rivalDecayNormalized`, R300 λ150 시절).
   * 빼면 그 개선이 사라진다.
   *
   * ⚠️ **(R, λ)마다 값이 다르다. 바꾸면 반드시 다시 잰다.** 0.90은 R300 λ150 전용이었고
   *    지금(R200 λ200)은 0.9593이다. 옛 값을 그대로 물려 쓰면 총량이 안 맞아서, 재려던
   *    "거리 구조"가 아니라 **총량 차이**를 재게 된다(2026-09-21에 한 번 그럴 뻔했다).
   *    재는 식은 위 그대로이고, `_decayUnderUtilization`·`_decayShapeSelection`이 찍어 준다.
   * ⚠️ 표본이 늘면 다시 잰다 — 38곳에서 나온 값이다.
   */
  rivalDistanceDecay: { plateauM: number; scaleM: number; weightFactor?: number } | null;
  /**
   * **지수 눈금 보정** (2026-09-21 신설 → **같은 날 밤 껐다. 지금 기본값은 null이다**).
   * 가동률 = A x (수요÷총공급)^b x 입지^c 의 b·c다.
   *
   * ⛔ **왜 껐나 — 기전이 아니라서다.** 사용자: *"이건 근거없는 단순보정 아닐까?"*
   *    b<1이라는 **사실**은 측정됐지만(38겹 전부 1을 거부), 그건 "출력이 2배 넓다"는
   *    증상이지 원인이 아니다. 그리고 채택값 0.45는 자료가 고른 값도 아니었다
   *    (자료는 b를 못 고른다 — 중첩 LOO가 훈련평균과 같다). 누르는 대신 **어디서 넓어지는지**
   *    찾기로 했다. 그날 밤에는 수준 오차를 `outsideOptionIp` 20대가 받게 뒀는데,
   *    **2026-09-22에 그것도 뺐다**(점 3개 적합이었고 정체가 반경 밖 경쟁점이다 —
   *    `outsideOptionIp` 주석). 그래서 지금은 **수준 오차 +2.69%p가 드러나 있고**,
   *    그게 "2km 경쟁점을 아직 안 세고 있다"는 표시등이다.
   *    남은 건 그 수준 오차와 **퍼짐 1.73배 과장**이고, 둘 다 2km 판정 뒤에 다시 잰다.
   *    ⚠️ 아래 성적표는 **켜 놓고 잰 값**이다. 되살릴 때 참고하라고 남겨 둔다.
   *
   * ── 왜 필요한가 ───────────────────────────────────────────────────────────
   * 지금까지 산식은 **b=1 · c=1을 강제**했다. Huff 모형의 교과서 형태를 그대로 쓴 것이고,
   * 자료로 고른 적이 한 번도 없다. 판정 기준을 가동률로 바꾸고(2026-09-21) 물어보니
   * 자료가 **1을 명확히 거부**한다:
   *
   *     b = 0.327   (38겹 재적합 0.284~0.371 · SD 0.017 · 1을 안 품는다)
   *     c = 0.169   (38겹 재적합 0.099~0.254 · SD 0.028 · 1을 안 품는다)
   *
   * ── 실무 뜻 — "값에 맞춘 것"이 아니다 ─────────────────────────────────────
   * 사용자 정리(2026-09-21): *"수요 > 가동률 이 산식이 현재 검증 가능한 부분이 없으니까
   * 값에 맞추는 형태로 하는 게 최선이라고 보긴 함."*
   *
   * 맞는 구분이다. **수요도 공급도 우리가 만든 지수**이고 실측할 방법이 없다 —
   *   수요   = 인구 x 연령별이용률 x 1인월시간 x 특수수요배수
   *   총공급 = 자사PC + Σ(경쟁PC x 품질비^θ x 거리무게)   ← 날PC수는 자사 것뿐이다
   * 절대 크기에도 매장 간 비율에도 검증할 근거가 없다. **눈금 없는 막대**다.
   * 그러면 눈금은 실측 가동률에 맞춰 새길 수밖에 없고, 그게 b·c다.
   * b<1은 **"그 지수가 매장 간 차이를 과장한다"**는 뜻이다(실측보다 2.3배 넓었다).
   *
   * ⚠️ 내가 한 번 틀린 길 — "공급은 PC를 셀 수 있으니 −1로 고정하고 수요만 보정하자"고
   *    했는데 **더 나빠진다**(MAE 6.57%p · 최악 105%). 총공급도 품질비 3제곱을 먹인
   *    구성 지수라서, 수요·공급을 **같은 만큼 눌러 비로 묶어야** 한다(p = −q).
   *
   * ── 성적 (기존점 33곳 · LOO · 점유율 상한 걸린 5곳 제외) ──────────────────
   *                      MAE      SD      최악(%p)   ±5%p
   *     지금(b=c=1)      6.26%p  5.26%p   16.6%p     16/33
   *     전부 훈련평균      4.41%p  4.03%p   —          22/33
   *     b=.327 c=.169    4.43%p  3.06%p   13.6%p     23/33
   *
   * ⚠️ **상대% 자로 보면 최악이 59.8 → 69.8%로 나빠 보인다.** 증평점 하나 때문이다
   *    (실측 17.6%로 표본 최저 · %p로는 10.5 → 12.3%p로 1.8%p 차이인데 분모가 작아 튄다).
   *    같은 자(%p)로 보면 최악도 16.6 → 13.6%p로 좋아진다.
   *
   * ── 구현 ──────────────────────────────────────────────────────────────────
   *   share  = min(1, 날점유율 x 입지배율^(c÷b))      ← 상한은 그대로 둔다
   *   가동률 = 기준가동률^(1−b) x (수요 x share ÷ 용량)^b
   * 입지 지수를 c÷b로 넣는 이유: 뒤에서 전체를 b제곱하므로 **최종 지수가 c**가 된다.
   * `referenceUtilization`은 **닻**이다 — 이 값에서는 예측이 안 움직인다. 기존점 실측
   * 가동률의 기하평균이라 hoursPerUserPerMonth가 뜻을 유지한다(크기는 축척이 흡수한다).
   *
   * ── ⭐ 2026-09-21 저녁 — b를 0.327에서 **0.45로 올렸다**(사용자 채택) ─────────
   * 사용자 지적: *"현재 점유율이 높게 나타나고있어 변별력이 떨어지니 이거 조정필요."*
   * 재 보니 범인은 점유율이 아니라 **이 b였다.** 점유율 자체는 변별을 하고 있다 —
   * '필요 점유율'(실측가동률 ÷ 수요식)과 로그상관 0.810, 무작위 대조군 1,000회 p=0.000,
   * 수요밀도를 빼고도 편상관 0.469(p=0.002). 오히려 산식 점유율 퍼짐(4.41배)이
   * 필요(7.08배)보다 **좁다**. 후보지가 21~34%로 몰려 보이는 건 b의 수축 때문이다.
   *
   * ⚠️ **자료는 b를 못 고른다.** 중첩 LOO가 훈련평균과 같다(`_calibrationSplit` 2절).
   *    그래서 이건 자료가 아니라 **용도로 고른 값**이다. 세 잣대를 같이 놓고 골랐다:
   *
   *      b        가동률MAE  가동률최악  매출MAPE  매출최악  ±20%  후보지 간격
   *      0.327     3.83%p    13.16%p    16.11%    56.9%    27곳    1.62배
   *      **0.45**  4.18%p    11.91%p    17.18%    51.8%    24곳    2.03배
   *      0.55      4.67%p    10.91%p    18.98%    55.2%    23곳    2.44배
   *
   *    0.45를 고른 이유: **가동률 최악과 매출 최악이 둘 다 준다**(매출 최악은 여기가 최소다).
   *    0.55는 가동률 퍼짐이 실측과 정확히 맞지만(퍼짐비 1.00) 매출에서 ±20% 적중이
   *    4곳 빠진다. 결재에 나가는 숫자가 매출이라 그 값이 너무 비싸다.
   *    측정: `_spreadChoice.test.ts`(b 격자·매출 환산) · `_shareDiscrimination.test.ts`.
   * ⚠️ c=0.169는 **안 바꿨다.** 최종 입지 지수가 c라서 b와 무관하다(c÷b로 넣고 b제곱).
   * ⚠️ b는 순위를 거의 안 바꾼다 — 바뀌는 건 **간격**이다(후보지 스피어만 0.989).
   *    완전히 1이 아닌 이유는 점유율 100% 상한이다(지수가 c/b로 들어가 잘리는 자리가 달라진다).
   *
   * ⚠️ **null이면 지금까지 동작과 정확히 같다**(중첩 모형). b=c=1도 마찬가지다.
   * ⚠️ 38곳에서 잰 값이다. **표본이 늘면 다시 잰다.**
   * 근거: `_demandSupplyExponents.test.ts` · `_ratioExponent.test.ts` · `_spreadChoice.test.ts`
   */
  indexCalibration: {
    /** (수요 ÷ 총공급)에 걸리는 지수. 1이면 지금 동작. */
    ratioExponent: number;
    /** 입지배율에 걸리는 **최종** 지수. 1이면 지금 동작. */
    locationExponent: number;
    /** 닻 — 이 가동률에서는 예측이 안 움직인다. 기존점 실측 기하평균. */
    referenceUtilization: number;
  } | null;
  /** 가동률 물리적 상한. 이 위로는 좌석이 모자라 못 받는다. */
  maxUtilization: number;
};

/** 경쟁력점수를 이루는 다섯 항목. 없는 항목은 null로 두면 비중에서 자동으로 빠진다. */
export type QualityParts = {
  spec: number | null;
  food: number | null;
  zone: number | null;
  interior: number | null;
  management: number | null;
};

/**
 * 경쟁력점수 — 있는 항목만 비중대로 섞는다. 자사·경쟁점에 **같은 함수**를 쓴다.
 *
 * ⚠️ 구성 점수(사양·존구성)는 저장 필드가 아니라 **파생값**이다. 원자료에서
 *    `computeSpecScore` / `computeOwnZoneComposition` / `computeCompetitorZoneComposition`으로
 *    계산해서 넣어야 한다. 2026-09-16에 이걸 "결측"으로 읽어 한 번 틀렸다.
 */
export function computeQualityScore(parts: QualityParts, w: TextbookParams["qualityWeights"]): number | null {
  const items: [number | null, number][] = [
    [parts.spec, w.spec], [parts.food, w.food], [parts.zone, w.zone],
    [parts.interior, w.interior], [parts.management, w.management],
  ];
  let num = 0, den = 0;
  for (const [v, weight] of items) {
    if (v == null || !Number.isFinite(v) || !(weight > 0)) continue;
    num += v * weight; den += weight;
  }
  return den > 0 ? num / den : null;
}

/**
 * 2026-09-16 확정본. 근거는 `docs/handoff-20260916-evening.md` 2절.
 *
 *   유동 400m  — 500m는 수원망포점처럼 지하철 상권을 먹는다(300→400m에서 4.19배 점프).
 *                100~300m는 너무 좁아 오차가 커진다. 독점 5% 체를 통과한 조합이 전부 400m 계열이었다.
 *   주거 1km   — 100~500m도 전부 시험했으나 1km가 이겼다. PC방은 평균 3시간·월 3.7회 오는
 *                목적지형이라 도보 10분권이 상권이라는 뜻이다.
 *   유동 x0.15 — **측정값이 아니라 보정상수다.** 한 숫자가 셋을 떠맡는다: 단위 변환(주거는 "명",
 *                유동은 "하루 통행량"), 중복 제거(거주자 통행이 양쪽에 두 번), 방문객의 낮은 이용
 *                성향. 그래서 "유동인구의 15%가 수요"라고 읽으면 안 된다.
 *   격차^4     — 경쟁력 쏠림. 우위가 있으면 한쪽으로 쏠린다는 현장 관찰을 지수로 옮긴 것이고,
 *                무작위 대조군 500회에서 p=0.004로 통과했다(가동률 MAPE 46.25% → 30.59%).
 *   상한 0.55  — 실측 월평균 가동률 최대가 46.5%(전대후문점), 월 최대의 최대가 52.0%다.
 */
export const DEFAULT_TEXTBOOK_PARAMS: TextbookParams = {
  // 주거 1km · 유동 400m는 초기 평가에서 **좁혀 잡은 값**이다(사용자, 2026-09-24 회고). 전대상대점은 전대후문점과
  // 배후지를 나눠 쓰는데 사람은 후문 쪽에 몰려 있어 상대점 상권을 넓히면 값이 안 맞았고, 수원망포점은 500m쯤에
  // 역이 있어 그 유동이 딸려 들어와 반경을 줄였다. 1km 밖 고리(residentRingDecayM)를 2026-09-23에 켰다가 09-24에
  // 끈 것도 같은 결의 문제였다 — 넓히면 경쟁점 적은 자리가 한결같이 오른다.
  residentRadius: 1000,
  floatingRadius: 400,
  useResidentAgeWeights: true,
  useFloatingAgeWeights: true,
  floatingFactor: 0.15,
  // ✅ **2026-09-23 채택 — 600m.** θ1.75·존구성 0.06과 한 묶음(타입 주석). 재고 표: `_bundleCandidate.test.ts`.
  //    주거 이용자가 1km만 셀 때의 중앙 1.64배가 된다(범위 1.22~2.52). 고리 인구 없는 매장은 1km만 센다.
  // ✅ 2026-09-23 저녁 — 600 → **800**. 고리 점유율을 "gravity"로 바꾸면 고리 몫이 줄어 수준이 내려가므로,
  //    사전 기준(두 편향 ±3%p)으로 λ를 다시 골랐다. **눈금 값이고 자료로 고른 것이다** — 그 사실을 적어 둔다.
  //    λ800·gravity: 자사 MAE 6.88 · 자사편향 −1.2 · 경쟁편향 −2.7 · 예측 SD 6.7(실측 6.4) · 자사우위 1.65(실측 1.74).
  // ⛔ **2026-09-24 되돌림 — 800 → 0(꺼짐).** 사용자: *"옛 데이터가 더 정확했다는 느낌. 호구포역점 실제 나올 수 없는
  //    매출이고, 하안금당도 높고 오송점도 높고."* 후보지 13곳에서 새 배선이 평균 +4.8%p, 9곳 3%p↑, 내린 곳 0
  //    (`_labCandidate` (8)). 경쟁점 1~2곳인 자리(호구포 55% 상한·오송 +11·영월)에서 고리 수요가 점유율에 안 눌리고
  //    그대로 통과한다. 실측 수요 잣대(14동네)도 λ0 1.14배 vs λ800 0.58배로 고리가 수요를 실측 하한 위로 밀었다.
  //    자사 40곳 성적은 옛/새 같다(라벨 4곳 빼면 5.68 vs 5.56%p, 2SE 1.94). 치르는 값: 경쟁점 편향 −2 → −11%p ·
  //    자사우위 1.65 → 3.5배(실측 1.74) — 바깥 잣대가 다시 나빠진다. 자료·배선은 남긴다(λ>0으로 켜면 그대로 산다).
  residentRingDecayM: 0,
  // ✅ 2026-09-23 저녁 — **"gravity"** 채택. 사용자: *"편차가 커진 게 별로다. 근거가 있으면 감수, 값을 위해
  //    조정한 거면 별로."* core는 밀집 도심을 부풀렸다(발산역 +23·수원망포 +21%p, 자사 예측 SD 9.9 vs 실측 6.4) —
  //    1~2km 사람은 세면서 그 곁 PC방은 안 센 비대칭. gravity(가까운 쪽으로 간다, 기하)로 SD 6.7·최악 사라짐·
  //    짝 r 0.55→0.53 유지. 치르는 값: 자사 r 0.337→0.187(동네 간 순서, n=37에서 1SE 안팎). 타입 쪽 주석.
  // ⛔ 2026-09-24 — 고리를 껐으므로(위) 이 값은 지금 안 읽힌다. 다시 켤 때의 기본은 gravity가 맞다(core는 밀집 도심 과대).
  residentRingShare: "gravity",
  // ✅ 2026-09-23 밤 채택 — 사용자가 8곳 목록을 보고 *"맞음 위에 매장 항아리"*. 2차 문항(그 방향 1~2km에 주거가 있나)으로
  //    52곳 중 8곳만 깎인다(1방향 6·2방향 2). 켬: 자사 MAE 6.11→5.86 · r 0.300→0.342 · 편향 그대로(`_bundleCandidate` (8)).
  //    판정은 storeEvalLabTradeAreaJudgments(ringCutCount). 1차 단절 문항은 폐기(63%가 2방향 이상, 구미산동 못 잡음).
  // ⛔ 2026-09-24 — 고리가 꺼져 있어(residentRingDecayM 0) 지금은 안 읽힌다. 고리를 다시 켜면 같이 산다.
  useRingEnclosure: true,
  // 연령가중 자체는 남녀 이용률을 지역 성비로 섞어 그때그때 만든다(blendUsageByGender).
  // 여기 값은 성비를 모를 때(남녀 1:1)의 기본값이다.
  ageWeights: blendUsageByGender(0.5),
  // 원장 실측 — 오픈 1년 안 4곳 평균(2026-09-21). 아래 hoursPerUserFixed가 true라 이 값이
  // 그대로 쓰인다. 그전에는 여기 1.0을 두고 독점매장에서 역산했다.
  hoursPerUserPerMonth: 8.20,
  hoursPerUserFixed: true,
  gapExponent: 4.0,
  // ⛔ **2026-09-22 — 뺐다(20 → 0).** 하루 전 채택했다가 사용자 지적으로 되돌린 값이다.
  //    사용자: *"독점매장만 넣었구나. 그건 결과맞추기 아니냐? 근거가 너무 얼토당토없는 거
  //    아니냐?"* 맞는 지적이었다. **계수 하나를 점 3개에 맞춘 것**이고, 그때 "관측값이라
  //    자유 계수가 아니다"라고 설명한 건 과한 표현이었다.
  //
  // ⚠️ 더 결정적인 건 **이름부터 틀렸다**는 것이다. 20대를 뽑아낸 "독점 3곳"은 독점이
  //    아니다 — 500m 안에 없을 뿐이고, 2km 안에는 영업 중인 경쟁점이 있다:
  //      남악 6곳(779m OXPC · 962m 크라우드PC …) · 탕정역 3곳 · 광주각화 27건(전부 미판정)
  //    거칠게 재면(90대 가정·현재 감쇠) 필요한 몫이 남악 10.4대인데 광주각화는 **51.9대**다.
  //    → "PC방 안 가는 몫"이라는 상수가 아니라 대부분 **반경 밖 경쟁점**이고, 그건
  //      매장마다 다른 값이어야 한다. 상수로 박으면 2km를 세는 작업과 같은 자리를 두 번 메운다.
  //
  // 빼면서 드러나는 것(가리지 않는다 — 이게 "아직 2km를 안 세고 있다"는 표시등이다):
  //   · 독점 3곳이 19% 과대로 남고 전체 편향이 +2.69%p로 드러난다
  //   · 입지배율이 점유율을 100%로 자르던 기존점이 0곳 → 5곳으로 다시 생긴다(호구포역·영월)
  //
  // 📌 어제 잰 값은 되살릴 때 쓰라고 남긴다. 절대형 20대(탕정역 20.9 · 광주각화 19.8 ·
  //    남악 19.3, 기하평균 20.0)는 전체 MAE 6.517%p · 경쟁 35곳 편향 −0.70%p였고,
  //    같은 19%를 "수요 ×0.84"로 읽으면 6.703%p · −3.75%p였다. **그 비교는 여전히 유효하다**
  //    — 수요 추정 자체가 19% 틀린 건 아니라는 뜻이다. 다만 그 몫의 정체가 상수가 아니다.
  // 측정: `_shareDiscrimination.test.ts` [B5] · `_scaleMeasured.test.ts` [측정 1]
  outsideOptionIp: 0,
  // ✅ 2026-09-24 밤 — 우리 몫 상한 80%(k 0.25). 근거는 타입 쪽 주석·`_layerSplit` (7). 사용자: "개선할 거 개선해도 괜찮을 듯" + 후보지 실무 감각 6곳.
  // ✅ 2026-09-25 낮 — **0.25 → 0.18(상한 85%)**. 가동률 창을 2~12개월차로 맞춘 뒤 정의대로 다시 잼: 독점 자사 3곳(탕정역 83 · 광주각화 85 · 남악 85%,
  //    구미산동은 경쟁 IP가 생겨 빠짐) 실제 몫 중앙 85% → k = 1/0.85 − 1 = 0.18. 09-24의 68~83%는 몫 상한·강도 문 채택 전 산식 값이었다.
  //    사용자 "정답으로 하자". 재고 표 `_windowRefit.test.ts` (2): MAE 4.13→3.90 · 편향 −1.94→−1.11 · 후보지 +0.7p. docs/releases/2026-09-25-window-refit.md
  ownShareCapK: 0.18,
  agglomerationFactor: 0,
  densityCorrection: 0,
  // ✅ **2026-09-21 밤 채택 — 1,493원으로 못 박는다**(아래 productUnitPriceFixed).
  //
  // 값의 출처가 바뀌었다. 그전에는 **잔차**로 구했다 — 총매출÷(PC×720×가동률) − 정가로 만든
  // PC몫. 매출DB에 `productSales`가 따로 있는데 그걸 안 쓰고 빼서 구한 것이다. 그러면
  // **PC몫이 틀린 만큼이 상품몫으로 흘러든다.** 직접 재서 대보니 평균은 같은데(둘 다 1,493원)
  // 퍼짐이 다르다 — 직접 SD 242원 vs 잔차 SD **328원**. 그 차(SD 149원)가 흘러든 오차다
  // (평내호평 +309원 · 김포구래 −341원). 정가와는 무관하다(상관 −0.001) — 정가 반응식 β가
  // 범인이 아니라 정가로 설명 안 되는 실제 요금 구조(정액권·할인 등)다.
  //
  // 1,493원 = 기존점 38곳의 **직접 측정** 평균(2026-09-21). 중앙 1,495원 · 범위 1,090~2,076원.
  // 다시 재는 자리: `_productShareFormula.test.ts` (1)번. ⚠️ 표본이 늘면 다시 잰다.
  // ✅ 2026-09-25 낮 — **1,493 → 1,471**. 가동률 창을 개점 2~12개월차(실매출과 같은 창)로 맞추면서 같은 창으로 다시 잰 값
  //    (38곳 Σ상품매출÷Σ(PC×720×가동률), 중앙 1,476, `_windowRefit.test.ts` (1)②). 정의를 맞춘 재측정이지 계수 조정이 아니다.
  //    상품몫이 매장별로 두 배 퍼지는 건 개점 세대 효과(2023 개점 1,287 → 2026 1,665)인데 기전(메뉴 구성?)을 못 밝혀 상수로 둔다
  //    (docs/releases/2026-09-25-product-share-drivers.md — 메뉴판 가격은 3년 반 +3%뿐, 물가 아님).
  productUnitPrice: 1471,
  productUnitPriceFixed: true,
  // 운영 산식 usageRevenue.ts effectiveHourlyRate와 같은 값 — 두 산식이 한 식을 쓴다.
  // 2026-09-16 저녁(3)부터 이 지수는 PC몫에만 걸린다(상품몫은 정가와 무관).
  rateElasticity: 0.546,
  referenceHourlyRate: 1343,
  // 2026-09-17 — 새 경쟁 항이 홀드아웃·대조군을 통과해 **품질 모드가 기본값**이 됐다.
  // 끄기(off)는 여전히 쓸모가 있다 — 화면의 "필요 점유율"이 그 모드로 계산된다.
  shareMode: "quality",
  effectiveRadiusM: 300,
  // ✅ **2026-09-23 채택 — 3 → 1.75.** 존구성 비중 0.06 · 고리 λ600과 **한 묶음**이다(타입 쪽 주석).
  //    사용자: *"한번 해볼까?"*. 되돌리려면 셋을 같이 되돌린다(3 · 0.238 · 0).
  // ⛔ **2026-09-24 되돌림 — 1.75 → 3.** 고리(λ0)·존구성(0.238)과 같이. 근거는 residentRingDecayM 주석.
  //    검산: `_bundleCandidate` (0) 자사 37곳 MAE 5.84%p(배수 군부대 2.25만).
  qualityExponent: 3,
  // 실무 감각 비중(입지 17.0% 제외, 시설 34.0%를 존구성 23.8 + 인테리어 10.2로 나눈 값).
  // 합이 0.83인 건 입지 몫이 빠져서다 — computeQualityScore가 있는 항목의 합으로 나눈다.
  // ✅ **2026-09-23 — 존구성 0.238 → 0.06.** 항목은 남기고 비중만 1/4로. 빼는 게 아니다(타입 쪽 주석).
  // ⛔ **2026-09-24 되돌림 — 0.06 → 0.238.** θ3·고리 꺼짐과 한 묶음(residentRingDecayM 주석).
  qualityWeights: { spec: 0.264, food: 0.075, zone: 0.238, interior: 0.102, management: 0.151 },
  // 입지 — [자료] 둘만 켜져 있고 [보류] 셋은 0이다. 자세한 근거는 타입 쪽 주석.
  // ⚠️ 중심도 ν는 2026-09-20에 0.25 -> 0.5로 바뀌었다. **잔차화와 한 묶음이다** —
  //    잔차화를 끄면(centralityResidual: null) ν도 0.25로 돌려야 한다. 0.5는 잔차 눈금 값이다.
  locationExponents: { centrality: 0.5, access: 0.25, direction: 0, flowBlock: 0, visibility: 0 },
  // 기준값 — 경쟁상권 29곳의 **기하평균**. 중앙값이 아니다(위 주석의 이유).
  locationReferences: { centrality: 3.22, access: 3.42 },
  // 중심도 잔차화 상수 — 기존점 38곳에서 적합(2026-09-20). 표본이 늘면 다시 적합할 것.
  // `_residualCentralityPort.test.ts`의 (1)번 시험이 지금 표본의 값을 찍어 준다.
  centralityResidual: { slope: 0.4801, geoMeanCentrality: 3.2719, geoMeanFloating400: 75093 },
  // ✅ **2026-09-21 채택 — 거리 감쇠를 켠다.** 켜면 effectiveRadiusM(계단)은 안 쓰인다.
  //
  //   무게 = 1                        (거리 <= 200m)
  //        = exp(−(거리 − 200) ÷ 200)  (그 밖)
  //   곡선: 200m 100% · 300m 61% · 350m 47% · 400m 37% · 500m 22% · 700m 8% · 1km 2%
  //   (표시값은 정규화 0.9593을 곱하기 전의 날무게다)
  //
  // 개념은 사용자가 택했다(*"감쇠로가자. (...) **나중에 401m 나오면그건어쩔건데.**"*).
  // 계단의 근본 문제를 짚은 말이다 — 경계를 어디에 두든 **그 바로 밖 사례가 계속 나온다.**
  // 오송점 팀플PC가 342m라 0으로 세졌고, 400으로 옮기면 하안금당 407m가 같은 신세가 된다.
  //
  // ── ⚠️ 값은 2026-09-21 저녁에 R300 λ150 -> R200 λ200으로 **다시 골랐다** ──────────
  //
  // 처음 값(R300 λ150)은 **매출 MAPE**로 고른 것이었다. 그날 사용자가 판정 기준을 바꿨다:
  //   *"지금 mape가 매출이잖아. 매출단가가 정확하지 않잖아? 같은 가동률이라도 실제매출이랑
  //    오차가 큰 편이긴 해. (...) 가동률로 판단하고 가동률을 맞춘 후에 매출로 넘어가는
  //    구조가 맞는 것 같다."*  그리고 값에 대해:
  //   *"거리감쇠는 적용하되, 수치값은 **가동률 오차폭과 평균을 줄일 수 있는 값**을 넣자."*
  //
  // 그 전제가 옳은지부터 쟀다(`_decayUnderUtilization` (1)절) — 단가만의 오차가 평균 10.0%로
  // 있고 가동률 오차와 **무관하다**(r=−0.060). 매출 오차 분산의 17%가 단가 몫이다.
  // 지배적이진 않지만 **무관한 잡음**이라, 매출로 거리 계수를 고르면 그 잡음에 맞추게 된다.
  //
  // 가동률 자로 격자를 다시 훑고(42칸), 사용자 기준 그대로 **MAE와 SD를 동시에** 봤다:
  //
  //            가동률 MAE   SD       최악
  //   계단300     6.06%p   4.58%p   52.4%
  //   R300 λ150   6.51%p   4.25%p   48.2%   <- 옛 값(매출로 고른 것)
  //   R200 λ200   6.34%p   4.19%p   49.0%   <- 지금. 옛 값보다 평균·폭 **둘 다** 낫다
  //
  // ⚠️ 셋의 차이는 **자료가 못 가른다** — 짝지은 부트스트랩 2000회에서 R200 λ200의 MAE
  //    개선 95% 구간이 [−0.45, +0.10]%p로 0을 품는다(R200 λ150·R150 λ200도 마찬가지).
  //    자료가 못 고르면 실무 판단이 이긴다는 관례에 따라, **사용자 기준(평균·폭 동시 개선)을
  //    만족하면서 최악 손해가 제일 작은 칸**(+0.7%p)을 골랐다.
  //
  // ⚠️ MAE만 쫓으면 R150 λ125(6.02%p)가 낫지만 **최악이 53.8%로 벌어진다** — 폭을 줄이라는
  //    지시를 어긴다. 중첩 LOO가 그 칸을 38번 중 34번 뽑는데도 안 쓴 이유가 그것이다.
  // ⚠️ 감쇠는 **어떤 값을 넣어도 계단300보다 평균·폭을 동시에 개선하지 못한다**(42칸 전부).
  //    계단보다 MAE는 지고 SD·최악은 이긴다. 감쇠를 쓰는 건 성적이 아니라 개념 때문이다.
  //
  // 근거: `_decayUnderUtilization.test.ts` (4)(5)(6)절. 매출 자 판정은 `_decayShapeSelection`.
  // ⚠️ **장산점**을 따로 볼 것 — 300m 밖 넷(211·193·111·131대)이 새로 세지며 경쟁량이
  //    54% 늘어 오차가 크게 벌어진다. 경계 문제가 아니라 총량 문제다(`_jangsanDecay`).
  // weightFactor 0.9593 — 기존점 38곳에서 Σ(ip x 계단300) ÷ Σ(ip x 감쇠)로 잰 총량 정규화
  //    상수다. 자의적 계수가 아니고, **(R, λ)를 바꾸면 반드시 다시 재야 한다**(타입 쪽 주석).
  rivalDistanceDecay: { plateauM: 200, scaleM: 200, weightFactor: 0.9593 },
  // 2026-09-16 측정값에서 **산업단지·기타를 2026-09-20에 1.00으로 껐다**(자세한 근거는
  // 타입 쪽 주석). 그 둘이 축척 기준점(탕정역·남악)을 부풀려 자를 휘게 하고 있었고,
  // 자를 바로잡고 다시 뽑으니 1.08 · 0.94로 사실상 1이었다. 표본 2~5곳이라 확정값이
  // 아니다 — 조절판에서 돌려볼 것.
  // ✅ **2026-09-23 저녁 — 대학가·산업단지를 켜고 군부대를 내렸다.** 사용자: *"특수상권 배수 켤 수 있는 건 켜보자
  //    — 대학상권·군인·산업단지."* 껐던 이유(축척을 독점 3곳에서 맞추던 순환)는 2026-09-21 축척 고정으로 사라졌다.
  //    이번엔 **바깥 표본(그 동네 경쟁점 핑봇)**을 같이 봤다 — 자사·경쟁점이 같이 과소면 수요 배수가 맞는 자리다.
  //    기준: 그 유형 자사·경쟁점 편향 |합| 최소(`_bundleCandidate` (7b), gravity λ800 배선):
  //      대학가   1.0 → **1.30**  자사 5곳 −8.9→−1.2 · 경쟁점 5곳 −3.3→+2.9 (둘 다 ±3%p 안). 1차 측정(09-16) 1.45는 과함
  //      산업단지 1.0 → **1.40**  자사 2곳 −10.1→−0.9 · 경쟁점 1곳 −17.9→−13.2(한 곳이라 못 맞춤). 1차 측정 1.39와 일치
  //      군부대  2.25 → **2.00**  자사 2곳 +1.4→−2.6 · 경쟁점 2곳 +7.5→+4.9 (|합| 최소)
  //    ⚠️ 산업단지·군부대는 표본 2~3곳이라 **값이 아니라 방향**이다. 표본이 늘면 다시 잰다. 기타·관광유흥은 그대로 1.0.
  // ⛔ 2026-09-24 낮 — 고리 되돌림과 같이 군부대 2.25만 남기고 껐다(위 값은 고리 λ800 배선에서 고른 것이라 근거가 같이 사라짐).
  // ✅ **2026-09-24 오후 — 옛 배선(θ3·존 0.238·λ0)에서 다시 재서 켰다.** 사용자: *"특수수요 배수값은 평균에 제일 유리한 값
  //    넣어주면 될 것 같은데? 대학/군인/산업."* 기준: 그 유형 **자사 편향이 0에 가장 가까운 값**(`_bundleCandidate` (7b)).
  //      대학가   1.30  자사 5곳 −7.9 → +0.2%p   (경쟁점 5곳 1:1 −11.7 → −8.1 — 다른 유형 경쟁점도 −10 안팎이라 θ 문제, 배수 자리 아님)
  //      산업단지 1.40  자사 2곳 −10.2 → −1.0%p  (1차 측정 09-16 1.39와 일치)
  //        ⚠️ 퇴근 직전 정정 → **1.20**. 재고 표(_bundleCandidate (7))는 경쟁점 있는 37곳만 넣어 **탕정역(독점·산업단지)을 못 봤다**.
  //        탕정역은 배수 없이 +7.4, 1.4면 +20.9(사용자 "탕정역 가동률 +20 뜨네"). 3곳(시흥정왕·탕정역·광주첨단) 평균 편향
  //        ×1.0 −4.1 · ×1.4 +6.6 → 0에 가장 가까운 1.2로. 세 곳이 25%p 갈려 값이 아니라 방향. 40곳 전체 표: `_labCandidate` (9)
  //      군부대   2.00  자사 2곳 +3.1(2.25) → −1.1%p
  //      관광·유흥 1.40  자사 2곳 −9.0 → 약 +0.4%p (사용자 "관광/유흥도 켜보던가". 1.3 −2.0 · 1.5 +2.8 사이)
  //    기타는 4곳 −1.1%p로 이미 0 근처라 1.0 유지. 없음(22곳 +1.0)은 기준선이라 안 건드린다.
  //    셋을 같이(관광유흥 전): 자사 37곳 MAE 5.84 → **5.22%p** · 편향 −1.4 → −0.1.
  //    ⚠️ 표본 2~5곳이라 값이 아니라 방향이다. 대학가는 매장별로 갈린다(전대후문 −8·청주대 −6 vs 부경대 +10·전대상대 +5) —
  //    하나의 배수로 5곳을 ±3%p에 못 넣는다. 다음 갈래: "대학가" 꼬리표 대신 대학 규모×거리.
  // ⛔ **2026-09-24 집 PC — 대학가·산업단지·관광유흥을 다시 1.0으로 껐다(사용자 결정). 군부대 2.0만 남긴다.**
  //    사용자: *"호구포역점, 오송점, 구리돌다리점, 여기 예상매출이 기존에 배수설정 안된게 더 적합했던거같아."*
  //    세 후보지가 산업단지(호구포역 41.3→49.5%)·대학가(오송 28.9→37.6%)·관광유흥(구리돌다리 23.8→33.3%) 각각이다.
  //    배수는 수요에만 곱하고 점유율은 수요와 무관해서 **그 유형 후보지 가동률이 정확히 ×배수**가 된다 — 자사에서 "평균 편향 0"으로
  //    고른 값이 후보지에선 검증 없이 통과한다(`_labCandidate` (10) 재고 표). 자사 40곳 비용: MAE 5.21 → 5.81%p(군부대만 2.0).
  //    유형별 근거도 약했다 — 산업단지 3곳은 25%p 갈림(1.0이어도 탕정역 +7.4) · 관광유흥 2곳(수원인계·야당)은 층 가르기(`_layerSplit`)에서
  //    "점유율 층, 경쟁점은 맞음"이라 수요 배수 자리가 아닐 수 있음 · 대학가 5곳은 4곳이 같은 방향이지만 매장별 −16~0으로 하나의 값이 없다.
  //    군부대 2곳(금촌역·문산)은 −17.6 → +0.1로 같은 방향·큰 폭이라 남긴다. 대학가는 배수 대신 "대학 규모×거리" 항목이 다음 갈래.
  // ✅ **같은 날 밤 — 대학가 1.3을 다시 켰다. 단 강도 "높음"에만**(`specialDemandHighOnly`). 사용자: *"대학배수 다시 켤까?
  //    오송점 같은 문제 다시 발생 안 하려면?"* → 오송은 유형이 틀린 것(약대·행정타운을 대학가/보통으로)이라 유형을 '기타'로
  //    고치고, 산식에는 강도 문을 달아 보통 이하가 배수를 못 타게 했다. 자사 대학가 5곳은 전부 높음이라 성적 그대로
  //    (40곳 MAE 5.81 → 5.56 · 편향 −1.0 → 0.0 · 대학가 5곳 −7.5 → +0.7). 산업단지·관광유흥은 그대로 1.0.
  //    ⚠️ 전대상대의 입지평가 강도 "높음"은 2026-09-02 '실측 백테스트 보정'이 올린 값이었다(기존점 문서는 보통) — 수동보정이라
  //    사용자 지시로 보통으로 되돌렸다. 문 아래에서 전대상대는 배수를 안 타고 −0.9%p(켜면 +5.8).
  // ✅ **같은 날 더 늦게 — 산업단지 1.4를 강도 "높음"에만 켰고, 군부대도 문에 넣었다**(사용자: *"산업단지나 유흥가 군부대상권도 특수수요
  //    강도에 따라서 배수 적용하는 거 검토해봐"* → *"오케이 일케하자"*). `_labCandidate` (11):
  //      산업단지  높음 2곳 함의 배수 시흥정왕 ×1.31 · 광주첨단 ×1.60(기하평균 1.45) vs 보통 탕정역 ×0.82 → 강도 방향이 맞다.
  //                1.4는 09-16 첫 측정 1.39 · 09-23 값 1.4와 같다. 어제 1.2로 내린 이유(탕정역 +20.9)는 탕정역이 보통이라 문에서 빠져 사라진다.
  //                후보지 호구포역(낮음)은 안 탄다(41.3% 그대로). 표본 2곳 — 값이 아니라 방향.
  //      군부대   자사 2곳 모두 높음이라 성적은 그대로. 앞으로 군부대/보통 후보지에 ×2.0이 검증 없이 통과하는 길을 막는 안전문.
  //      관광유흥 1.0 유지 — 자사 2곳 모두 높음이라 강도로 못 가르고, 후보지 5곳 중 4곳이 높음이라 문이 구리돌다리·신중동을 못 막는다.
  //                층 가르기에서 수원인계·야당은 "점유율 층"이라 수요 배수 자리인지도 확인이 안 됐다.
  //    결과: 자사 40곳 MAE 5.44 → 5.11%p · 편향 −0.2 → +0.3 · 산업단지 3곳 편향 −4.1 → +2.1(탕정역 +7.4 포함) · 후보지 13곳 무변동.
  // ✅ **2026-09-24 밤 늦게 — 관광유흥 1.4도 강도 "높음"에만 켰다**(사용자 "오호 좋은 거 같은데?"). 몫 상한(ownShareCapK) 채택 뒤에 다시 잰 값(`_layerSplit` (6)(7)):
  //      자사 이해되는 오차 4.07 → 3.61 · 전체 MAE 5.30 → 4.84 · ±5%p 26 → 28 · 수원인계·야당(높음 2곳) −8 → 0 근처 · 경쟁점 −11.7 → −11.4(그대로)
  //      후보지 실무 잣대 0.34 → 0.23 — 창원상남 1,937 → 2,712만(실무 4.5~4.8천) · 울산삼산 2,707 → 3,790(실무 6천). 치르는 값: 구리돌다리 4,083 → 5,716 ·
  //      신중동 3,853 → 5,394(바깥 잣대 없음). 2.0은 창원·울산이 실무에 붙지만 자사 쪽이 1.4보다 나쁘고(4.26) 구리돌다리 8,166이라 안 씀.
  //    낮에 "구리돌다리는 배수 없는 쪽이 맞다"고 껐던 것을 되돌린 셈 — 그때는 몫 상한이 없어 후보지 5곳이 한꺼번에 떴고, 사용자가 실무 감각의 정확도를 낮춰 봤다.
  //    자사 근거 2곳(함의 배수 1.32 · 1.34)이라 값이 아니라 방향. 유형 정의(유흥가·관광지 방문객)는 locationEvalAi 프롬프트.
  specialDemandMultipliers: { "군부대": 2.0, "대학가": 1.3, "산업단지": 1.4, "기타": 1.0, "관광·유흥": 1.4, "관광유흥": 1.4, "없음": 1.0 },
  specialDemandHighOnly: ["대학가", "산업단지", "군부대", "관광·유흥", "관광유흥"],
  // ✅ **2026-09-21 채택 — 지수 눈금 보정을 켠다** (자세한 근거는 타입 쪽 주석).
  //    그날 저녁 b를 0.327 → **0.45**로 올렸다(변별력. 타입 쪽 주석의 표를 볼 것).
  //
  //   가동률 = 0.3011^(1−0.45) x (수요 x 점유율 ÷ 용량)^0.45,  점유율에 입지^(0.169÷0.45)
  //
  // 사용자 결정: *"산식 건드려도 돼. 기존에 매출 MAPE로 기대값 잡고 했다면 현재는 가동률
  // 기준으로 잡아야 하니 바꾸는 게 적합하다면 바꿔도 되지."* + *"수요 > 가동률 이 산식이
  // 현재 검증 가능한 부분이 없으니까 값에 맞추는 형태로 하는 게 최선이라고 보긴 함."*
  //
  // 자료가 고른 b는 0.327이었고(33곳 · 38겹 재적합 0.284~0.371로 **1을 전혀 안 품는다**),
  // c=0.169도 마찬가지다(0.099~0.254). 다만 **자료는 b를 못 고른다**(중첩 LOO가 훈련평균과
  // 같다) — 그래서 0.45는 용도로 고른 값이다. 최악 오차(가동률·매출 둘 다)와 변별을 사고
  // 평균 오차를 조금 판 것이다. 표본이 늘면 다시 잰다.
  // 닻 0.3011 = 기존점 평가창 실측 가동률의 기하평균(40곳). 크기는 축척이 흡수하므로
  // 정확한 값이 성적을 바꾸지는 않는다 — hoursPerUserPerMonth가 뜻을 유지하게 하는 용도다.
  // ⛔ **2026-09-21 밤 — 껐다.** 사용자: *"줄이는 게 아니라 없앨까? 이건 근거없는 단순보정
  //    아닐까?"* 맞는 지적이었다. 이건 기전이 아니라 **출력을 누르는 보정**이고, 값(0.45)도
  //    자료가 고른 게 아니라 판단이었다(자료는 b를 못 고른다). 실험실 취지가 "값에 맞추지
  //    말고 기전을 찾자"라서 껐다. 되살리려면 아래 한 줄을 되돌리면 된다.
  //    (그날 밤엔 b가 삼키던 수준 오차를 outsideOptionIp 20대에 넘겼는데, **2026-09-22에
  //     그것도 뺐다** — 점 3개 적합이었다. 지금은 수준 오차가 드러난 상태다.)
  //
  //    치르는 값: 가동률 MAE 4.18 -> 6.52%p · ±5%p 24 -> 18곳 · 예측 퍼짐이 실측의 1.73배로
  //    남는다. **그 퍼짐 과장이 이제 눈앞에 남은 유일한 문제**다 — b가 켜져 있으면 안 보인다.
  //    실험실 산식은 운영에 안 들어가므로(결재는 V62) 성적 하락의 비용은 "언제 올릴 수
  //    있나"가 늦어지는 것뿐이다.
  //
  // ⚠️ **왜 `null`이 아니라 b=1인가.** null로 두면 b만 빠지는 게 아니라 **입지 지수까지**
  //    c/b(=0.169)에서 **1로 튄다**(본체 locExpInShare). 입지 지수는 눌러 담는 보정이 아니라
  //    구조 계수이고, 자료가 1을 강하게 거부한다(38겹 재적합 0.099~0.254). b=1이면
  //    ref^(1−b)=1이라 **당기는 항은 정확히 사라지고** c만 남는다. 그게 의도한 상태다.
  // ✅ **2026-09-22 밤 — 입지 지수를 0.169 -> 0.5로 올렸다(사용자 결정).**
  //
  // 사용자: *"경쟁점 1층인데 우리 7층이야. 유입에 불리함. 우리매장 상권 끝에 있어, 점포 옆은
  //   그냥 공터야. 이러면 유입에 불리함. 이런 개념이지."* -> **항목은 맞다.** 세기가 문제였다.
  //
  // ⚠️ 날것 상관만 보면 입지는 **음수**다(실측 가동률과 r=-0.193). 그건 **교란**이다 —
  //    중심도 높은 동네는 경쟁점도 많아 가동률이 낮다. 수요·점유율을 설명하고 남은
  //    **잔차와 보면 r=+0.480**으로 유의선(±0.41)을 넘는다. 방향이 맞다.
  //
  // ── 왜 0.5인가 — 가정치를 자료가 고르게 했다 ─────────────────────────────
  // 입지배율 자체는 측정값이지만(유동 300/1000 · 층·엘리베이터), **그게 얼마나 세게 작용하나는
  // 잴 방법이 없다 = 가정치**다. 그래서 어제 눈금 보정 b에 쓴 잣대를 그대로 댔다:
  //   중첩 LOO(한 곳 빼고 39곳으로 고른 뒤 안 본 그 곳을 맞힌다) -> 겹마다 고른 값이
  //   **0.4(22회)·0.5(18회) 두 가지뿐**이고, LOO MAE 6.08%p로 옛값(6.26%p)보다 낫다.
  //   ⭐ b는 이 잣대를 못 넘어서 껐다(겹마다 훨씬 넓게 갈렸다). **이건 넘는다.**
  //
  // ── 치르는 값 — 오차와 분별력이 맞바꿈이다 ───────────────────────────────
  //   지수     MAE      최악       분별력 r   순위 분별력
  //   0.000   6.72%p   22.28%p    0.516      0.569
  //   0.169   6.26%p   17.68%p    0.525      0.584   <- 옛값. **분별력은 여기가 최고**
  //   0.500   5.85%p   16.24%p    0.516      0.544   <- 채택
  //   1.000   6.55%p   14.29%p    0.456      0.518   <- 교과서. 분별력이 확실히 나쁘다
  // 분별력 손실(r −0.009 · 순위 −0.04)은 40곳 표본에서 **잡음 수준**이고, 오차 개선
  // (MAE −0.41%p · 최악 −1.44%p)은 실질적이라 사용자가 0.5를 골랐다.
  // ⚠️ **표본이 늘면 다시 잰다.** 분별력 차이가 잡음이 아닌 것으로 드러나면 0.169로 돌아간다.
  // 재는 자리: `_spreadLayers.test.ts` (3)(4)번.
  indexCalibration: { ratioExponent: 1, locationExponent: 0.5, referenceUtilization: 0.3011 },
  maxUtilization: 0.55,
};

/** 한 점포(기존점이든 후보지든)의 교과서식 입력. */
export type TextbookInput = {
  storeCode: string;
  storeName: string | null;
  pcCount: number | null;
  hourlyRate: number | null;
  /**
   * **실측 월평균 가동률**(0~1). 평가창 월매출의 `utilizationRate` 평균이다.
   *
   * 2026-09-16 신설. 수요층 축척을 여기에 맞춘다 — 그전에는 축척 하나가 "수요를 가동률로
   * 바꾸는 일"과 "가동률을 매출로 바꾸는 일"을 겸해서, 환산층이 틀린 만큼이 가동률로
   * 되밀려 들어갔다(독점 3곳 가동률이 화면에서 −3.2~−5.3% 어긋나 보이던 원인).
   * 층마다 실측값이 따로 있으니(가동률은 매출DB·게토, 매출은 매출DB) 축척도 따로 맞춘다.
   */
  actualUtilization: number | null;
  competitivenessGap: number | null;
  competitorIp: number | null;
  /**
   * **자사 경쟁력 항목별 점수** — 합친 점수가 아니라 **항목별 원점수**를 넣는다
   * (shareMode="quality"용). 비중이 조절판에서 바뀌면 점수도 같이 바뀌어야 하기 때문이다.
   * null이면 품질 항을 통째로 빼고 비례 배분한다("모른다 = 중립").
   */
  ownQualityParts: QualityParts | null;
  /**
   * **경쟁점 한 곳씩** — 거리·PC수·항목별 점수 (shareMode="quality"용).
   *
   * ⚠️ 합계인 `competitorIp`와 달리 **거리로 걸러야 하므로 낱개가 필요하다.** 그리고
   *    `competitorIp`는 미조사 500m 점포를 100대로 추정해 채우는데(`computeCompetitorIp`),
   *    그런 점포는 거리도 품질도 없어서 여기 못 들어온다. 두 값은 일부러 다르다.
   */
  /**
   * `name`은 **화면에 그리려고만** 들고 다닌다 — 계산에는 안 쓴다. 이름을 따로 뽑아
   * 화면에서 다시 짝지으면 순서가 어긋나 "엉뚱한 경쟁점이 잡혔다"고 오해하게 된다
   * (2026-09-18 사용자 요청: *"어느매장을 인식하고있는지 내가봐야겠어"*).
   */
  rivals: { ip: number; distanceM: number | null; parts: QualityParts | null; name?: string | null }[] | null;
  /**
   * **입지 5항목** (2026-09-17). 전부 null이면 그 항은 1배(중립)로 빠진다 —
   * 자료가 없다고 틀린 값을 만들지 않는다. 화면에는 "자료없음"으로 표시한다.
   */
  location: {
    /** 상권 중심도 = (유동 300m ÷ 유동 1km) x (1000/300)². 1보다 크면 문 앞이 빽빽하다. */
    centrality: number | null;
    /** 접근성 — `computeLocationScoreFromFacts(층수, 지상지하, 엘베)`. 자사·경쟁점이 같은 자다. */
    access: number | null;
    /** 유동 방향 편심도(0~1). 0이면 사방이 고르고, 1에 가까울수록 한쪽으로 쏠렸다(상권 끝). */
    direction: number | null;
    /** 동선 방해 — 아직 자료 없음(경쟁점 좌표 22%). 자리만 둔다. */
    flowBlock: number | null;
    /** 간판·출입구 가시성 — 아직 자료 없음(로드뷰 평가 필요). 자리만 둔다. */
    visibility: number | null;
  } | null;
  /** 특수수요 유형 — "군부대"/"대학가"/"산업단지"/"관광·유흥"/"기타"/"없음". 수요 배수에 쓴다. */
  specialDemandType: string | null;
  /** 특수수요 강도(없음/낮음/보통/높음). `specialDemandHighOnly`에 든 유형은 "높음"일 때만 배수를 탄다. 모르면 null = 안 탄다. */
  specialDemandIntensity?: string | null;
  /** 상권 흡인력 계산용. 조사된 경쟁점 수(IP가 아니라 점포 수). */
  competitorCount: number | null;
  /** 주거 — 500m는 총수만, 1km는 연령 분해까지 있다. */
  pop500m: number | null;
  pop1km: number | null;
  residentAges: ResidentAges | null;
  /**
   * **1km 밖 누적 원 인구**(연령 분해) — 1.5km · 2km · 5km. `residentRingDecayM`이 켜졌을 때만 쓴다.
   * 누적값이다(고리 인구가 아니다). 없으면 null — 고리 항은 빠지고 `missing`에 적힌다.
   * 출처: SGIS 반경 집계(2026-09-20 수집). 2026-09-23 신설.
   */
  residentAgesByRadius: Partial<Record<ResidentRingRadius, ResidentAges | null>> | null;
  /**
   * **주거 상권 반경(m) — 사람이 확인한 매장별 사실.** 기본 1000(없음·null). 1500·2000이면 주거 원을 그 반경의
   * **누적 인구**(`residentAgesByRadius`)로 바꿔 끼운다 — 고리(별도 점유율·감쇠)가 아니라 **원 자체를 넓히는 것**이라
   * 넓힌 원의 사람도 1km 안 사람과 같은 점유율·같은 배수로 센다. 그 반경의 누적 자료가 없으면 1km로 두고 `missing`에 적는다.
   *
   * 사용자 기준(2026-09-24 밤): **"2km 안에 다른 PC방 상권(묶음)이 자리잡혀 있나"** — 없으면 2km(양주덕정·문경·영월),
   * 있으면 1km(구미산동·호구포역·진주혁신·야당·송도·김포구래는 옆 상권에 PC방이 있어 못 준다). 경쟁점 모양으로는 못 가른다 —
   * 같은 모양(300m 묶음 밖으로 1km까지 경쟁 없음) 6곳에 2km를 일괄로 걸면 4곳이 +13~+34%p 터진다(`_labCandidate` (16)).
   * AI 지도 판정(otherCommercialWithin2km)은 구미산동·양주덕정을 둘 다 반대로 봐서 자동화하지 않는다.
   * 출처: `storeEvalLabResidentRadius`(실험실 전용, scripts/writeLabResidentRadius.mjs). 운영 V62는 읽지 않는다.
   * ⚠️ 이 값은 계수가 아니라 자료다 — 적중률로 고르지 않는다. 근거 한 줄(note)이 없는 매장은 1km로 둔다.
   */
  residentRadiusM?: number | null;
  /**
   * **막힌 방향 수**(0~4) — 동·서·남·북 중 고리(1~2km) 구간이 하천·철도·고속도로·산으로 끊긴 방향의 개수.
   * AI 지도 판정(scripts/tradeArea/judge.mjs → storeEvalLabTradeAreaJudgments). null이면 판정 없음 → 안 깎는다.
   * `useRingEnclosure`가 켜졌을 때만 쓴다. 2026-09-23 신설.
   */
  ringBlockedDirections: number | null;
  /**
   * 주거인구의 남성비율(0~1). 연령별 이용률을 성비로 섞는 데 쓴다(blendUsageByGender).
   * 없으면 남녀 1:1로 본다.
   */
  residentMaleRatio: number | null;
  /** 유동인구의 반경별 남성비율(0~1). */
  floatingMaleRatioByRadius: Partial<Record<FloatingRadius, number | null>>;
  /** 유동 — 반경별. 아직 500만 채워진다. */
  floatingByRadius: Partial<Record<FloatingRadius, number | null>>;
  floatingAgesByRadius: Partial<Record<FloatingRadius, { age10s: number; age20s: number; age30s: number; age40s: number; age50s: number; age60plus: number } | null>>;
};

export type TextbookBreakdown = {
  /** 주거 이용자 — 1km + (켜져 있으면) 1km 밖 고리까지 합친 값. */
  residentDemandUsers: number | null;
  /** 그중 1km 밖 고리 몫. 고리를 끈 상태나 자료가 없으면 null. 화면에 "얼마가 밖에서 왔나"를 그린다. */
  residentRingUsers: number | null;
  /** 실제로 적용된 주거 상권 반경(m). 1000이 기본. 입력이 2000이어도 누적 자료가 없으면 1000이고 missing에 적힌다. */
  residentRadiusM: 1000 | ResidentRingRadius;
  /** 고리 수요에 적용된 점유율(입지 곱하기 전). "gravity"일 때만 값이 있고 "core"면 null(1km 안과 같다). */
  residentRingShare: number | null;
  floatingDemandUsers: number | null;
  totalDemandUsers: number | null;
  totalDemandHours: number | null;
  share: number | null;
  /** 입지가 점유율에 곱한 배율. 1이면 입지가 아무 일도 안 한 것(자료없음 또는 계수 0). */
  locationMultiplier: number | null;
  /** 어느 입지 항목이 얼마를 곱했는지 — 화면에 그대로 보여준다. */
  locationFactors: { key: string; value: number }[];
  ownDemandHours: number | null;
  utilization: number | null;
  capped: boolean;
  /** 이 매장에 적용된 총단가(원/PC·시간) = PC몫 + 상품몫. 화면에 그대로 보여준다. */
  unitPrice: number | null;
  /** PC몫(원/PC·시간). 정가가 여기에만 들어간다. */
  pcUnitPrice: number | null;
  pcRevenue: number | null;
  productRevenue: number | null;
  monthlyRevenue: number | null;
  /**
   * 상품매출 비율 — **결과값이다.** 2026-09-16 저녁(3)부터 파라미터가 아니다.
   * PC몫과 상품몫을 따로 계산하니 비율은 나눠 보면 나온다.
   */
  productRatio: number | null;
  /** 왜 값이 안 나왔는지. 화면에서 그대로 보여준다. */
  missing: string[];
};

const MONTH_HOURS = 24 * 30;

function weightedAges(
  ages: { age0s?: number; age10s: number; age20s: number; age30s: number; age40s: number; age50s: number; age60plus: number },
  w: AgeUsageWeights,
): number {
  return (ages.age0s ?? 0) * w.age0s + ages.age10s * w.age10s + ages.age20s * w.age20s
    + ages.age30s * w.age30s + ages.age40s * w.age40s + ages.age50s * w.age50s
    + ages.age60plus * w.age60plus;
}

/**
 * 연령 자료가 없을 때 총인구에 쓸 평균 이용률. 연령가중을 켠 매장과 끈 매장이 서로 다른
 * 자로 재지는 걸 막으려고, 가중치의 인구 가중평균이 아니라 **그냥 평균 이용률**을 쓴다
 * (전국 연령 분포를 모르는 상태에서 지어내지 않는다는 뜻이다).
 */
export function flatUsageRate(w: AgeUsageWeights): number {
  const vals = [w.age0s, w.age10s, w.age20s, w.age30s, w.age40s, w.age50s, w.age60plus];
  return vals.reduce((a, b) => a + b, 0) / vals.length;
}

/**
 * 경쟁점 **한 곳의 거리 무게**(0~1 남짓). 분모에 `ip x (품질비)^θ x 이 값`으로 들어간다.
 *
 * ⚠️ **산식과 화면이 반드시 같이 써야 하는 함수다.** 2026-09-21에 실험실 화면이 이 계산을
 *    따로 들고 있었고, 본체가 계단 -> 감쇠로 바뀐 뒤에도 화면만 계단으로 남아 "302m 경쟁점은
 *    안 셈"이라고 **거짓으로** 그리고 있었다. 한 곳에 두면 그 종류의 사고가 구조적으로 안 난다.
 *
 * - `rivalDistanceDecay`가 있으면: 평지 안 1배, 밖은 exp 감쇠, 전체에 총량 정규화를 곱한다.
 * - 없으면(null): 옛 계단 — `effectiveRadiusM` 안이면 1, 밖이면 0.
 * - 거리를 모르면 **1배(가장 가까운 것처럼)** 센다. 빼는 쪽이 아니라 세는 쪽이 보수적이다.
 */
export function rivalDistanceWeight(
  distanceM: number | null | undefined,
  p: Pick<TextbookParams, "rivalDistanceDecay" | "effectiveRadiusM">,
): number {
  const d = distanceM ?? null;
  const decay = p.rivalDistanceDecay;
  if (!decay) return d == null || d <= p.effectiveRadiusM ? 1 : 0;
  const raw = d == null || d <= decay.plateauM
    ? 1
    : decay.scaleM > 0 ? Math.exp(-(d - decay.plateauM) / decay.scaleM) : 0;
  return raw * (decay.weightFactor ?? 1);
}

export function computeTextbook(input: TextbookInput, p: TextbookParams): TextbookBreakdown {
  const missing: string[] = [];
  const empty: TextbookBreakdown = {
    residentDemandUsers: null, residentRingUsers: null, residentRadiusM: 1000, residentRingShare: null, floatingDemandUsers: null, totalDemandUsers: null,
    totalDemandHours: null, share: null, locationMultiplier: null, locationFactors: [], ownDemandHours: null, utilization: null,
    capped: false, unitPrice: null, pcUnitPrice: null, pcRevenue: null, productRevenue: null,
    monthlyRevenue: null, productRatio: null, missing,
  };

  // 연령별 이용률은 **그 자료의 지역 성비로 섞어서** 쓴다(2026-09-16). 주거와 유동은 성비가
  // 다르므로(주거 중앙 49.9%, 유동 중앙 57.9%) 각각 따로 섞는다.
  // 성비 자료가 없으면 남녀 1:1로 떨어진다(blendUsageByGender가 null을 0.5로 본다).
  // `?.`를 쓰는 이유: 이 필드는 2026-09-16에 추가돼서, 그 전에 만들어진 호출부는 아예 안 넘긴다.
  const residentWeights = blendUsageByGender(input.residentMaleRatio ?? null);
  const floatingWeights = blendUsageByGender(input.floatingMaleRatioByRadius?.[p.floatingRadius] ?? null);

  // ── 1) 주거 수요 ────────────────────────────────────────────────────────
  let residentUsers: number | null = null;
  // 주거 상권 반경(사람 확인 사실, 타입 쪽 `residentRadiusM`) — 1500·2000이면 그 반경의 누적 원을 1km 자리에 넣는다.
  // 고리와 다르다: 점유율·배수를 따로 두지 않고 **원만 넓힌다.** 자료가 없으면 1km로 떨어지고 missing에 적는다.
  const wantRadius = input.residentRadiusM ?? 1000;
  let appliedRadius: 1000 | ResidentRingRadius = 1000;
  const wideAges = wantRadius > 1000 && (wantRadius === 1500 || wantRadius === 2000 || wantRadius === 5000)
    ? input.residentAgesByRadius?.[wantRadius] ?? null : null;
  if (wantRadius > 1000 && !wideAges) missing.push(`${wantRadius}m 주거인구(상권 반경)`);
  if (p.residentRadius === 1000) {
    const ages = wideAges ?? input.residentAges;
    if (wideAges) appliedRadius = wantRadius as ResidentRingRadius;
    if (p.useResidentAgeWeights && ages) {
      residentUsers = weightedAges(ages, residentWeights);
    } else if (wideAges) {
      residentUsers = (wideAges.age0s + wideAges.age10s + wideAges.age20s + wideAges.age30s + wideAges.age40s + wideAges.age50s + wideAges.age60plus) * flatUsageRate(residentWeights);
    } else if (input.pop1km != null) {
      residentUsers = input.pop1km * flatUsageRate(residentWeights);
    } else missing.push("1km 주거인구");
  } else {
    // 500m는 연령 분해가 없다 — 총수 x 평균 이용률만 가능하다.
    if (input.pop500m != null) residentUsers = input.pop500m * flatUsageRate(residentWeights);
    else missing.push("500m 주거인구");
  }

  // ── 1b) 1km 밖 고리 (2026-09-23 신설 · 기본 꺼짐) ──────────────────────────
  // 누적 원 인구에서 안쪽 원을 빼 고리 인구를 만들고, 멀수록 덜 센다(타입 쪽 `residentRingDecayM` 주석).
  // 자료가 없으면 지어내지 않는다 — missing에 적고 1km만으로 간다.
  let ringUsers: number | null = null;
  /** 띠별 고리 이용자(무게 먹인 값)와 중간반경 — "gravity" 점유율에 쓴다. */
  const ringBands: { users: number; midM: number }[] = [];
  if (p.residentRingDecayM > 0 && p.residentRadius === 1000) {
    const cum = input.residentAgesByRadius;
    const core = input.residentAges;
    const usersOf = (a: ResidentAges) => p.useResidentAgeWeights
      ? weightedAges(a, residentWeights)
      : (a.age0s + a.age10s + a.age20s + a.age30s + a.age40s + a.age50s + a.age60plus) * flatUsageRate(residentWeights);
    if (!cum || !core) {
      missing.push("1km 밖 고리 인구");
    } else {
      // 상권 반경을 넓힌 매장은 고리를 **그 반경 밖**에서 시작한다 — 안쪽 띠는 이미 원에 들어 있다(이중계산 방지).
      let acc = 0, prev: ResidentAges = appliedRadius === 1000 ? core : (cum[appliedRadius] ?? core), any = false;
      for (const band of RESIDENT_RING_BANDS) {
        if (band.to <= appliedRadius) continue;
        const outer = cum[band.to];
        if (!outer) break; // 바깥 고리가 없으면 거기서 멈춘다(있는 고리까지만 센다)
        const ring: ResidentAges = {
          age0s: Math.max(0, outer.age0s - prev.age0s), age10s: Math.max(0, outer.age10s - prev.age10s),
          age20s: Math.max(0, outer.age20s - prev.age20s), age30s: Math.max(0, outer.age30s - prev.age30s),
          age40s: Math.max(0, outer.age40s - prev.age40s), age50s: Math.max(0, outer.age50s - prev.age50s),
          age60plus: Math.max(0, outer.age60plus - prev.age60plus),
        };
        // 막힌 상권 보정 — 막힌 방향의 고리 주민은 안 온다(타입 쪽 `useRingEnclosure`). 판정이 없으면 1배.
        const openFrac = p.useRingEnclosure && input.ringBlockedDirections != null
          ? Math.max(0, 1 - Math.min(4, Math.max(0, input.ringBlockedDirections)) / 4) : 1;
        const bandUsers = residentRingWeight(band.midM, p.residentRingDecayM) * usersOf(ring) * openFrac;
        acc += bandUsers;
        ringBands.push({ users: bandUsers, midM: band.midM });
        prev = outer; any = true;
      }
      if (any) { ringUsers = acc; residentUsers = (residentUsers ?? 0) + acc; } else missing.push("1km 밖 고리 인구");
    }
  }

  // ── 2) 유동 수요 ────────────────────────────────────────────────────────
  let floatingUsers: number | null = null;
  const flow = input.floatingByRadius[p.floatingRadius];
  const flowAges = input.floatingAgesByRadius[p.floatingRadius];
  if (flow == null) {
    missing.push(`${p.floatingRadius}m 유동인구`);
  } else if (p.useFloatingAgeWeights && flowAges) {
    floatingUsers = weightedAges(flowAges, floatingWeights) * p.floatingFactor;
  } else {
    floatingUsers = flow * flatUsageRate(floatingWeights) * p.floatingFactor;
  }

  if (residentUsers == null && floatingUsers == null) return empty;

  const baseUsers = (residentUsers ?? 0) + (floatingUsers ?? 0);
  // 상권 흡인력 — 경쟁점이 많다는 건 그 자리가 좋다는 뜻이기도 하다. 0이면 끈 것이다.
  const agg = p.agglomerationFactor > 0
    ? 1 + p.agglomerationFactor * Math.log(1 + (input.competitorCount ?? 0))
    : 1;
  // 주거 밀집도 보정 — 500m에 몰려 있는 상권을 과대평가하던 걸 상쇄한다.
  const dens = p.densityCorrection > 0 && input.pop500m && input.pop1km
    ? Math.pow(input.pop500m / input.pop1km, -p.densityCorrection)
    : 1;
  // 특수수요 배수 — 인구 통계에 안 잡히는 수요원(군부대·대학가·산업단지)을 수요에서 키운다.
  // 강도 문 — 문에 든 유형은 강도가 "높음"일 때만 곱한다(타입 쪽 `specialDemandHighOnly` 주석, 오송 오분류).
  const sdType = input.specialDemandType ?? "없음";
  const sdGated = (p.specialDemandHighOnly ?? []).includes(sdType) && input.specialDemandIntensity !== "높음";
  const sdMul = sdGated ? 1 : (p.specialDemandMultipliers?.[sdType] ?? 1);
  // 배수를 곱하는 항(타입 쪽 `specialDemandTerm`). 없으면 총수요 — 2026-09-24까지의 동작 그대로.
  const sdTerm = p.specialDemandTerm?.[sdType] ?? "total";
  const baseUsersMul = sdTerm === "resident" ? (residentUsers ?? 0) * sdMul + (floatingUsers ?? 0)
    : sdTerm === "floating" ? (residentUsers ?? 0) + (floatingUsers ?? 0) * sdMul
    : baseUsers * sdMul;
  const totalUsers = baseUsersMul * agg * dens;
  const totalHours = totalUsers * p.hoursPerUserPerMonth;

  // ── 3) 점유율 ───────────────────────────────────────────────────────────
  const pc = input.pcCount;
  if (!pc) { missing.push("PC대수"); return { ...empty, residentDemandUsers: residentUsers, residentRingUsers: ringUsers, residentRingShare: null, floatingDemandUsers: floatingUsers, totalDemandUsers: totalUsers, totalDemandHours: totalHours }; }
  const gap = input.competitivenessGap ?? 1;
  const rivalIp = input.competitorIp ?? 0;
  let ownWeight: number;
  let denom: number;
  if (p.shareMode === "quality") {
    // 품질 모드 — 유효거리 안의 경쟁점만 세고, 각자의 품질을 지수만큼 올려 겨룬다.
    //
    //   점유율 = 자사PC x 자사품질^θ ÷ (자사PC x 자사품질^θ + Σ[d≤R] 경쟁PC x 경쟁품질ᵢ^θ)
    //
    // 품질의 **기준값은 약분된다** — 분자·분모를 같은 수로 나눠도 비가 안 변하기 때문이다.
    // 그래서 "몇 점 만점"인지는 상관없고 자사/경쟁의 **비**만 작용한다.
    //
    // 자료가 없을 때는 "모른다 = 중립"으로 둔다. 자사 품질을 모르면 품질 항을 통째로 빼고
    // (비례 배분), 경쟁점 품질을 모르면 그 경쟁점을 자사와 같은 품질로 본다. 어느 쪽도
    // 결측을 유리/불리로 해석하지 않는다.
    // 비중은 조절판에서 바뀌므로 점수를 여기서 매번 만든다.
    const oq = input.ownQualityParts ? computeQualityScore(input.ownQualityParts, p.qualityWeights) : null;
    // 자사 품질로 정규화한다 — 그러면 자사 항이 정확히 pc가 되고 경쟁점만 배율을 갖는다.
    const ratio = (parts: QualityParts | null) => {
      if (oq == null || !(oq > 0) || !parts) return 1;
      const v = computeQualityScore(parts, p.qualityWeights);
      return v == null || !(v > 0) ? 1 : v / oq;
    };
    ownWeight = pc;
    let rivalWeight = 0;
    const decay = p.rivalDistanceDecay;
    for (const r of input.rivals ?? []) {
      if (!(r.ip > 0)) continue;
      // 거리를 모르면 유효거리 안으로 본다 — 경쟁점을 빼는 쪽이 아니라 세는 쪽이 보수적이다.
      // 감쇠를 켜면 계단 대신 exp(−d/scaleM)으로 센다(타입 쪽 주석 참고). 모르면 1배.
      // ⚠️ 무게 계산은 `rivalDistanceWeight` **한 곳**에만 둔다 — 화면(실험실 경쟁점 인식표)이
      //    같은 함수를 쓴다. 2026-09-21에 화면이 따로 계단을 구현해 두는 바람에, 산식은 302m를
      //    89%로 세는데 화면은 "안 셈"이라고 그리고 있었다.
      const w = rivalDistanceWeight(r.distanceM, p);
      if (w <= 0) continue;
      rivalWeight += r.ip * Math.pow(ratio(r.parts), p.qualityExponent) * w;
    }
    // 우리 몫 상한 — 자사 규모에 비례하는 "안 오는 몫"(타입 쪽 `ownShareCapK`). 경쟁이 없어도 1/(1+k)가 최대.
    denom = ownWeight + rivalWeight + p.outsideOptionIp + (p.ownShareCapK ?? 0) * pc;
  } else {
    ownWeight = pc * Math.pow(gap, p.gapExponent);
    // 분모의 outsideOptionIp는 "PC방을 안 가는 몫"자리다. **지금 기본값은 0이다**
    // (2026-09-22에 뺐다 — 파라미터 주석). 0이면 반경 안에 경쟁점이 하나도 없는 매장은
    // 점유율이 100%가 되어 동네 수요를 통째로 먹는다. 그게 지금 독점 3곳이 19% 과대인
    // 이유이고, 그 몫의 정체는 상수가 아니라 **아직 안 세고 있는 반경 밖 경쟁점**이다.
    denom = ownWeight + rivalIp + p.outsideOptionIp + (p.ownShareCapK ?? 0) * pc;
  }
  // shareMode="off"면 점유율을 아예 1로 둔다 — 경쟁 항을 통째로 들어낸 상태다.
  // 그러면 화면의 "필요 점유율"(실측가동률 ÷ 이 예측)이 **이 매장이 실제로 먹은 몫**이 되고,
  // 그 값으로 수요식을 2차 검증할 수 있다(2026-09-16 사용자 설계).
  const coreShare = p.shareMode === "off" ? 1 : (denom > 0 ? ownWeight / denom : 1);

  // ── 3b) 고리 수요의 점유율 — "gravity"면 띠마다 따로 구해 이용자 수로 가중평균 ─────────
  // 1km 안 사람의 점유율(coreShare)은 그대로 두고, 고리 사람에게는 "우리와 그 근처 PC방 중 가까운
  // 쪽으로 간다"를 적용한다(타입 쪽 `residentRingShare` 주석). 경쟁점의 품질비^θ는 1km 안과 같은 값.
  let ringShare: number | null = null;
  let rawShare = coreShare;
  if (p.shareMode === "quality" && p.residentRingShare === "gravity" && ringBands.length && ringUsers && ringUsers > 0) {
    const oq2 = input.ownQualityParts ? computeQualityScore(input.ownQualityParts, p.qualityWeights) : null;
    const ratio2 = (parts: QualityParts | null) => {
      if (oq2 == null || !(oq2 > 0) || !parts) return 1;
      const v = computeQualityScore(parts, p.qualityWeights);
      return v == null || !(v > 0) ? 1 : v / oq2;
    };
    let ringOwnHoursW = 0;
    for (const b of ringBands) {
      if (!(b.users > 0)) continue;
      let rw = 0;
      for (const r of input.rivals ?? []) {
        if (!(r.ip > 0)) continue;
        const g = ringRivalFraction(r.distanceM, b.midM);
        if (g <= 0) continue;
        // 고리 사람에겐 품질이 덜 작용한다(타입 쪽 `ringQualityExponent`). 없으면 θ 그대로.
        rw += r.ip * Math.pow(ratio2(r.parts), p.ringQualityExponent ?? p.qualityExponent) * g;
      }
      const sb = pc / (pc + rw + p.outsideOptionIp + (p.ownShareCapK ?? 0) * pc);
      ringOwnHoursW += b.users * sb;
    }
    ringShare = ringOwnHoursW / ringUsers;
    // 1km 안(주거 1km + 유동)과 고리를 이용자 수로 가중평균 — 배수(흡인력·밀집도·특수수요)는 양쪽에
    // 똑같이 걸리므로 비에는 영향이 없다.
    const coreUsers = Math.max(0, baseUsers - ringUsers);
    rawShare = baseUsers > 0 ? (coreUsers * coreShare + ringUsers * ringShare) / baseUsers : coreShare;
  }

  // ── 입지 — 점유율에 곱한다 (2026-09-17) ────────────────────────────────
  // 경쟁력점수 안이 아니라 **밖**이다. 접근성은 "경쟁점보다 낮은 층인가"가 아니라
  // "올라오기 얼마나 번거로운가"라서, 비율이 아니라 곱셈이 맞다. 타입 쪽 주석 참고.
  //
  // 자료가 없으면 그 항은 1배로 빠진다 — **없는 값을 지어내지 않는다.**
  // shareMode="off"일 때도 빠진다. "off"는 경쟁·입지를 통째로 들어내 필요 점유율을 보는 모드다.
  const locFactors: { key: string; value: number }[] = [];
  if (p.shareMode !== "off" && input.location) {
    const L = input.location, E = p.locationExponents, R = p.locationReferences;
    const mul = (key: string, v: number | null, exp: number, ref: number) => {
      if (v == null || !Number.isFinite(v) || !(v > 0) || !(ref > 0) || exp === 0) return;
      locFactors.push({ key, value: Math.pow(v / ref, exp) });
    };
    // 중심도는 **잔차화해서** 넣는다 — 날값에는 동네 규모(유동400m)가 섞여 있어 수요식과
    // 이중계산이 된다. 자세한 근거와 상수의 성격은 `centralityResidual` 타입 주석에 있다.
    // ⚠️ 유동400m가 없으면 **항을 통째로 뺀다**(null). 날값으로 되돌리지 않는다 —
    //    ν=0.5는 잔차 눈금에 맞춘 값이라 날값에 먹이면 딴 걸 재게 된다.
    const CR = p.centralityResidual;
    const f400 = input.floatingByRadius[400] ?? null;
    const centrality = (() => {
      if (!CR) return L.centrality; // 잔차화를 끈 상태 — 옛 동작(ν도 0.25로 돌릴 것)
      if (L.centrality == null || !(L.centrality > 0)) return null;
      if (f400 == null || !(f400 > 0)) return null;
      return R.centrality * (L.centrality / CR.geoMeanCentrality)
        * Math.pow(CR.geoMeanFloating400 / f400, CR.slope);
    })();
    mul("centrality", centrality, E.centrality, R.centrality);
    mul("access", L.access, E.access, R.access);
    // 편심도는 클수록 불리하므로 (1 - 편심도)를 쓴다. 기준 0.75는 실측 중앙 편심도 0.25의 여집합.
    if (L.direction != null && Number.isFinite(L.direction) && E.direction !== 0) {
      const room = Math.max(0.01, 1 - L.direction);
      locFactors.push({ key: "direction", value: Math.pow(room / 0.75, E.direction) });
    }
    mul("flowBlock", L.flowBlock, E.flowBlock, 3);
    mul("visibility", L.visibility, E.visibility, 3);
  }
  const locationMultiplier = locFactors.reduce((a, f) => a * f.value, 1);

  // ── 4) 가동률 — 순서대로 읽히게 쓴다 ────────────────────────────────────
  //
  // 사용자 요청(2026-09-21): 보정을 **수요 구하는 자리**로 옮겨 달라. 전에는 계산을 다 한 뒤
  // 맨 끝에 지수를 붙여서 "가동률에 제곱이 붙네?"로 잘못 읽혔다. 결과 숫자는 같고 순서만 바꿨다.
  //
  //   (1) 날 가동률 = 수요시간 ÷ 총공급 ÷ 720      ← 순수한 산수. 손 안 댄다
  //   (2) 눈금 보정 = 그 값의 눈금만 고친다         ← 수요·총공급이 구성 지수라서
  //   (3) 입지배율을 곱한다
  //   (4) 두 상한 — 동네 수요보다 많이 못 먹는다 · 좌석이 모자라 못 받는다

  const cal = p.indexCalibration;
  // 입지 지수를 c÷b로 넣는다 — 아래 (3)에서 전체를 b제곱하므로 **최종 지수가 c**가 된다.
  const locExpInShare = cal && cal.ratioExponent !== 0 ? cal.locationExponent / cal.ratioExponent : 1;

  // (1) **우리 몫** — 경쟁과 입지로 정해진다. 1을 넘을 수 없다(동네 수요보다 많이 못 먹는다).
  //     ⚠️ 사용자 확인(2026-09-21): *"입지 때문에 수요가 늘어나진 않잖아, 수요에서 점유율에
  //        작용하는 부분 아님?"* — 맞다. 그래서 입지는 여기, 점유율에 곱한다.
  const share = Math.min(1, rawShare * Math.pow(locationMultiplier, locExpInShare));

  // (2) **날 가동률** — 여기까지가 순수한 산수다. 손 안 댄다.
  //       우리 이용시간 = 수요시간 x 우리 몫 ·  가동률 = 그 시간 ÷ (자사PC x 720)
  const rawUtil = totalHours * share / (pc * MONTH_HOURS);

  // (3) **눈금 보정** — 산수를 고치는 게 아니라 **들어간 추정치의 눈금**을 고친다.
  //     수요도 총공급도 우리가 만든 지수라 실측할 방법이 없고, 매장 간 차이를 과장한다
  //     (자세한 근거는 타입 쪽 `indexCalibration` 주석).
  //     닻(referenceUtilization)에서 배율이 정확히 1이라 그 값은 안 움직이고,
  //     그보다 낮게 나온 건 올리고 높게 나온 건 내린다 — **전 매장 같은 곡선 하나**다.
  //     ⚠️ 매장별로 맞추지 않는다(사용자 확인: *"매장마다 수요를 값에 맞추려고 하지 말고
  //        일관된 산식으로"*). 쓰이는 수는 ratioExponent·referenceUtilization **둘뿐**이다.
  //     ⚠️ 순서를 "수요 구하는 자리"로 옮기는 건 **수학적으로 안 된다** — 2026-09-21에
  //        해 봤다가 축척 적합이 망가졌다(상한이 배율 1에서 엉뚱하게 걸려 133배가 나왔다).
  //        (1)의 상한을 먼저 먹인 뒤 보정해야 한다.
  const calibratedUtil = cal && rawUtil > 0
    ? Math.pow(cal.referenceUtilization, 1 - cal.ratioExponent) * Math.pow(rawUtil, cal.ratioExponent)
    : rawUtil;

  // (4) 좌석이 모자라 못 받는 상한.
  const capHours = pc * MONTH_HOURS * p.maxUtilization;
  const rawOwnHours = calibratedUtil * pc * MONTH_HOURS;
  const capped = rawOwnHours > capHours;
  const ownHours = Math.min(rawOwnHours, capHours);
  const utilization = ownHours / (pc * MONTH_HOURS);

  // ── 총단가 = PC몫 + 상품몫 ─────────────────────────────────────────────
  //
  //   PC몫   = 기준정가 x (정가 / 기준정가)^β   <- 정가가 여기에만 들어간다
  //   상품몫 = 상수                             <- 정가와 무관하다 (라면 값은 PC요금을 안 따라간다)
  //
  // 2026-09-16 저녁(3) 이전에는 총단가 전체에 정가를 곱했다. 그러면 정가를 80% 올릴 때
  // 총단가가 38% 오르는데, 상품매출은 안 오르므로 과하다(덧셈 구조에서는 17%).
  const rate = input.hourlyRate;
  const ref = p.referenceHourlyRate;
  if (rate == null) missing.push("시간당 요금");
  // 요금을 모르면 기준정가(표본 중앙)인 매장으로 가정한다. 화면에 "자료없음"으로 표시된다.
  const effRate = rate == null || !(ref > 0)
    ? ref
    : ref * Math.pow(rate / ref, p.rateElasticity);
  const pcUnitPrice = effRate;
  const unitPrice = pcUnitPrice + p.productUnitPrice;
  const pcRevenue = ownHours * pcUnitPrice;
  const productRevenue = ownHours * p.productUnitPrice;
  const monthlyRevenue = unitPrice > 0 ? pcRevenue + productRevenue : null;

  return {
    residentDemandUsers: residentUsers,
    residentRingUsers: ringUsers,
    residentRadiusM: appliedRadius,
    residentRingShare: ringShare,
    floatingDemandUsers: floatingUsers,
    totalDemandUsers: totalUsers,
    totalDemandHours: totalHours,
    share,
    locationMultiplier,
    locationFactors: locFactors,
    ownDemandHours: ownHours,
    utilization,
    capped,
    unitPrice,
    pcUnitPrice,
    pcRevenue,
    productRevenue,
    monthlyRevenue,
    productRatio: monthlyRevenue != null && monthlyRevenue > 0 ? productRevenue / monthlyRevenue : null,
    missing,
  };
}

/**
 * ── 축척은 **두 개**다. 층마다 실측값이 따로 있으니 따로 맞춘다 (2026-09-16 구조 변경) ──
 *
 * 그전에는 축척 하나(hoursPerUserPerMonth)를 실매출에 맞춰서 두 일을 겸하게 했다. 그랬더니
 * 환산층(정가 ÷ (1-상품비율))이 매장마다 −12.7%~+19.2% 틀린 만큼이 **가동률로 되밀려
 * 들어갔다** — 독점 3곳 가동률이 화면에서 −3.2~−5.3% 어긋나 보이던 원인이 이것이다.
 * 수요식은 멀쩡했는데 환산층 오차를 대신 뒤집어쓰고 있었다.
 *
 *   1) hoursPerUserPerMonth ← **가맹점 원장 실측** 8.20h  (2026-09-21부터. 안 맞춘다)
 *   2) productUnitPrice     ← **전 매장** 실매출·실측가동률 (fitProductUnitPrice)
 *
 * 순서가 중요하다. 1)을 먼저 정해 가동률을 고정한 뒤, 2)가 남은 몫만 맡는다.
 *
 * ⚠️ 1)은 2026-09-16~09-21 사이 독점매장 실측 가동률에서 **역산**했다. 지금은 원장으로
 *    직접 재서 못 박는다 — 근거는 `hoursPerUserFixed` 타입 주석. `false`로 두면 옛 역산.
 */

/** 독점매장이 있으면 거기서만 고른다. 없으면 전체를 쓴다. */
function calibrationTarget<T extends { input: TextbookInput }>(rows: T[]): T[] {
  // 독점상권(경쟁IP=0)은 점유율이 1이라 격차^gamma가 약분되고 **수요식만 남는다.** 그래서
  // 축척을 여기서 정하면 경쟁 항의 오차가 축척으로 스며들지 않는다. 전체로 맞추면 반대가
  // 된다 — 경쟁 항이 틀린 몫까지 축척이 흡수해서 "수요가 맞는지"를 영영 못 가른다.
  //
  // 사용자(2026-09-16): "일단 독점매장이 무조건 맞아야 돼 산식이. 그 개념이 맞잖아."
  // 실제로 2026-09-16에 이걸 안 하고 전체로 맞췄더니 광주각화점(독점)이 90.9% 과대예측됐다.
  const monopoly = rows.filter((r) => !(r.input.competitorIp ?? 0));
  return monopoly.length > 0 ? monopoly : rows;
}

const logMean = (xs: number[]) => Math.exp(xs.reduce((a, b) => a + b, 0) / xs.length);

/**
 * 수요 축척(hoursPerUserPerMonth)을 정한다.
 *
 * **2026-09-21부터 기본은 '안 맞추는 것'이다** — `hoursPerUserFixed: true`면 원장 실측값을
 * 그대로 돌려준다. 1인당 이용시간은 가맹점 원장으로 **직접 잴 수 있는 값**이라, 역산으로
 * 구하면 잴 수 있는 것을 굳이 추정하고 그 자리에 수요식 오차까지 숨기게 된다.
 * 근거와 값은 `hoursPerUserFixed` 타입 주석에 있다.
 *
 * 아래는 `hoursPerUserFixed: false`일 때의 옛 경로다 — 독점매장 **실측 가동률**에 맞춘다.
 * 실측 가동률이 하나도 없으면 매출로 떨어진다(더 옛 방식). 그 경우 가동률과 매출을 한 축척이
 * 겸하게 되므로 위에 적은 되밀림이 다시 생긴다 — 반환값만 보고 넘어가지 말 것.
 */
export function fitHoursPerUser(
  rows: { input: TextbookInput; actualRevenue: number }[],
  p: TextbookParams,
): number {
  // 원장 실측으로 못 박은 상태 — 자료에 맞추지 않는다. 이게 기본이다.
  if (p.hoursPerUserFixed) return p.hoursPerUserPerMonth;
  const target = calibrationTarget(rows);

  const byUtil: number[] = [];
  for (const r of target) {
    const actual = r.input.actualUtilization;
    if (actual == null || !(actual > 0)) continue;
    const b = computeTextbook(r.input, { ...p, hoursPerUserPerMonth: 1, maxUtilization: Number.POSITIVE_INFINITY });
    if (b.utilization == null || b.utilization <= 0) continue;
    byUtil.push(Math.log(actual / b.utilization));
  }
  // ⚠️ 지수 보정이 켜져 있으면 예측이 hours^b에 비례한다 — 그래서 **b로 나눠야** 맞는 배율이
  //    나온다(2026-09-21). 안 나누면 축척이 b배만큼 어긋나고 전 매장 수준이 틀어진다.
  const bExp = p.indexCalibration?.ratioExponent ?? 1;
  if (byUtil.length) return Math.exp(Math.log(logMean(byUtil)) / (bExp !== 0 ? bExp : 1));

  // ── 되돌림: 실측 가동률이 없으면 매출로 맞춘다 (예전 방식) ──────────────────────
  const byRevenue: number[] = [];
  for (const r of target) {
    const b = computeTextbook(r.input, { ...p, hoursPerUserPerMonth: 1 });
    if (b.monthlyRevenue == null || b.monthlyRevenue <= 0 || !(r.actualRevenue > 0)) continue;
    // 상한에 걸린 매장은 배율을 키워도 매출이 안 늘어 배율 추정을 왜곡한다 — 제외한다.
    if (b.capped) continue;
    byRevenue.push(Math.log(r.actualRevenue / b.monthlyRevenue));
  }
  if (!byRevenue.length) return p.hoursPerUserPerMonth;
  return logMean(byRevenue);
}

/**
 * 상품몫(productUnitPrice)을 **실측 가동률이 있는 전 매장**에서 구한다.
 *
 * 덧셈 구조라 배수가 아니라 **빼서** 구한다 — PC몫은 정가에서 이미 정해졌으니, 실매출에서
 * PC몫을 빼고 남는 게 상품몫이다.
 *
 *   상품몫 = 평균( 실매출 ÷ (PC x 720 x 실측가동률) − PC몫 )
 *
 * 남는 오차가 곧 "세 독점매장이 PC·시간당 실제로 얼마나 다르게 버는가"다. 2026-09-16 실측으로
 * 총단가가 탕정역 3,206원 · 광주각화 2,853원 · 남악 2,745원이라 16.8% 벌어져 있고, **그 차이의
 * 75%가 상품몫**이다(탕정역 1,591원 vs 남악 1,247원). 후보지에서 알 수 있는 값으로는 설명되지
 * 않았다 — 그래서 독점 최대오차 10.6%가 지금 자료의 바닥이다.
 */
export function fitProductUnitPrice(
  rows: { input: TextbookInput; actualRevenue: number }[],
  p: TextbookParams,
): number {
  // ── 실측 가동률이 있으면 **전 매장**을 쓴다 (2026-09-16 저녁(3) 수정) ──────────────
  //
  // 상품몫은 수요와 달리 **직접 측정된다** — 실매출 ÷ (PC x 720 x 실측가동률) − PC몫.
  // 산식을 한 번도 거치지 않으니 독점이라는 지렛대가 필요 없다.
  //
  // 사용자(2026-09-16): "정액권비중은 독점매장비율로쓰면안될거같은데, 전체매장으로해야하지
  // 않음? 평균값이라" — 맞다. 평균으로 쓸 값이면 표본이 많을수록 낫다.
  //
  // ⚠️ 그전에는 **모형이 예측한** 이용시간(ownDemandHours)으로 나눴다. 경쟁상권 매장은 그
  //    값이 29% 틀려서 독점으로 제한할 수밖에 없었다. 실측 가동률로 나누면 그 제약이 사라진다.
  //    독점 3곳 1,492원 vs 전체 32곳 1,508원으로 값 자체는 1%밖에 안 달랐지만, 표본이 3곳에서
  //    32곳으로 늘고 독점 매출 최대오차도 10.6% → 10.1%로 조금 나아진다.
  //
  // ⚠️ **2026-09-21 밤부터 기본은 '안 맞추는 것'이다** — `productUnitPriceFixed: true`면
  //    아래를 돌지 않고 상수를 그대로 돌려준다. 표본이 바뀔 때마다 산식이 움직이는 걸 막는다.
  //    근거는 `productUnitPriceFixed` 타입 주석. 아래는 `false`일 때의 옛 경로다.
  //    ⚠️ 옛 경로는 **잔차**로 구한다(실매출 − PC몫). 지금 고정값은 `productSales`를 직접
  //       잰 값이라 출처가 다르다 — 평균은 같지만 퍼짐이 다르다. 되살릴 때 헷갈리지 말 것.
  if (p.productUnitPriceFixed) return p.productUnitPrice;
  const measured: number[] = [];
  for (const r of rows) {
    const au = r.input.actualUtilization;
    const pc = r.input.pcCount;
    if (au == null || !(au > 0) || !pc || !(r.actualRevenue > 0)) continue;
    const b = computeTextbook(r.input, { ...p, productUnitPrice: 0 });
    if (b.pcUnitPrice == null) continue;
    measured.push(r.actualRevenue / (pc * MONTH_HOURS * au) - b.pcUnitPrice);
  }
  if (measured.length) return Math.max(0, measured.reduce((a, b) => a + b, 0) / measured.length);

  // ── 되돌림: 실측 가동률이 하나도 없으면 독점매장에서 모형 이용시간으로 맞춘다 ──────
  const target = calibrationTarget(rows);
  const shares: number[] = [];
  for (const r of target) {
    // 상품몫을 0으로 두고 계산하면 PC몫과 자사이용시간만 남는다.
    const b = computeTextbook(r.input, { ...p, productUnitPrice: 0 });
    if (b.ownDemandHours == null || b.ownDemandHours <= 0 || !(r.actualRevenue > 0)) continue;
    if (b.capped) continue;
    shares.push(r.actualRevenue / b.ownDemandHours - (b.pcUnitPrice ?? 0));
  }
  if (!shares.length) return p.productUnitPrice;
  // 상품몫이 음수면 PC몫이 실매출보다 크다는 뜻이다 — 0으로 막는다.
  return Math.max(0, shares.reduce((a, b) => a + b, 0) / shares.length);
}

export type TextbookScore = {
  sampleCount: number;
  mape: number | null;
  medianAbsErr: number | null;
  within10: number | null;
  within20: number | null;
  maxAbsErr: number | null;
  /**
   * 실제로 쓰인 hoursPerUserPerMonth. 화면에 그대로 보여준다.
   * `hoursPerUserFixed`면 원장 실측값이 그대로 나오고, 끄면 옛 역산값이 나온다.
   */
  fittedHoursPerUser: number;
  /** 전 매장 실측으로 구한 productUnitPrice(상품몫, 원/PC·시간). 화면에 그대로 보여준다. */
  fittedProductUnitPrice: number;
  /**
   * 수요 축척이 **매출로 떨어지지 않았는지**. false면 가동률과 매출을 한 축척이 겸하게 되어
   * 두 층이 다시 엉킨다(화면에 경고를 띄운다).
   * 원장 실측으로 못 박은 상태(`hoursPerUserFixed`)면 그럴 일이 없으므로 항상 true다.
   */
  scaledOnUtilization: boolean;
  /** 가동률 성적 — 실측 가동률이 있는 매장만. 매출과 따로 봐야 층별 판정이 된다. */
  utilizationMape: number | null;
  utilizationSampleCount: number;
  /**
   * 가동률 **절대 오차(%p)** 성적 — 판정 기준은 이것이다(2026-09-21부터 가동률 우선).
   * 값은 비율(0.03 = 3%p)이다. 화면에서 x100 해서 %p로 적는다.
   *   utilizationMaePoints      평균 |예상 − 실측|
   *   utilizationMaxAePoints    최악 매장
   *   utilizationWithinTarget   |예상 − 실측| ≤ UTILIZATION_TARGET_MAE_POINTS 인 매장 수
   *   utilizationBaselineMaePoints  '전부 평균' 바닥 — 실측 평균 하나로 전부 찍었을 때의 MAE.
   *                                 산식이 이걸 못 이기면 아무것도 안 한 것과 같다.
   */
  utilizationMaePoints: number | null;
  utilizationMaxAePoints: number | null;
  utilizationWithinTarget: number;
  utilizationBaselineMaePoints: number | null;
  /**
   * **필요 점유율** 요약 — 이 매장이 실제로 먹은 몫. 수요식 2차 검증용이다.
   * 필요 점유율 = 실측가동률 ÷ (경쟁 없다고 볼 때의 예측 가동률)
   * 수요식이 맞다면 0~1 안에 들어야 한다. 1을 넘으면 그 동네 수요를 과소평가한 것이다.
   */
  requiredShare: { count: number; median: number; min: number; max: number; overOne: number } | null;
  rows: {
    storeCode: string; storeName: string | null;
    predicted: number | null; actual: number; absErrPct: number | null;
    utilization: number | null; actualUtilization: number | null; utilErrPct: number | null;
    share: number | null; capped: boolean;
    /** 적용된 주거 상권 반경(m). 1000이 기본, 사람이 확인한 매장만 1500·2000. 화면 비고에 배지로 그린다. */
    residentRadiusM: 1000 | ResidentRingRadius;
    unitPrice: number | null; pcUnitPrice: number | null; productRatio: number | null;
    /** 경쟁이 없다고 볼 때의 예측 가동률 — 필요 점유율의 분모다. */
    utilizationNoShare: number | null;
    /** 필요 점유율 = 실측가동률 ÷ utilizationNoShare. 실측 가동률이 없으면 null. */
    requiredShare: number | null;
    missing: string[];
  }[];
};

/** 38곳에 대고 점수를 낸다. 배율(hoursPerUserPerMonth)은 여기서 자동으로 맞춘다. */
/**
 * **가동률 목표 오차 — 평균 3%p** (2026-09-23 사용자 확정: "한 3프로 잡아야겠네").
 *
 * ── 왜 3인가 ──────────────────────────────────────────────
 * '전부 평균'(실측 평균 가동률 하나로 전부 찍기) 바닥이 이미 MAE 4.24%p다(2026-09-23,
 * 실측 26곳 · 실험실 40곳 4.26%p). 5%p를 목표로 두면 산식 없이도 26곳 중 20곳이 통과해서
 * "산식이 뭘 했다"는 증명이 못 된다. 4%p는 바닥과 같은 선이다. 3%p라야 바닥을 1.2%p
 * 이상 벌리고, 그건 40곳 2SE(0.70%p)로도 가려낼 수 있는 크기다.
 *
 * ── 매출로 얼마인가 ────────────────────────────────────────
 * 1%p = PC x 720h x 0.01 x 총단가. 평균 매장(99대 · 총단가 2,600원)에서 월 약 195만원,
 * 매출 비율로 약 3.2%(1%p ÷ 평균 가동률 31%). 그래서 3%p ≈ 매출 ±10%로 기존 MAPE 목표와
 * 한 자리다. 매장 크기에 비례해서 76대는 130만원, 168대는 255만원이다.
 *
 * 화면·문서는 이 상수를 읽어 그린다. 숫자를 글자로 박지 않는다(CLAUDE.md).
 */
export const UTILIZATION_TARGET_MAE_POINTS = 0.03;

export function scoreTextbook(
  rows: { input: TextbookInput; actualRevenue: number }[],
  p: TextbookParams,
): TextbookScore {
  // 축척 둘. 1) 수요 축척은 **가맹점 원장 실측**으로 못 박혀 있다(2026-09-21부터. 그전에는
  // 독점 실측 가동률에서 역산했다), 2) 상품몫은 **전 매장** 실측 가동률로 직접 측정한다.
  const fitted = fitHoursPerUser(rows, p);
  const scaledOnUtilization = p.hoursPerUserFixed || rows.some((r) => (r.input.actualUtilization ?? 0) > 0);
  const withFit: TextbookParams = { ...p, hoursPerUserPerMonth: fitted };
  const fittedProductUnitPrice = fitProductUnitPrice(rows, withFit);
  const full: TextbookParams = { ...withFit, productUnitPrice: fittedProductUnitPrice };

  const out: TextbookScore["rows"] = [];
  const errs: number[] = [];
  const utilErrs: number[] = [];
  // 가동률 절대 오차(%p 판정용)와 실측값 — 실측값은 '전부 평균' 바닥을 재는 데 쓴다.
  const utilPointErrs: number[] = [];
  const utilActuals: number[] = [];
  const reqShares: number[] = [];
  for (const r of rows) {
    const b = computeTextbook(r.input, full);
    const err = b.monthlyRevenue != null && r.actualRevenue > 0
      ? Math.abs(b.monthlyRevenue - r.actualRevenue) / r.actualRevenue
      : null;
    if (err != null) errs.push(err);
    const au = r.input.actualUtilization;
    const utilErr = b.utilization != null && au != null && au > 0
      ? Math.abs(b.utilization - au) / au
      : null;
    if (utilErr != null) utilErrs.push(utilErr);
    if (b.utilization != null && au != null && au > 0) {
      utilPointErrs.push(Math.abs(b.utilization - au));
      utilActuals.push(au);
    }
    // **필요 점유율** — 경쟁을 아예 없다고 본 예측 가동률로 실측을 나눈 값이다.
    // 이게 "이 매장이 실제로 먹은 몫"이고, 수요식 2차 검증의 자다(2026-09-16 사용자 설계).
    // 상한을 무한대로 두는 이유: 상한에 걸리면 분모가 잘려 필요 점유율이 부풀려진다.
    const bNo = computeTextbook(r.input, { ...full, shareMode: "off", maxUtilization: Number.POSITIVE_INFINITY });
    const req = bNo.utilization != null && bNo.utilization > 0 && au != null && au > 0
      ? au / bNo.utilization
      : null;
    if (req != null) reqShares.push(req);
    out.push({
      storeCode: r.input.storeCode, storeName: r.input.storeName,
      predicted: b.monthlyRevenue, actual: r.actualRevenue, absErrPct: err,
      utilization: b.utilization, actualUtilization: au ?? null, utilErrPct: utilErr,
      share: b.share, capped: b.capped, residentRadiusM: b.residentRadiusM, unitPrice: b.unitPrice, pcUnitPrice: b.pcUnitPrice,
      productRatio: b.productRatio, utilizationNoShare: bNo.utilization, requiredShare: req,
      missing: b.missing,
    });
  }
  const sorted = [...errs].sort((a, b) => a - b);
  const reqSorted = [...reqShares].sort((a, b) => a - b);
  return {
    sampleCount: errs.length,
    mape: errs.length ? errs.reduce((a, b) => a + b, 0) / errs.length : null,
    medianAbsErr: sorted.length ? sorted[Math.floor(sorted.length / 2)] : null,
    within10: errs.length ? errs.filter((v) => v <= 0.1).length / errs.length : null,
    within20: errs.length ? errs.filter((v) => v <= 0.2).length / errs.length : null,
    maxAbsErr: sorted.length ? sorted[sorted.length - 1] : null,
    fittedHoursPerUser: fitted,
    fittedProductUnitPrice,
    scaledOnUtilization,
    utilizationMape: utilErrs.length ? utilErrs.reduce((a, b) => a + b, 0) / utilErrs.length : null,
    utilizationSampleCount: utilErrs.length,
    utilizationMaePoints: utilPointErrs.length ? utilPointErrs.reduce((a, b) => a + b, 0) / utilPointErrs.length : null,
    utilizationMaxAePoints: utilPointErrs.length ? Math.max(...utilPointErrs) : null,
    utilizationWithinTarget: utilPointErrs.filter((v) => v <= UTILIZATION_TARGET_MAE_POINTS).length,
    // '전부 평균' 바닥: 실측 평균 하나로 전부 찍었을 때의 평균 |실측 − 평균|.
    utilizationBaselineMaePoints: utilActuals.length
      ? (() => {
          const mean = utilActuals.reduce((a, b) => a + b, 0) / utilActuals.length;
          return utilActuals.reduce((a, b) => a + Math.abs(b - mean), 0) / utilActuals.length;
        })()
      : null,
    requiredShare: reqSorted.length ? {
      count: reqSorted.length,
      median: reqSorted[Math.floor(reqSorted.length / 2)],
      min: reqSorted[0],
      max: reqSorted[reqSorted.length - 1],
      overOne: reqSorted.filter((v) => v > 1).length,
    } : null,
    rows: out.sort((a, b) => (b.absErrPct ?? 0) - (a.absErrPct ?? 0)),
  };
}

/**
 * 채점에서 **맞춰진 축척까지 먹인 파라미터 한 벌**. 실매출이 없는 곳(신규후보지)에
 * `computeTextbook`을 직접 돌릴 때 쓴다 (2026-09-18).
 *
 * ── 왜 함수로 두나 ─────────────────────────────────────────────────────────
 * 부르는 쪽에서 `{ ...p, hoursPerUserPerMonth: score.fittedHoursPerUser, ... }`를 손으로
 * 조립하면, 나중에 축척이 하나 더 늘 때 **부르는 자리를 전부 찾아 고쳐야 한다.** 한 군데라도
 * 빠지면 그 화면만 옛 축척으로 돌아가는데, 값이 그럴듯해서 티가 안 난다. 조립을 한 곳에 둔다.
 *
 * ⚠️ 축척은 **실매출·실측가동률이 있는 기존점에서만** 맞춘다. 후보지 행으로 `scoreTextbook`을
 *    부르지 말 것 — 맞출 실측값이 없어서 되돌림 경로로 새어 들어가고, 결국 예측값으로
 *    예측값을 맞추는 순환이 된다.
 */
export function fittedParams(p: TextbookParams, score: TextbookScore): TextbookParams {
  return {
    ...p,
    hoursPerUserPerMonth: score.fittedHoursPerUser,
    productUnitPrice: score.fittedProductUnitPrice,
  };
}
